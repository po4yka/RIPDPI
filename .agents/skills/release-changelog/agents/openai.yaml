interface:
  display_name: "Release Changelog"
  short_description: "Collect verified evidence for RIPDPI release notes"
  default_prompt: "Use $release-changelog to collect a read-only evidence pack and draft release notes for RIPDPI 0.1.4."
policy:
  allow_implicit_invocation: true
