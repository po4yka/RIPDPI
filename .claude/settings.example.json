{
  "//": "Optional per-developer settings. Merge only the entries you need into gitignored .claude/settings.local.json; project hooks are committed in .claude/settings.json.",
  "enabledPlugins": {
    "codex@openai-codex": true
  },
  "permissions": {
    "allow": [
      "Bash(android:*)"
    ]
  },
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "mcpServers": {
    "rust-analyzer": {
      "//": "Install zeenix/rust-analyzer-mcp or replace command with another compatible server.",
      "command": "rust-analyzer-mcp",
      "args": [],
      "cwd": "${workspaceFolder}/native/rust"
    }
  }
}
