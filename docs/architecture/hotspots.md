# Hotspot Inventory

Generated by `scripts/ci/generate_hotspots_doc.py`. Content is derived purely from `config/static/file-loc-baseline.json` plus the current repo state; regenerate after any refactor. Code-only line counts strip blanks and comments.

No baseline-exempt files are currently recorded.
