# Feature Test Completion Audit

Status: **not complete**.

This audit keeps the path expected by `test-lab/scripts/check-feature-test-signoff.sh`, while the current evidence was refreshed on 2026-05-19 from the connected non-rooted Pixel 8 Pro and local lab tooling, then updated through the tested PR #118 head `0c745ec9`, the 2026-05-20 `main` continuation on non-rooted Pixel 7, and the local `act`/native CI mirror plus rooted raw strategy work through `337b68cc`. Earlier Pixel 8 Pro artifacts remain valid for the recorded pass, and Pixel 7 now provides current physical readiness plus adb-reverse packet-smoke evidence. Direct device-profile MacBook LAN diagnostics remain blocked because the Mac is currently on `172.20.10.5/28` while Pixel 7 is on `192.168.1.3/24`; the probe wrapper now detects and ignores stale `MACBOOK_LAN_IP` values before reporting that topology block. The `RIPDPI_Root_API34` AVD is Magisk-rooted with app-granting `su`, the opt-in root-helper lifecycle smoke passes there, baseline rooted-emulator packet smoke has raw on-device pcap artifacts, the focused rooted raw extension verifies SeqOverlap plus FakeRst packet patterns, and `337b68cc` verifies MultiDisorder plus TCP IpFrag2 packet-owned TCP patterns. Fresh emulator diagnostics/proxy/VPN probes passed with only expected Android debug QUIC unsupported degradation, and the emulator TalkBack pass has active-service readiness plus Home, Settings, Advanced Settings, and Diagnostics UI hierarchy evidence. Local CI mirrors now pass for build, native-bloat, release-verification, coverage, rust-cross-check, rust-lint, the native Rust batch, CLI packet smoke, and local-network-lab; a release-verification warning found in that pass was fixed in `0747b94f`. The rooted physical-device release row remains open because the Pixel evidence is intentionally non-rooted and a rooted physical device is still unavailable.

## Prompt-to-Artifact Checklist

| Requirement | Current artifact or command | Result |
| --- | --- | --- |
| Use `docs/feature-test-checklist.md` as the source checklist | `docs/feature-test-checklist.md` and `docs/feature-test-evidence-2026-05-14.md` | Used as the coverage inventory. |
| Fix all issues found during the local pass | QA commits through `0c745ec9` plus `main` fixes through `337b68cc`, including physical packet-smoke, readiness, root-helper launch fallback, macOS host UDP/DNS, VPN readiness-contract fixes, root-helper smoke diagnostics, Docker netem readiness reporting, root-helper IPC lifecycle hardening, Android network E2E stabilization, diagnostics scan gating, mutation harness fixture exposure, TSPU dry-run command repair, signoff Python detection hardening, macOS SDK resolution for emulator helpers, CodeQL evidence naming, PR #118 remote-evidence refresh, TSPU dry-run Python resolver coverage, packet-smoke skip rejection, release-verification split-packaging hardening, raw-packet-only E2E exclusion, duplicate receiver cleanup, stale physical probe host detection, fuzz lock refresh, autolearn shutdown flush, local `act` runner repair, Rust clippy cleanup, cross-check hardening, Hilt qualifier cleanup, local-network-lab act support, root-helper capability/preflight hardening, rooted SeqOverlap/FakeRst packet-smoke coverage, rooted MultiDisorder/TCP IpFrag2 packet-owned TCP coverage, and emulator/Pixel 7 evidence refresh | Local findings were fixed and verified where the required device or lab was available; remaining blockers are environment/operator evidence, not known unfixed local failures. |
| Verify Appium installation and current app flows | `ANDROID_SERIAL=38080DLJG000GX RUNNER_TEMP=/tmp/ripdpi-appium-20260518 bash scripts/ci/run-appium-smoke.sh` | Passed after installing missing Python test dependencies and fixing one scroll expectation: 78 passed, 18 skipped. |
| Verify Maestro installation and current smoke flows | `run-proxy-e2e.sh` and `run-vpn-e2e.sh` artifacts under `test-lab/artifacts/` | Passed on the Pixel and again on the rooted emulator with DNS, HTTP, HTTPS, TCP, UDP, relay readiness, and mode-specific service readiness green; only Android debug QUIC probing remains explicitly unsupported. |
| Verify static local quality gates for the current head | `./gradlew :app:testGithubDebugUnitTest -Pripdpi.skipNativeBuild=true`, local `act`/native CI mirror jobs, focused Kotlin compile checks, and hook checks on each commit | Passed. |
| Verify local artifacts referenced by the evidence ledger exist | `test-lab/artifacts/...`, `appium/appium-report.html`, `build/packet-smoke/android/...` | Present locally; generated artifacts remain gitignored. |
| Verify remaining environment readiness | `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json`, `test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json`, `test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json`, and `/tmp/ripdpi-feature-gap-readiness-qa-current.json` | Blocked/manual rows remain; Pixel 7 is attached and non-rooted, TalkBack is active only on the emulator and inactive on Pixel 7, routed netem is manual because the Docker netem container is running but not proven in the device traffic path, and remote workflow confirmation is blocked because local `main` is ahead of `origin/main`. |
| Verify rooted behavior | Readiness probe, `RootHelperManagerTest`, current opt-in `RootHelperInstrumentedTest`, rooted-emulator baseline packet smoke, and focused SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 raw packet smoke on Magisk-rooted `RIPDPI_Root_API34` | Partial: the app-granting rooted emulator lifecycle smoke passes and cleans up without a helper leak, rooted-emulator packet smoke produced raw `device-capture.pcap` files for baseline scenarios, and SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 now have dedicated PCAP assertions; rooted physical evidence remains blocked because the connected Pixel is non-rooted. |
| Verify physical network matrix | Readiness probe and device transport state | Manual cellular handover, IPv4, and IPv6 evidence remain. |
| Verify relay provider matrix | Readiness probe | Blocked until relay provider configuration is supplied. |
| Verify accessibility with TalkBack | `test-lab/artifacts/talkback-emulator-20260520-current/`, `test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json`, and `test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json` | Partial: TalkBack is active on the emulator with hierarchy summaries, but Pixel 7 readiness reports TalkBack installed and inactive. |
| Verify routed VM packet-loss lab | `test-lab/artifacts/netem-container-capability-20260519-current.txt` plus `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json` | Container capability and readiness detection are current; routed netem Pixel path remains unproven. |
| Verify remote release gates | PR #118 check rollup, local `act`/native CI mirror jobs, and main workflow list for CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly | Partial: PR #118 checks were green before merge and local mirrors are green through `337b68cc`, including local-network-lab through act, but final main-branch CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly evidence still must be collected where release policy requires remote confirmation. |

## Current Local State

The local non-rooted Pixel pass is healthy for the surface that was executable while the Pixel was attached: app unit tests, Maestro proxy/VPN E2E, physical instrumentation E2E, Android packet-smoke proxy and VPN families on Pixel 8 Pro and Pixel 7, Appium UI/workflow smoke, adversarial middlebox dry-run, root-helper launch fallback unit coverage, current opt-in root-helper instrumentation on the Magisk-rooted `RIPDPI_Root_API34` AVD, rooted-emulator packet smoke with raw capture, active emulator TalkBack hierarchy coverage, relay example validation, current netem container capability/readiness reporting, and local CI mirror lanes all pass after the committed fixes. The local CI mirror result is explicit: `act-local.sh build`, `native-bloat`, `release-verification`, `coverage`, `rust-cross-check`, `rust-lint`, the native Rust batch, the `act` CLI packet-smoke lane, and `act-local.sh local-network-lab` passed locally; Rust line coverage from `coverage` is 81.51%. The rooted-emulator result is explicit: app-context `su` is granted, helper extraction and socket readiness pass, cleanup leaves no `ripdpi-root-helper` process or socket file, the baseline packet-smoke pass produced raw on-device pcaps under `test-lab/artifacts/android-packet-smoke-20260520-emulator-raw`, focused runs under `test-lab/artifacts/android-packet-smoke-root-verified-20260520-main` validated SeqOverlap plus FakeRst packet patterns, and focused runs under `test-lab/artifacts/android-packet-smoke-root-packet-owned-20260520-main-final` validated MultiDisorder plus TCP IpFrag2 packet-owned TCP patterns. The fresh emulator local-lab result is also explicit: diagnostics, proxy, and VPN probes passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness; VPN mode additionally reported `vpnEstablished=true`, `tunActive=true`, `egressProtected=true`, and `ipv4RouteInstalled=true`; each probe degraded only on `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE` with `errors=[]`. The Pixel 7 result is explicit: readiness selects a physical Android 17 / API 37 device, non-root is confirmed by failed `su 0 id`, direct MacBook LAN diagnostics are topology-blocked because Pixel and Mac are on different subnets, and adb-reverse packet-smoke completed the non-root physical matrix in indirect capture mode. The TalkBack result is split: emulator TalkBack is enabled and covered by hierarchy summaries, but Pixel 7 TalkBack is installed and inactive. This still does not close the rooted physical row unless the emulator substitution is explicitly accepted. The routed-netem result remains explicit: the `ripdpi-linux-netem` container is running with `tc`, but no evidence proves Pixel traffic is routed through it. The remaining blockers are not hidden failures; they are missing environment or operator evidence for rooted physical acceptance, physical TalkBack if required, cellular handover with IPv4 and IPv6 coverage, routed netem traffic, relay provider coverage, and final main-branch workflow confirmation where release policy still requires remote evidence.

## Requirement Table

| Requirement | Evidence inspected | Result | Remaining evidence required |
| --- | --- | --- | --- |
| Use `docs/feature-test-checklist.md` as the source checklist | Checklist, evidence ledger, and audit table | Covered locally | None |
| Fix all issues found during the local pass | QA commits through `0c745ec9` plus current `main` fixes through `337b68cc` and retest commands | Partial | Locally reproducible fix commits passed; remaining open rows require environment/operator evidence rather than a known local code fix |
| Verify Appium installation and current app flows | Full Appium smoke after fix: 78 passed, 18 skipped | Covered locally | None for Appium smoke; skipped seeded flows remain non-blocking for this pass |
| Verify Maestro installation and current smoke flows | Proxy and VPN E2E artifacts from 2026-05-19 plus rooted-emulator proxy/VPN artifacts from 2026-05-20 | Covered locally | None for non-rooted Pixel or rooted-emulator Maestro smoke |
| Verify static local quality gates for the current head | Full app unit lane, local `act`/native CI mirror lanes, focused Kotlin compile checks, and commit hooks | Covered locally | None |
| Verify local artifacts referenced by the evidence ledger exist | Artifact paths listed in `docs/feature-test-evidence-2026-05-14.md` | Covered locally | None |
| Verify remaining environment readiness | `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json`, `test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json`, `test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json`, and `/tmp/ripdpi-feature-gap-readiness-qa-current.json` | Blocked/manual | Rooted physical, physical TalkBack unless emulator substitution is accepted for accessibility, cellular handover, IPv4, IPv6, routed netem, relay provider, and final remote workflow rows |
| Verify rooted behavior | `adb shell su 0 id`, `RootHelperManagerTest`, current opt-in `RootHelperInstrumentedTest`, rooted-emulator baseline packet smoke, and focused SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 raw packet smoke on Magisk-rooted `RIPDPI_Root_API34` | Partial | Rooted physical device evidence remains unless the app-granting rooted-emulator substitution is explicitly accepted |
| Verify physical network matrix | Readiness row, Pixel 7 transport state, direct LAN diagnostics attempt, and adb-reverse packet smoke | Blocked/manual | Direct Pixel-to-lab reachability, cellular handover, IPv4-only, IPv6-only, private-DNS, and limited-path evidence |
| Verify relay provider matrix | Example relay matrix validation and readiness row | Blocked/manual | Operator-provided relay provider matrix and full relay provider run |
| Verify accessibility with TalkBack | Accessibility readiness row and UI hierarchy summaries | Partial | None for the emulator TalkBack pass; physical Pixel 7 TalkBack is installed but inactive |
| Verify routed VM packet-loss lab | Netem container capability artifact and netem-aware readiness row | Blocked/manual | Routed netem VM/router path carrying Pixel traffic |
| Verify remote release gates | PR #118 check rollup, local `act`/native CI mirror lanes, and workflow list | Partial | Fresh main-branch CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly remote workflow results remain where release policy still requires remote evidence |

## Stop Rules

Do not claim release sign-off while rooted physical evidence or explicit acceptance of the app-granting rooted-emulator substitution is absent, the required TalkBack readiness row lacks an accepted active-service artifact for the target device class, cellular handover plus IPv4 and IPv6 physical network evidence is missing, direct Pixel-to-lab reachability is unresolved, routed netem is only proven inside a container and not on the Pixel traffic path, relay provider configuration is unavailable, or final main-branch workflow evidence for CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly has not been collected.

Do not mark the active goal complete from this audit alone. The current local pass proves the non-rooted Pixel and local-tooling slice plus app-granting rooted-emulator helper lifecycle, but the objective also includes rooted physical acceptance, routed netem, relay provider checks, and remote workflow confirmation where applicable.

## Next Concrete Actions

1. Run rooted physical device evidence, or explicitly accept the app-granting Magisk `RIPDPI_Root_API34` emulator substitution with the SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 packet-action artifacts.
2. Run or explicitly waive the physical TalkBack manual pass; do not enable TalkBack from repository scripts.
3. Put the Pixel and Mac/routed lab on a mutually reachable network, then execute cellular handover and physical network matrix runs that cover Wi-Fi, cellular, IPv4, IPv6, private DNS, and limited-path behavior.
4. Route Pixel traffic through the Linux routed netem VM/router, then run VPN and diagnostics probes under packet loss and QUIC-drop conditions.
5. Provide the relay provider matrix and run proxy, VPN, diagnostics, restart, invalid credentials, reset, timeout, malformed response, DNS fallback, and handover rows.
6. Collect main-branch workflow results for CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly so the `remote_workflow_confirmation` readiness row can move to ready.
