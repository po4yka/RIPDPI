# Feature Test Completion Audit

Status: **not complete**.

This audit keeps the path expected by `test-lab/scripts/check-feature-test-signoff.sh`, while the current evidence was refreshed on 2026-05-19 from the connected non-rooted Pixel 8 Pro and local lab tooling, then updated through the tested PR #118 head `0c745ec9`, the 2026-05-20 `main` continuation on non-rooted Pixel 7, local `act`/native CI mirror evidence through `7d7535e0`, and the 2026-05-21 `main` continuation through `70c70a5c`. Earlier Pixel 8 Pro artifacts remain valid for the recorded pass, and Pixel 7 now provides current physical readiness, current-head instrumentation E2E, adb-reverse packet-smoke evidence, direct same-LAN diagnostics against local lab host `192.168.1.13`, Toxiproxy latency/timeout/TLS-reset diagnostics artifacts, a fresh host-backed DNS/UDP diagnostics probe, Docker UDP publication blocker evidence, and partial netem-forwarder proof for HTTP, HTTPS, TCP echo, and relay traffic through a Linux `tc` container. The current readiness artifact `/tmp/ripdpi-feature-gap-readiness-70c70a5c.json` selects Pixel 7, accepts `/tmp/ripdpi-local-workflow-evidence-70c70a5c.json` for the required local workflow lanes, and keeps the true blocked/manual rows explicit: no rooted physical `su`, TalkBack installed but inactive, cellular data out of service, physical routed netem not fully proven in the device path, and no operator relay provider matrix. The `RIPDPI_Root_API34` AVD is Magisk-rooted with app-granting `su`, the current opt-in root-helper lifecycle smoke passes there with post-run process/socket cleanup proof, baseline rooted-emulator packet smoke has raw on-device pcap artifacts, the focused rooted raw extension verifies SeqOverlap plus FakeRst packet patterns, `337b68cc` verifies MultiDisorder plus TCP IpFrag2 packet-owned TCP patterns, the current arm64 emulator relay smoke passes 29/29 tests with an empty crash buffer, the rooted-emulator on-device netem artifact proves DNS, HTTP, HTTPS, TCP, UDP, and relay diagnostics remain structured under `tc netem` delay/loss with `errors=[]`, and the emulator Persian RTL/large-font artifact covers Home, Settings, Diagnostics, and Advanced Settings with empty crash buffers. The current local workflow substitute is explicit: `scripts/ci/act-local.sh rust-workspace-tests rust-cross-check` passed with 3,400 native tests plus all Android ABI cross-checks, `scripts/ci/act-local.sh rust-lint cargo-deny rust-fuzz-smoke` passed on the current continuation, CodeQL workflow wiring passed under `act -n`, `scripts/ci/act-local.sh local-network-lab` passed, `scripts/ci/act-local.sh cli-packet-smoke` passed all eight CLI packet-smoke families, offline analytics produced the sample-corpus outputs, focused diagnostics mutation testing had no missed mutants, the fuzz smoke completed configured targets without crashes, the current TSPU dry-run passed 53 harness tests plus 63 matrix cells, `scripts/ci/act-local.sh tspu-dryrun` passed, `scripts/ci/act-local.sh tspu-live` passed under `act`, the Docker-backed TSPU live smoke passed 9 live nfqueue cells after `69d1a7fe` made the runner root/container-aware and `e77b3a36` added dynamic smoke-port selection plus local `act` wiring, and `70c70a5c` fixed the Pixel 7 packet-smoke runner so non-root physical runs skip raw-PCAP-only proxy rows before execution. The rooted physical-device release row remains open because the Pixel evidence is intentionally non-rooted and a rooted physical device is still unavailable.

## Prompt-to-Artifact Checklist

| Requirement | Current artifact or command | Result |
| --- | --- | --- |
| Use `docs/feature-test-checklist.md` as the source checklist | `docs/feature-test-checklist.md` and `docs/feature-test-evidence-2026-05-14.md` | Used as the coverage inventory. |
| Fix all issues found during the local pass | QA commits through `0c745ec9` plus `main` fixes through `70c70a5c`, including physical packet-smoke, readiness, root-helper launch fallback, macOS host UDP/DNS, VPN readiness-contract fixes, root-helper smoke diagnostics, Docker netem readiness reporting, root-helper IPC lifecycle hardening, Android network E2E stabilization, diagnostics scan gating, mutation harness fixture exposure, TSPU dry-run command repair, signoff Python detection hardening, macOS SDK resolution for emulator helpers, CodeQL evidence naming, PR #118 remote-evidence refresh, TSPU dry-run Python resolver coverage, packet-smoke skip rejection, release-verification split-packaging hardening, raw-packet-only E2E exclusion, duplicate receiver cleanup, stale physical probe host detection, fuzz lock refresh, autolearn shutdown flush, local `act` runner repair, Rust clippy cleanup, cross-check hardening, Hilt qualifier cleanup, local-network-lab act support, root-helper capability/preflight hardening, rooted SeqOverlap/FakeRst packet-smoke coverage, rooted MultiDisorder/TCP IpFrag2 packet-owned TCP coverage, physical network evidence refresh, cellular out-of-service readiness blocking, local mutation diff materialization, local mutation workflow support, privileged packet mutation coverage, local workflow evidence validation, fuzz smoke corpus isolation, unsafe root-helper probe ownership, diagnostics duplicate-start stabilization, Android CMake cross-check stabilization, local lab Corefile recovery, diagnostics Maestro flow repair, native-bloat CMake selection, `c4b2cdaa` dormant-service E2E cleanup, netem-forwarder evidence recording, the `b0f9ec57` local-instrument evidence refresh, the `591afc7f` local `act` evidence refresh, current TSPU dry-run evidence, rooted-emulator on-device netem evidence, RTL/accessibility evidence, current host-backed Pixel 7 DNS/UDP evidence, current-head Pixel 7 E2E instrumentation evidence, Docker-backed TSPU live-smoke runner hardening, local `act` TSPU workflow wiring with dynamic smoke-port selection, and current Pixel 7 packet-smoke raw-PCAP capability filtering | Local findings were fixed and verified where the required device or lab was available; remaining blockers are environment/operator evidence, not known unfixed local failures. |
| Verify Appium installation and current app flows | `ANDROID_SERIAL=38080DLJG000GX RUNNER_TEMP=/tmp/ripdpi-appium-20260518 bash scripts/ci/run-appium-smoke.sh` | Passed after installing missing Python test dependencies and fixing one scroll expectation: 78 passed, 18 skipped. |
| Verify Maestro installation and current smoke flows | `run-proxy-e2e.sh` and `run-vpn-e2e.sh` artifacts under `test-lab/artifacts/` | Passed on the Pixel and again on the rooted emulator with DNS, HTTP, HTTPS, TCP, UDP, relay readiness, and mode-specific service readiness green; only Android debug QUIC probing remains explicitly unsupported. |
| Verify static local quality gates for the current head | `./gradlew :app:testGithubDebugUnitTest -Pripdpi.skipNativeBuild=true`, local `act`/native CI mirror jobs, focused Kotlin compile checks, and hook checks on each commit | Passed. |
| Verify local artifacts referenced by the evidence ledger exist | `test-lab/artifacts/...`, `appium/appium-report.html`, `build/packet-smoke/android/...` | Present locally; generated artifacts remain gitignored. |
| Verify remaining environment readiness | `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json`, `test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json`, `test-lab/artifacts/feature-gap-readiness-20260520-after-cellular-state-fix.json`, `test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/feature-gap-readiness-talkback-active.json`, `test-lab/artifacts/talkback-pixel7-blocked-20260521-024320-7f02179c/`, `test-lab/artifacts/diagnostics-pixel7-20260520-main-same-lan/probe-device-diagnostics.json`, `test-lab/artifacts/diagnostics-pixel7-20260521-021753-b0f9ec57/probe-device-diagnostics.json`, `test-lab/artifacts/physical-host-dns-udp-pixel7-20260521-025413-ace161b5/probe-physical-diagnostics.json`, `test-lab/artifacts/pixel7-e2e-current-20260521-030803-33f60083/`, `test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c/`, `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/`, `test-lab/artifacts/toxiproxy-pixel7-20260521-013905-c4b2cdaa/`, `test-lab/artifacts/netem-forwarder-tcp-pixel7-20260521-015154-6432eeb8/`, `test-lab/artifacts/root-helper-current-20260521-034956-ff7e0163/`, `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/`, `test-lab/artifacts/rtl-large-font-emulator-20260521-024157-7f02179c/`, and `/tmp/ripdpi-feature-gap-readiness-70c70a5c.json` | Blocked/manual rows remain; Pixel 7 is attached and non-rooted, current-head E2E instrumentation passed 39/39 with no crash-buffer entries, direct same-LAN diagnostics passed again on `b0f9ec57`, current host-backed DNS/UDP diagnostics passed on `ace161b5`, current Pixel 7 packet-smoke passed 17 observable scenario directories and skipped raw-PCAP-only proxy rows before execution, local fault-injection diagnostics produced structured results, partial netem-forwarder diagnostics carried HTTP, HTTPS, TCP echo, and relay through a Linux `tc` container, Docker-backed UDP publication was reproven as a host transport blocker, rooted-emulator root-helper lifecycle passed with cleanup proof, rooted-emulator on-device netem diagnostics carried DNS, HTTP, HTTPS, TCP, UDP, and relay under delay/loss with `errors=[]`, emulator Persian RTL/large-font app windows rendered with empty crash buffers, TalkBack active-service readiness is proven on the emulator and physical Pixel 7 while active, current Pixel TalkBack state is blocked because TalkBack is inactive and the device is locked, cellular handover is blocked because the phone reports voice/data `OUT_OF_SERVICE`, physical routed netem is still manual because DNS/UDP were not proven through the physical routed path, and remote workflow confirmation is ready from local workflow evidence. |
| Verify rooted behavior | Readiness probe, `RootHelperManagerTest`, current opt-in `RootHelperInstrumentedTest`, rooted-emulator baseline packet smoke, and focused SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 raw packet smoke on Magisk-rooted `RIPDPI_Root_API34` | Partial: the app-granting rooted emulator lifecycle smoke passes and cleans up without a helper leak, the current artifact proves no remaining `ripdpi-root-helper` process or root-helper socket file, rooted-emulator packet smoke produced raw `device-capture.pcap` files for baseline scenarios, and SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 now have dedicated PCAP assertions; rooted physical evidence remains blocked because the connected Pixel is non-rooted. |
| Verify physical network matrix | Readiness probe, same-LAN Pixel 7 diagnostics, Toxiproxy fault matrix, handover artifact, and device transport state | Wi-Fi same-LAN diagnostics passed and local latency/timeout/TLS-reset faults produced structured artifacts; cellular handover, IPv4, and IPv6 evidence remain blocked/manual because Pixel 7 reports telephony `OUT_OF_SERVICE`. |
| Verify relay provider matrix | Readiness probe, host relay interoperability, and arm64 Android relay emulator smoke | Local relay code paths pass; provider-backed release matrix remains blocked until operator relay configuration is supplied. |
| Verify accessibility with TalkBack | `test-lab/artifacts/talkback-emulator-20260520-current/`, `test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json`, `test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json`, `test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/feature-gap-readiness-talkback-active.json`, and `test-lab/artifacts/talkback-pixel7-blocked-20260521-024320-7f02179c/` | Partial: TalkBack is active on the emulator with hierarchy summaries, and Pixel 7 readiness marks `manual_talkback` ready while active in the older operator artifact, but the current physical app-window hierarchy capture is blocked because the Pixel is locked and TalkBack is inactive. |
| Verify routed VM packet-loss lab | `test-lab/artifacts/netem-container-capability-20260519-current.txt`, `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json`, `test-lab/artifacts/netem-forwarder-pixel7-20260521-014938-6432eeb8/`, `test-lab/artifacts/netem-forwarder-tcp-pixel7-20260521-015154-6432eeb8/`, `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/`, and `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/` | Partial: the Linux `tc` forwarder carried Pixel HTTP, HTTPS, TCP echo, and relay traffic under baseline, 200 ms delay, and 15% loss with `errors=[]`; rooted-emulator on-device `tc netem` carried DNS, HTTP, HTTPS, TCP, UDP, and relay diagnostics under 200 ms delay plus 1% loss with `errors=[]`; Docker Desktop UDP publication misdirected UDP replies after source translation, preventing DNS/UDP proof through the physical Pixel forwarder, so the full physical routed-netem row remains open. |
| Verify remote release gates | PR #118 check rollup, local `act`/native CI mirror jobs, current relay interoperability, Android relay smoke, local workflow evidence validator, Docker-backed TSPU live smoke, `scripts/ci/act-local.sh tspu-dryrun`, `scripts/ci/act-local.sh tspu-live`, current Pixel 7 packet-smoke capability-filter rerun, and main workflow list for CI, CodeQL, local-network-lab, CLI packet-smoke, offline analytics, mutation-testing, Fuzz Nightly, and TSPU | Covered locally: PR #118 checks were green before merge, current `main` local mirrors are green through `70c70a5c`, `/tmp/ripdpi-local-workflow-evidence-70c70a5c.json` validates all required local workflow lanes for the current commit, and readiness marks `remote_workflow_confirmation` ready from explicit local evidence; hosted reruns remain optional only where release policy requires remote confirmation beyond local `act`/VM/emulator substitutes. |

## Current Local State

The local non-rooted Pixel pass is healthy for the surface that was executable while the Pixel was attached: app unit tests, Maestro proxy/VPN E2E, physical instrumentation E2E, Android packet-smoke proxy and VPN families on Pixel 8 Pro and Pixel 7, Pixel 7 same-LAN diagnostics, Pixel 7 Toxiproxy latency/timeout/TLS-reset diagnostics, partial Pixel 7 netem-forwarder diagnostics, Appium UI/workflow smoke, emulator Persian RTL/large-font UI capture, current TSPU dry-run, Docker-backed TSPU live smoke, root-helper launch fallback unit coverage, current opt-in root-helper instrumentation on the Magisk-rooted `RIPDPI_Root_API34` AVD, rooted-emulator packet smoke with raw capture, rooted-emulator on-device netem diagnostics, active emulator TalkBack hierarchy coverage, physical Pixel 7 TalkBack active-service readiness, relay example validation, current-head host relay interoperability, arm64 rooted-emulator relay smoke, current netem container capability/readiness reporting, and local CI mirror lanes all pass after the committed fixes. The local CI mirror/substitute result is explicit: `act-local.sh build`, `native-bloat`, `release-verification`, `coverage`, `rust-cross-check`, `rust-lint`, the native Rust batch, the `act` CLI packet-smoke lane, current `act-local.sh local-network-lab`, the offline analytics unit/sample-corpus workflow substitute, `RIPDPI_FUZZ_SECONDS=30 bash scripts/ci/run-rust-fuzz-smoke.sh`, the scoped local Linux `act` mutation-testing workflow, and the Docker Linux TSPU live smoke passed locally; Rust line coverage from `coverage` is 81.51%, the short fuzz substitute covered `packets_parse`, `packets_tls_quic`, `failure_http_response`, and `failure_field_cache` without sanitizer crashes, mutation testing caught 3/3 changed-line privileged packet mutants, and TSPU live smoke recorded 9 live nfqueue cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive under `test-lab/artifacts/tspu-live-smoke-20260521-032212-69d1a7fe/`. The rooted-emulator result is explicit: app-context `su` is granted, helper extraction and socket readiness pass, cleanup leaves no `ripdpi-root-helper` process or socket file, the baseline packet-smoke pass produced raw on-device pcaps under `test-lab/artifacts/android-packet-smoke-20260520-emulator-raw`, focused runs under `test-lab/artifacts/android-packet-smoke-root-verified-20260520-main` validated SeqOverlap plus FakeRst packet patterns, focused runs under `test-lab/artifacts/android-packet-smoke-root-packet-owned-20260520-main-final` validated MultiDisorder plus TCP IpFrag2 packet-owned TCP patterns, and `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/` shows baseline plus `tc netem delay 200ms 40ms loss 1%` diagnostics both returned `errors=[]` with DNS, HTTP, HTTPS, TCP, UDP, and relay readiness. The fresh emulator local-lab result is also explicit: diagnostics, proxy, and VPN probes passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness; VPN mode additionally reported `vpnEstablished=true`, `tunActive=true`, `egressProtected=true`, and `ipv4RouteInstalled=true`; each probe degraded only on `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE` with `errors=[]`. The emulator UI result is explicit: Home, Settings, Diagnostics, and Advanced Settings focused RIPDPI at app locale `[fa]` and `font_scale=1.45`, exposed expected screen tags in UI summaries, had empty crash buffers, and restored font and app locale settings afterward. The Pixel 7 result is explicit: readiness selects a physical Android 17 / API 37 device, non-root is confirmed by failed `su 0 id`, same-LAN diagnostics against host `192.168.1.13` passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness, adb-reverse packet-smoke completed the non-root physical matrix in indirect capture mode, Toxiproxy latency produced a slower successful HTTP probe, Toxiproxy timeout/reset faults produced structured recoverable errors without crashing the debug probe, and the netem-forwarder run carried HTTP, HTTPS, TCP echo, and relay diagnostics through a Linux `tc` container under baseline, delay, and loss. The physical handover result is split: Wi-Fi before/after probes passed and Wi-Fi was restored, but cellular is not valid evidence because the phone reports voice and data `OUT_OF_SERVICE`. The TalkBack result is split: emulator TalkBack is enabled and covered by hierarchy summaries, physical Pixel 7 TalkBack active-service readiness is proven in the older operator artifact, and the current Pixel artifact shows physical app-window hierarchy capture remains blocked because the device is locked and TalkBack is inactive. This still does not close the rooted physical row unless the emulator substitution is explicitly accepted. The routed-netem result is split: the rooted emulator proved on-device shaping for all debug-probe transports, and TCP-family Pixel diagnostics reached the Linux `tc` forwarder, but physical Pixel DNS/UDP were not proven through that container because Docker Desktop UDP publication timed out from the Mac and Pixel sides. The remaining blockers are not hidden failures; they are missing environment or operator evidence for rooted physical acceptance, physical TalkBack app-window capture if required, cellular handover with IPv4 and IPv6 coverage, full physical routed netem traffic, relay provider coverage, and hosted workflow confirmation where release policy still requires remote evidence beyond the accepted local `act`/VM/emulator evidence JSON.

The TSPU local-workflow result at `e77b3a36` is also explicit: `scripts/ci/act-local.sh tspu-dryrun` passed 53 harness tests plus 63 dry-run cells; `scripts/ci/act-local.sh tspu-live` passed under `act` with dynamic smoke port `59421`; the persisted Docker artifact `test-lab/artifacts/tspu-live-smoke-act-local-20260521-034101-e77b3a36/` passed with dynamic smoke port `46927`; `/tmp/ripdpi-local-workflow-evidence-e77b3a36.json` validated the TSPU local workflow lanes at that commit; and `/tmp/ripdpi-feature-signoff-e77b3a36.log` blocked only on the known environment/operator rows.

The current Pixel 7 packet-smoke result at `70c70a5c` is explicit: the first current-head rerun reproduced a non-root physical harness bug when `android_proxy_multidisorder_family` tried to start a raw-PCAP-only root strategy on `physical_indirect`; after the fix, `test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c/` contains 17 successful scenario directories and no failure screenshots, while `android_proxy_multidisorder_family`, `android_proxy_ipfrag2_family`, `android_proxy_seqovl_family`, and `android_proxy_fakerst_family` were skipped before execution because they require `adb_raw_pcap`. `/tmp/ripdpi-local-workflow-evidence-70c70a5c.json` validates the current local workflow lanes, `/tmp/ripdpi-feature-gap-readiness-70c70a5c.json` keeps the same blocked/manual environment rows, and `/tmp/ripdpi-feature-signoff-70c70a5c.log` still exits 1 for those blockers.

## Requirement Table

| Requirement | Evidence inspected | Result | Remaining evidence required |
| --- | --- | --- | --- |
| Use `docs/feature-test-checklist.md` as the source checklist | Checklist, evidence ledger, and audit table | Covered locally | None |
| Fix all issues found during the local pass | QA commits through `0c745ec9` plus current `main` fixes through `70c70a5c` and retest commands | Covered locally | None for known local failures found during this pass; remaining open rows require environment/operator evidence rather than a known local code fix |
| Verify Appium installation and current app flows | Full Appium smoke after fix: 78 passed, 18 skipped | Covered locally | None for Appium smoke; skipped seeded flows remain non-blocking for this pass |
| Verify Maestro installation and current smoke flows | Proxy and VPN E2E artifacts from 2026-05-19 plus rooted-emulator proxy/VPN artifacts from 2026-05-20 | Covered locally | None for non-rooted Pixel or rooted-emulator Maestro smoke |
| Verify static local quality gates for the current head | Full app unit lane, local `act`/native CI mirror lanes, focused Kotlin compile checks, and commit hooks | Covered locally | None |
| Verify local artifacts referenced by the evidence ledger exist | Artifact paths listed in `docs/feature-test-evidence-2026-05-14.md` | Covered locally | None |
| Verify remaining environment readiness | `/tmp/ripdpi-feature-gap-readiness-70c70a5c.json` plus prior readiness artifacts for netem, emulator TalkBack, Pixel 7 TalkBack active-service, current Pixel 7 TalkBack blocker, cellular state, the current Toxiproxy fault matrix, the fresh Pixel 7 diagnostics probe, current-head Pixel 7 E2E instrumentation, the current host-backed DNS/UDP probe, current Pixel 7 packet-smoke capability-filter artifact, Docker UDP publication blocker evidence, rooted-emulator on-device netem diagnostics, and emulator Persian RTL large-font capture | Blocked/manual | Rooted physical, physical TalkBack app-window capture unless emulator hierarchy plus physical active-service readiness is accepted, cellular handover, IPv4, IPv6, physical routed netem, and relay provider rows |
| Verify rooted behavior | `adb shell su 0 id`, `RootHelperManagerTest`, current opt-in `RootHelperInstrumentedTest`, rooted-emulator baseline packet smoke, and focused SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 raw packet smoke on Magisk-rooted `RIPDPI_Root_API34` | Blocked/manual | Rooted physical device evidence remains unless the app-granting rooted-emulator substitution is explicitly accepted |
| Verify physical network matrix | Readiness row, Pixel 7 transport state, current-head Pixel 7 E2E instrumentation, same-LAN direct diagnostics, current host-backed DNS/UDP diagnostics, Toxiproxy fault matrix, handover attempt, and adb-reverse packet smoke | Blocked/manual | Cellular handover, IPv4-only, IPv6-only, private-DNS, and limited-path evidence on a device/SIM with cellular data in service |
| Verify relay provider matrix | Example relay matrix validation, readiness row, host relay interoperability, and Android relay emulator smoke | Blocked/manual | Operator-provided relay provider matrix and full relay provider run |
| Verify accessibility with TalkBack | Accessibility readiness rows, UI hierarchy summaries, and current Pixel 7 blocker artifact | Blocked/manual | None for the emulator TalkBack pass, emulator large-font/RTL UI capture, or the older Pixel 7 active-service readiness row; physical Pixel 7 app-window hierarchy capture remains blocked until the device is unlocked and TalkBack is manually enabled |
| Verify routed VM packet-loss lab | Netem container capability artifact, failed full UDP/DNS forwarder attempt, Docker UDP publication blocker repro, successful TCP-family netem-forwarder artifact, and rooted-emulator on-device netem diagnostics | Blocked/manual | Full physical routed netem VM/router path carrying Pixel DNS, UDP, TCP, HTTPS, and relay traffic |
| Verify remote release gates | `/tmp/ripdpi-local-workflow-evidence-70c70a5c.json` plus the local `act`/native CI, CodeQL dry-run, local-network-lab, CLI packet-smoke, offline analytics, mutation, fuzz, relay interoperability, Android relay smoke, `scripts/ci/act-local.sh tspu-dryrun`, `scripts/ci/act-local.sh tspu-live`, Docker-backed TSPU live-smoke artifacts, and the current Pixel 7 packet-smoke capability-filter rerun named in the evidence ledger | Covered locally | None for the local workflow-evidence path requested for this pass; collect hosted workflow evidence only if release policy requires remote confirmation beyond local `act`/VM/emulator substitutes |

## Stop Rules

Do not claim release sign-off while rooted physical evidence or explicit acceptance of the app-granting rooted-emulator substitution is absent, physical TalkBack app-window hierarchy evidence is still required and the Pixel remains locked with TalkBack inactive, cellular handover plus IPv4 and IPv6 physical network evidence is missing, physical routed netem is only partially proven for Pixel traffic and fully proven only on the rooted emulator, or relay provider configuration is unavailable. The current local workflow evidence closes the local substitute path requested for this pass; collect hosted workflow evidence separately if release policy requires remote confirmation beyond local `act`/VM/emulator substitutes.

Do not mark the active goal complete from this audit alone. The current local pass proves the non-rooted Pixel and local-tooling slice plus app-granting rooted-emulator helper lifecycle and rooted-emulator on-device netem, and it now closes the local workflow evidence row for the tested commit, but the objective still includes rooted physical acceptance or an explicit emulator-substitution waiver, physical routed netem traffic proof, relay provider checks, cellular handover, and physical TalkBack app-window evidence where applicable.

## Next Concrete Actions

1. Run rooted physical device evidence, or explicitly accept the app-granting Magisk `RIPDPI_Root_API34` emulator substitution with the SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 packet-action artifacts.
2. Unlock the Pixel 7, enable TalkBack manually, and rerun the physical TalkBack app-window pass, or explicitly waive that app-window requirement and accept the emulator UI hierarchy plus Pixel 7 active-service readiness artifact; do not enable TalkBack from repository scripts.
3. Use a physical Pixel/SIM with cellular data in service, then execute cellular handover and physical network matrix runs that cover Wi-Fi, cellular, IPv4, IPv6, private DNS, and limited-path behavior.
4. Route Pixel traffic through the Linux routed netem VM/router, then run VPN and diagnostics probes under packet loss and QUIC-drop conditions; rooted-emulator on-device netem is now proven but does not close the physical routed-path row.
5. Provide the relay provider matrix and run proxy, VPN, diagnostics, restart, invalid credentials, reset, timeout, malformed response, DNS fallback, and handover rows.
6. Collect hosted main-branch workflow results for CI, CodeQL, local-network-lab, offline analytics, mutation-testing, Fuzz Nightly, and TSPU only where release policy still requires remote confirmation beyond the accepted local evidence JSON; the current readiness row already marks `remote_workflow_confirmation` ready from `/tmp/ripdpi-local-workflow-evidence-e77b3a36.json`.
