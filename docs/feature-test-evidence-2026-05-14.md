# Feature Test Evidence Ledger

Status: local evidence refreshed on 2026-05-21; release sign-off is not complete.

This file keeps the path expected by `test-lab/scripts/check-feature-test-signoff.sh`, while the evidence below reflects the 2026-05-19 local QA pass, the tested PR #118 head `0c745ec9`, the 2026-05-20 `main` continuation on non-rooted Pixel 7, local CI mirror/substitute evidence through `7d7535e0`, and the 2026-05-21 `main` continuation through `aa3208a7` plus the current local mock-relay matrix, locked-readiness, packet/TSPU `act` reruns, rooted-emulator netem rerun, signoff-guard Bash portability fix, host DNS/UDP fault-control fix, repeatable host DNS/UDP fault-matrix runner, and repeatable TCP-family netem forwarder runner. Earlier physical-device artifacts were collected while the non-rooted Pixel 8 Pro was attached; the Pixel 7 continuation restored a physical target for current readiness, current-head instrumentation E2E, adb-reverse packet-smoke coverage, direct same-LAN diagnostics against `192.168.1.13`, Toxiproxy fault evidence, host-backed DNS/UDP evidence, and partial Linux `tc` forwarder proof. The `RIPDPI_Root_API34` AVD remains the rooted substitute: current root-helper lifecycle smoke passes there, rooted packet-smoke pcaps cover baseline fake-segment rows, focused runs verify SeqOverlap, FakeRst, MultiDisorder, and TCP IpFrag2 packet patterns, rooted-emulator on-device `tc netem` carries DNS, HTTP, HTTPS, TCP, UDP, and relay diagnostics with `errors=[]`, current rooted-emulator UI E2E passes both proxy and VPN flows, and the controlled mock-relay matrix verifies ready, invalid-credential, malformed-response, server-reset, and timeout diagnostics on the rooted emulator and non-rooted Pixel 7 without breaking the other local probes. Current `main` local workflow evidence includes native workspace and Android cross-checks, CodeQL workflow wiring under `act -n`, local-network-lab under `act`, CLI packet-smoke under `act`, offline analytics sample output, focused diagnostics mutation testing, 30s fuzz smoke, relay interoperability, Android relay smoke, fresh Pixel 7 diagnostics, emulator Persian RTL/large-font UI capture, partial routed-netem evidence, `scripts/ci/act-local.sh tspu-dryrun`, `scripts/ci/act-local.sh tspu-live`, Docker-backed TSPU live-smoke evidence, current Pixel 7 packet-smoke capability-filter coverage, current rooted-emulator raw packet-smoke coverage, current rooted-emulator proxy/VPN E2E coverage, repeatable rooted-emulator netem coverage, mock relay fault-matrix coverage, the Pixel 7 combined DNS-UDP/UDP-echo fault probe, committed-head Pixel 7 host fault matrix, and committed-head TCP-family netem forwarder proof; `/tmp/ripdpi-local-workflow-evidence-3437c467.json` validates the runtime lanes through the host DNS fallback fix, the current `/tmp/ripdpi-act-packet-tspu-8bc87c2d.log` rerun passed CLI packet-smoke plus TSPU live under `act`, `test-lab/artifacts/rooted-emulator-netem-20260521-064100-59309e78/summary.tsv` passed the current rooted-emulator netem lane, `69d1a7fe` removes the host-sudo assumption that blocked the live smoke from running cleanly inside the privileged Docker/VM lab, `e77b3a36` wires the TSPU workflows into the local `act` wrapper with dynamic smoke-port selection for host-network conflicts, `70c70a5c` prevents non-root physical packet-smoke runs from executing raw-PCAP-only proxy rows, `b519a26a` adds the repeatable same-LAN host fault-matrix runner, and `aa3208a7` adds the repeatable Docker `tc netem` TCP forwarder runner. The rooted physical-device release row remains open because the Pixel evidence is intentionally non-rooted and a rooted physical device is still unavailable.

The 2026-05-21 continuation on `main` through `f3ba98ac` adds current-head E2E cleanup verification, relay-focused coverage, Pixel 7 local fault-injection evidence, and partial routed-netem proof. `c4b2cdaa` fixes Android E2E teardown so dormant Hilt services are not started only to stop them; current-head emulator reruns passed `EnvironmentPreflightE2ETest`, `DiagnosticsNetworkE2ETest`, and `NetworkPathE2ETest` with empty crash buffers, then `:app:ktlintCheck`, `:app:detekt`, and `git diff --check` passed. Relay evidence now includes host relay interoperability and the arm64 rooted-emulator relay smoke. Pixel 7 Toxiproxy diagnostics under `test-lab/artifacts/toxiproxy-pixel7-20260521-013905-c4b2cdaa/` prove the lab still emits structured output under latency, timeout, and TLS reset faults: baseline and latency rows were non-failing with only `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE`, timeout produced a recoverable HTTP `Fail`, and TLS reset produced a recoverable HTTPS `Degraded` result. The netem-forwarder pass under `test-lab/artifacts/netem-forwarder-tcp-pixel7-20260521-015154-6432eeb8/` proves Pixel 7 HTTP, HTTPS, TCP echo, and relay diagnostics can be carried through a Linux container with `tc netem` delay and loss; it is partial routed-netem evidence because DNS and UDP remained on the host helpers after Docker Desktop UDP publication failed in `test-lab/artifacts/netem-forwarder-pixel7-20260521-014938-6432eeb8/`. `/tmp/ripdpi-local-workflow-evidence-f3ba98ac.json` validates the required local workflow lanes for `f3ba98ac`; `/tmp/ripdpi-feature-gap-readiness-f3ba98ac.json` still blocks sign-off on true environment gaps rather than code failures.

The 2026-05-21 continuation on `main` through `591afc7f` refreshed the local-instrument release path without waiting on hosted GitHub CI. `scripts/ci/act-local.sh rust-lint cargo-deny rust-fuzz-smoke` passed in 173 seconds; `scripts/ci/act-local.sh local-network-lab` passed in 63 seconds with Docker Compose validation, emulator-profile lab startup, host lab doctor `8 pass, 0 fail, 3 skip`, archive creation, and redaction; `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" act -n -W .github/workflows/codeql.yml -j analyze` resolved the CodeQL actions job successfully; and `scripts/ci/act-local.sh cli-packet-smoke` passed in 384 seconds across TCP split, disorder, fake, OOB, disoob, transport knobs, UDP/QUIC, and adaptive packet-smoke families. The fresh Pixel 7 diagnostics probe under `test-lab/artifacts/diagnostics-pixel7-20260521-021753-b0f9ec57/` passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against host `192.168.1.13` with `errors=[]`, degrading only on expected `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE`. The follow-up TSPU dry-run under `test-lab/artifacts/tspu-dryrun-20260521-022523-591afc7f/` ran 53 harness tests and 63 matrix cells with 23 blocked, 40 pass-through, 0 degraded, and 0 inconclusive cells. The rooted-emulator netem diagnostic run under `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/` installed the current `githubDebug` APK on `emulator-5554`, applied `tc qdisc replace dev wlan0 root netem delay 200ms 40ms loss 1%`, and proved baseline plus shaped-path diagnostics both returned `errors=[]` with DNS, HTTP, HTTPS, TCP, UDP, and relay readiness present; cleanup restored the emulator qdisc afterward. `/tmp/ripdpi-local-workflow-evidence-591afc7f.json` validates all required local workflow lanes for `591afc7f`; `/tmp/ripdpi-feature-gap-readiness-591afc7f.json` and `/tmp/ripdpi-feature-signoff-591afc7f.log` keep the true blocked/manual rows explicit.

The 2026-05-21 continuation on `main` through `ace161b5` added emulator Persian RTL/large-font UI evidence, a read-only current Pixel 7 TalkBack blocker capture, and a fresh physical Pixel 7 DNS/UDP lab split. Docker-backed DNS/UDP evidence under `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/` reproduced Docker Desktop UDP publication as the blocker: the UDP echo container received datagrams but saw source `142.250.75.177`, so replies were misdirected and the host and Pixel timed out. The host-backed rerun under `test-lab/artifacts/physical-host-dns-udp-pixel7-20260521-025413-ace161b5/` passed host DNS/UDP sanity checks and Pixel 7 diagnostics for DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`; the verdict remains `Degraded` only because Android debug QUIC probing is intentionally unsupported. `/tmp/ripdpi-local-workflow-evidence-ace161b5.json`, `/tmp/ripdpi-feature-gap-readiness-ace161b5.json`, and `/tmp/ripdpi-feature-signoff-ace161b5.log` keep the local workflow lane ready and the remaining environment blockers explicit.

The 2026-05-21 continuation on `main` at `33f60083` reran the current-head physical E2E instrumentation package on Pixel 7. `ANDROID_SERIAL=31130DLH2000EG ./gradlew :app:connectedGithubDebugAndroidTest -Pripdpi.localNativeAbis=arm64-v8a -Pandroid.testInstrumentationRunnerArguments.package=com.poyka.ripdpi.e2e --console=plain` finished 39/39 tests with zero failures, zero errors, and zero skips in `BUILD SUCCESSFUL`; the artifact under `test-lab/artifacts/pixel7-e2e-current-20260521-030803-33f60083/` preserves the XML result, textproto result, window state, and a filtered crash buffer with no RIPDPI entries.

The 2026-05-21 continuation on `main` at `69d1a7fe` fixed `scripts/ci/run-tspu-live-smoke.sh` so the Linux-only TSPU live smoke runs both on sudo-backed Linux hosts and as root inside a privileged Docker or VM lab. A post-commit Docker rerun under `test-lab/artifacts/tspu-live-smoke-20260521-032212-69d1a7fe/` exercised the nfqueue-attached live handler without a sudo shim and produced `verdict-report.json` with 9 live cells: 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive.

The 2026-05-21 continuation on `main` at `e77b3a36` made the TSPU workflows first-class local checks: `scripts/ci/act-local.sh tspu-dryrun` passed 53 harness tests plus 63 dry-run matrix cells, and `scripts/ci/act-local.sh tspu-live` passed under `act` after the workflow skipped hosted artifact upload under `ACT=true`, installed NetfilterQueue in the lean runner, and selected a free dynamic smoke port instead of colliding with local lab services. The post-commit `act` log `/tmp/ripdpi-act-tspu-live-e77b3a36.log` recorded smoke port `59421` and 9 live nfqueue cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive; the persisted Docker artifact under `test-lab/artifacts/tspu-live-smoke-act-local-20260521-034101-e77b3a36/` recorded the same 9-cell verdict on smoke port `46927`.

The 2026-05-21 continuation on `main` at `ff7e0163` reran the opt-in root-helper instrumentation smoke on the Magisk-rooted `RIPDPI_Root_API34` emulator. The artifact `test-lab/artifacts/root-helper-current-20260521-034956-ff7e0163/` contains the Gradle log, JUnit XML, test textproto, focused root-helper logcat, empty crash buffer, and post-run cleanup proof: `su -c id` returned uid 0, `pidof ripdpi-root-helper` was empty after the run, and no root-helper socket files remained.

The 2026-05-21 continuation on `main` at `70c70a5c` fixed the Android packet-smoke runner so `physical_indirect` devices honor each scenario `requiredCapability` and skip `adb_raw_pcap` rows before starting root-only raw packet proxy tests. The current Pixel 7 run at `8a9e4869` reproduced the bug by failing `android_proxy_multidisorder_family` on a non-rooted phone after the service halted with `raw-packet desync requires raw IPv4 sockets, raw IPv6 sockets, TCP repair`; `70c70a5c` then passed `bash -n` plus `scripts/ci/test-android-packet-smoke-lib.sh`, `:app:compileGithubDebugAndroidTestKotlin`, `:app:runKtlintCheckOverAndroidTestSourceSet --rerun-tasks`, `:app:ktlintCheck`, `git diff --check`, and a clean-head Pixel 7 packet-smoke rerun. The artifact `test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c/` contains 17 successful scenario directories, no `failure-screenshot.png`, and the runner skipped `android_proxy_multidisorder_family`, `android_proxy_ipfrag2_family`, `android_proxy_seqovl_family`, and `android_proxy_fakerst_family` because they require `adb_raw_pcap` while the device profile was `physical_indirect`. `/tmp/ripdpi-local-workflow-evidence-70c70a5c.json` validates the local workflow lanes for `70c70a5c`; `/tmp/ripdpi-feature-gap-readiness-70c70a5c.json` keeps the same true blockers explicit, and `/tmp/ripdpi-feature-signoff-70c70a5c.log` still exits 1 as expected.

The 2026-05-21 continuation on `main` at `3f678d4c` refreshed the current-head local workflow/readiness evidence and reran the full rooted-emulator raw Android packet-smoke matrix on the Magisk-rooted `RIPDPI_Root_API34` AVD. `/tmp/ripdpi-local-workflow-evidence-3f678d4c.json` validates the required local lanes for `3f678d4c`, `/tmp/ripdpi-feature-gap-readiness-3f678d4c.json` selects the non-rooted Pixel 7 as the physical target while keeping rooted physical, TalkBack activation, cellular handover, routed-netem route proof, and production relay matrix rows blocked/manual, and `/tmp/ripdpi-feature-signoff-3f678d4c.log` still exits 1 for those known blockers. The rooted-emulator artifact `test-lab/artifacts/android-packet-smoke-rooted-emulator-current-20260521-043138-3f678d4c/` contains 21 scenario directories plus a shared harness directory, 21 non-empty `device-capture.pcap` files, no `failure-screenshot.png`, no crash/assertion/failure markers, 7 raw-validated root-only proxy rows, and 14 instrumentation-success rows. Hostfake, fakedsplit, and fakeddisorder were accepted only after raw packet-pattern validation proved the low-TTL fake payload reached the fixture path; FakeRst, TCP IpFrag2, SeqOverlap, and MultiDisorder were accepted only after their dedicated pcap assertions passed.

The 2026-05-21 continuation on `main` at `1e13bbd2` refreshed the local workflow/readiness evidence for the docs-only head and retested the attached Pixel 7 against the local lab. `/tmp/ripdpi-local-workflow-evidence-1e13bbd2.json` validates the required local lanes for `1e13bbd2`, `/tmp/ripdpi-feature-gap-readiness-1e13bbd2.json` marks the local workflow row ready while leaving rooted physical, TalkBack activation, cellular handover, routed-netem route proof, and production relay matrix rows blocked/manual, and `/tmp/ripdpi-feature-signoff-1e13bbd2.log` still exits 1 for those blockers. `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-proxy-e2e.sh --profile device --timeout-ms 12000 --out-dir test-lab/artifacts/proxy-e2e-pixel7-current-20260521-045450-1e13bbd2 --keep-lab` installed the current `githubDebug` APK successfully, then blocked before Maestro because the device remained behind secure keyguard; the artifact preserves device logs, `wake-unlock.exit=1`, and `window-after-wake-unlock.txt` with `mDreamingLockscreen=true` and `isKeyguardShowing=true`. The locked-device-safe diagnostics broadcast under `test-lab/artifacts/diagnostics-pixel7-current-20260521-045627-1e13bbd2/probe-device-diagnostics.json` passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against `192.168.1.13` with `errors=[]`, degrading only on expected `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE`.

The 2026-05-21 continuation on `main` at `a42181d8` reran current-head rooted-emulator UI-driven proxy and VPN E2E on the Magisk-rooted `RIPDPI_Root_API34` AVD. `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-proxy-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/proxy-e2e-emulator-current-20260521-050326-a42181d8 --keep-lab` passed Maestro connect/disconnect, proxy readiness, DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`, then left no RIPDPI foreground service. `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-vpn-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/vpn-e2e-emulator-current-20260521-050456-a42181d8 --skip-start --keep-lab` passed Maestro connect/disconnect, VPN establishment, TUN, protected egress, IPv4 route, DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`, then left no RIPDPI foreground service. The emulator crash buffer remained empty after both runs.

The 2026-05-21 continuation after `d2760602` added a repeatable rooted-emulator netem runner and verified it against the Magisk-rooted `RIPDPI_Root_API34` AVD. `ANDROID_SERIAL=emulator-5554 RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-rooted-emulator-netem.sh --skip-start --skip-install --out-dir test-lab/artifacts/rooted-emulator-netem-runner-20260521-052444-d2760602` passed baseline and shaped diagnostics, asserted DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`, recorded `qdisc netem 8005: root ... delay 200.0ms 40.0ms loss 1%`, and restored the default emulator qdisc after cleanup. This makes the rooted-emulator controlled-network lane reproducible while leaving the physical routed-netem release row open.

The 2026-05-21 continuation after `b6b19991` added a repeatable local mock-relay fault matrix and verified it on the Magisk-rooted `RIPDPI_Root_API34` AVD. `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PROBE_TIMEOUT_MS=3000 test-lab/scripts/run-mock-relay-matrix.sh --profile emulator --out-dir test-lab/artifacts/mock-relay-matrix-20260521-054535-b6b19991` validated the example relay matrix, installed the current `githubDebug` APK, and wrote five scenario results: ready exited 0 with `relayReady=true` and `errors=0`, while invalid credentials, malformed response, server reset, and timeout all exited non-zero with `relayReady=false`, one typed relay error, and DNS, HTTP, HTTPS, TCP, and UDP still true. This closes a fast local relay-readiness regression slice while leaving the provider-backed relay release row open.

The 2026-05-21 continuation at `6ac4143d` reran the same local mock-relay fault matrix on the attached non-rooted Pixel 7. `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PROBE_TIMEOUT_MS=5000 test-lab/scripts/run-mock-relay-matrix.sh --profile device --out-dir test-lab/artifacts/mock-relay-matrix-pixel7-20260521-055211-6ac4143d` restarted the device-profile lab, installed the current `githubDebug` APK, and wrote five physical-device scenario results: ready exited 0 with `relayReady=true` and `errors=0`, while invalid credentials, malformed response, server reset, and timeout all exited non-zero with `relayReady=false`, one typed relay error, and DNS, HTTP, HTTPS, TCP, and UDP still true. This adds non-root physical same-LAN relay-fault evidence without changing the provider-backed relay blocker.

The 2026-05-21 continuation at `cc77ba1d` tightened the read-only readiness preflight for physical TalkBack evidence. `test-lab/scripts/check-feature-gap-readiness.sh` now reports secure-keyguard state in the `manual_talkback` row and keeps that row blocked when the target is locked even if TalkBack is active, because physical app-window and UI-driven TalkBack verification cannot run behind keyguard. `bash -n`, `test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, live Pixel 7 readiness under `/tmp/ripdpi-feature-gap-readiness-cc77ba1d.json`, expected-blocking signoff under `/tmp/ripdpi-feature-signoff-cc77ba1d.log`, and `scripts/ci/act-local.sh local-network-lab` passed; the live Pixel row now says TalkBack is inactive and the device is locked.

The 2026-05-21 continuation at `8bc87c2d` reran the current-head local packet and TSPU workflow substitutes under `act`. `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh cli-packet-smoke tspu-live` wrote `/tmp/ripdpi-act-packet-tspu-8bc87c2d.log`, passed CLI packet smoke in 375 seconds across the TCP split, disorder, fake, OOB, disoob, transport knobs, UDP/QUIC, and adaptive families, then passed TSPU live in 59 seconds with 9 live nfqueue cells: 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive. The combined local `act` run finished in 434 seconds with both jobs passed.

The 2026-05-21 continuation at `59309e78` reran the repeatable rooted-emulator controlled-network lane on the Magisk-rooted `RIPDPI_Root_API34` AVD. `ANDROID_SERIAL=emulator-5554 RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-rooted-emulator-netem.sh --skip-install` restarted the emulator-profile lab, verified app-context root, applied `tc qdisc replace dev wlan0 root netem delay 200ms 40ms loss 1%`, and wrote `test-lab/artifacts/rooted-emulator-netem-20260521-064100-59309e78/summary.tsv` with baseline and shaped diagnostics both at `errors=0` plus DNS, HTTP, HTTPS, TCP, UDP, and `relayReady=true`. `qdisc-netem.txt` records the shaped qdisc, and `qdisc-after-cleanup.txt` confirms the emulator qdisc was restored after the run.

The 2026-05-21 continuation at `89472ae0` fixed the local signoff guard so the same readiness check works when invoked from macOS system Bash 3.2 wrappers as well as the Homebrew Bash used by the normal test scripts. `/tmp/ripdpi-local-workflow-evidence-89472ae0.json` validates the required local workflow lanes for that commit, `/tmp/ripdpi-feature-gap-readiness-89472ae0.json` keeps the current Pixel 7/rooted-emulator readiness rows explicit, and `/tmp/ripdpi-feature-signoff-89472ae0.log` still blocks only on the known environment/operator rows.

The 2026-05-21 continuation at `3437c467` fixed the host DNS helper so DNS-over-TCP reads exact TCP frame prefixes instead of assuming `recv(2)` returns both bytes at once, and the Android debug probe now records DNS transport plus fallback error metadata. The reproduced failure showed Pixel 7 DNS UDP drop followed by a one-byte TCP prefix read and `fallbackErrorCode=ILLEGALARGUMENTEXCEPTION`; after the fix, `test-lab/artifacts/host-dns-udp-and-udp-loss-pixel7-20260521-073009-3437c467/probe-device-diagnostics.json` passed the combined controlled fault: host sanity proved `dns_udp_drop=ok`, `dns_tcp_fallback=ok`, and `udp_echo_drop=ok`; Pixel 7 reported `dns.ok=true`, `dns.transport=tcp`, `dns.fallbackErrorCode=null`, HTTP/HTTPS/TCP/relay readiness true, and only the intentionally dropped UDP echo as a recoverable degraded error. `/tmp/ripdpi-local-workflow-evidence-3437c467.json` validates the local workflow lanes for this head, `/tmp/ripdpi-feature-gap-readiness-3437c467.json` keeps the same real environment/operator blockers explicit, and `/tmp/ripdpi-feature-signoff-3437c467.log` still blocks only on those known rows plus the intentionally incomplete audit.

The 2026-05-21 continuation at `b519a26a` added `test-lab/scripts/run-host-fault-matrix.sh` so same-LAN DNS/UDP fault combinations are repeatable instead of ad hoc. `ANDROID_SERIAL=31130DLH2000EG RIPDPI_PROBE_TIMEOUT_MS=5000 test-lab/scripts/run-host-fault-matrix.sh --profile device --skip-start` wrote `test-lab/artifacts/host-fault-matrix-20260521-074805-b519a26a/summary.tsv` with four passing rows: baseline used DNS UDP and UDP echo successfully, DNS UDP drop recovered through `dns.transport=tcp`, UDP echo drop surfaced `SOCKETTIMEOUTEXCEPTION`, and the combined DNS UDP plus UDP echo drop kept DNS/HTTP/HTTPS/TCP/relay readiness true while UDP alone degraded.

The 2026-05-21 continuation at `aa3208a7` added `test-lab/scripts/run-netem-forwarder.sh` so the physical TCP-family netem proof is repeatable. `ANDROID_SERIAL=31130DLH2000EG RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-netem-forwarder.sh --profile device --skip-start` wrote `test-lab/artifacts/netem-forwarder-tcp-20260521-075959-aa3208a7/summary.tsv`: baseline, `tc netem delay 200ms 40ms`, and `tc netem loss 15%` all passed with `errors=0`, DNS/HTTP/HTTPS/TCP/UDP/relay readiness true, and the delay row visibly raised HTTP, HTTPS, and TCP latencies while DNS and UDP stayed on host helpers by design.

## Run Metadata

| Field | Value |
| --- | --- |
| Date/time | 2026-05-21, Asia/Tbilisi |
| Operator | Codex local QA agent |
| Git commit | Tested PR #118 head `0c745ec9`; current `main` continuation includes local fixes and evidence commits through `7d7535e0`, plus the 2026-05-21 continuation through `aa3208a7` including `654a5932`, `5700607c`, `1f58dab9`, `c4b2cdaa`, `6432eeb8`, `f3ba98ac`, `b0f9ec57`, `591afc7f`, `7f02179c`, `ace161b5`, `33f60083`, `f91afb30`, `69d1a7fe`, `53aca3ab`, `e77b3a36`, `ff7e0163`, `70c70a5c`, `8a9e4869`, `3f678d4c`, `1e13bbd2`, `a42181d8`, `d2760602`, `b6b19991`, `6ac4143d`, `f8916be2`, `cc77ba1d`, `8bc87c2d`, `59309e78`, `89472ae0`, `f7e85f07`, `f8b3bade`, `3437c467`, `41f9a099`, `b519a26a`, `c681dcf8`, and `aa3208a7` |
| Device | Pixel 8 Pro `38080DLJG000GX`, Android 16 / API 36, non-rooted for recorded 2026-05-19 physical runs; Pixel 7 `31130DLH2000EG`, Android 17 / API 37, non-rooted for the 2026-05-20 `main` continuation |
| Local lab host | macOS Darwin host `po4ykas-MacBook-Pro.local`; stale device-profile lab env had `192.168.1.9`, the earlier corrected-host retry exposed an unreachable `172.20.10.5/28` topology, and the current lab restart detected reachable host `192.168.1.13` while Pixel 7 was on `192.168.1.3/24` |
| Readiness artifact | `test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json` |
| Latest validated continuation readiness artifact | `/tmp/ripdpi-feature-gap-readiness-3437c467.json` |
| Latest validated local workflow evidence | `/tmp/ripdpi-local-workflow-evidence-3437c467.json` |
| Latest validated sign-off guard output | `/tmp/ripdpi-feature-signoff-3437c467.log` |
| Emulator TalkBack artifact | `test-lab/artifacts/talkback-emulator-20260520-current/` |
| Pixel 7 TalkBack active-service artifact | `test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/` |
| Pixel 7 current TalkBack blocker artifact | `test-lab/artifacts/talkback-pixel7-blocked-20260521-024320-7f02179c/` |
| Emulator Persian RTL large-font artifact | `test-lab/artifacts/rtl-large-font-emulator-20260521-024157-7f02179c/` |
| Emulator diagnostics artifact | `test-lab/artifacts/diagnostics-emulator-20260520-current/probe-emulator-diagnostics.json` |
| Pixel 7 direct diagnostics artifact | `test-lab/artifacts/diagnostics-pixel7-20260520-main-same-lan/probe-device-diagnostics.json` |
| Pixel 7 current diagnostics artifact | `test-lab/artifacts/diagnostics-pixel7-20260521-021753-b0f9ec57/probe-device-diagnostics.json` |
| Pixel 7 latest diagnostics artifact | `test-lab/artifacts/diagnostics-pixel7-current-20260521-045627-1e13bbd2/probe-device-diagnostics.json` |
| Pixel 7 latest proxy E2E keyguard blocker artifact | `test-lab/artifacts/proxy-e2e-pixel7-current-20260521-045450-1e13bbd2/` |
| Pixel 7 current host DNS/UDP diagnostics artifact | `test-lab/artifacts/physical-host-dns-udp-pixel7-20260521-025413-ace161b5/probe-physical-diagnostics.json` |
| Pixel 7 current controlled host DNS/UDP loss artifact | `test-lab/artifacts/host-dns-udp-and-udp-loss-pixel7-20260521-073009-3437c467/probe-device-diagnostics.json` |
| Pixel 7 current host fault matrix artifact | `test-lab/artifacts/host-fault-matrix-20260521-074805-b519a26a/summary.tsv` |
| Pixel 7 current E2E instrumentation artifact | `test-lab/artifacts/pixel7-e2e-current-20260521-030803-33f60083/` |
| Pixel 7 Toxiproxy fault matrix | `test-lab/artifacts/toxiproxy-pixel7-20260521-013905-c4b2cdaa/` |
| Pixel 7 Docker UDP publication blocker artifact | `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/` |
| Pixel 7 netem forwarder attempt | `test-lab/artifacts/netem-forwarder-pixel7-20260521-014938-6432eeb8/` |
| Pixel 7 netem TCP-family route proof | `test-lab/artifacts/netem-forwarder-tcp-pixel7-20260521-015154-6432eeb8/` |
| Pixel 7 repeatable netem TCP forwarder artifact | `test-lab/artifacts/netem-forwarder-tcp-20260521-075959-aa3208a7/summary.tsv` |
| Rooted-emulator on-device netem diagnostics | `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/` |
| Rooted-emulator netem runner artifact | `test-lab/artifacts/rooted-emulator-netem-20260521-064100-59309e78/summary.tsv` |
| Mock relay fault matrix artifact | `test-lab/artifacts/mock-relay-matrix-20260521-054535-b6b19991/summary.tsv` |
| Pixel 7 mock relay fault matrix artifact | `test-lab/artifacts/mock-relay-matrix-pixel7-20260521-055211-6ac4143d/summary.tsv` |
| Pixel 7 physical handover artifact | `test-lab/artifacts/physical-handover-pixel7-20260520-main/` |
| Appium report | `appium/appium-report.html` |
| Proxy E2E artifact | `test-lab/artifacts/proxy-e2e-20260519-015247/probe-device-proxy.json` |
| Emulator proxy E2E artifact | `test-lab/artifacts/proxy-e2e-emulator-20260520-current/probe-emulator-proxy.json` |
| Rooted-emulator latest proxy E2E artifact | `test-lab/artifacts/proxy-e2e-emulator-current-20260521-050326-a42181d8/probe-emulator-proxy.json` |
| VPN E2E artifact | `test-lab/artifacts/vpn-e2e-20260519-015119/probe-device-vpn.json` |
| Emulator VPN E2E artifact | `test-lab/artifacts/vpn-e2e-emulator-20260520-current/probe-emulator-vpn.json` |
| Rooted-emulator latest VPN E2E artifact | `test-lab/artifacts/vpn-e2e-emulator-current-20260521-050456-a42181d8/probe-emulator-vpn.json` |
| Android packet-smoke artifact | `test-lab/artifacts/android-packet-smoke-pixel8pro-full-after-autolearn-fixture-20260519` |
| Pixel 7 packet-smoke artifact | `test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c/` |
| Rooted-emulator packet-smoke artifact | `test-lab/artifacts/android-packet-smoke-20260520-emulator-raw` |
| Rooted-emulator verified raw strategy artifact | `test-lab/artifacts/android-packet-smoke-root-verified-20260520-main` |
| Rooted-emulator packet-owned TCP verified artifact | `test-lab/artifacts/android-packet-smoke-root-packet-owned-20260520-main-final` |
| Rooted-emulator current full packet-smoke artifact | `test-lab/artifacts/android-packet-smoke-rooted-emulator-current-20260521-043138-3f678d4c/` |
| Rooted-emulator current root-helper lifecycle artifact | `test-lab/artifacts/root-helper-current-20260521-034956-ff7e0163/TEST-root-helper.xml` |
| Adversarial middlebox dry-run artifact | `test-lab/artifacts/tspu-dryrun-act-local-20260521-034613-e77b3a36/verdict-report.json` |
| Adversarial middlebox live-smoke artifact | `test-lab/artifacts/tspu-live-smoke-act-local-20260521-034101-e77b3a36/verdict-report.json` |
| Netem capability artifact | `test-lab/artifacts/netem-container-capability-20260519-current.txt` |

## Command Evidence

| Command or artifact | Result | Notes |
| --- | --- | --- |
| `adb devices -l` | Ready | Connected Pixel 8 Pro `38080DLJG000GX`. |
| `./gradlew :app:testGithubDebugUnitTest -Pripdpi.skipNativeBuild=true` | Passed | Full app unit lane passed after fixing the local-mode stop policy and Compose assertion issues. |
| `ANDROID_SERIAL=38080DLJG000GX ./test-lab/scripts/run-proxy-e2e.sh --profile device --skip-start --skip-install --keep-lab --timeout-ms 12000` | Passed with expected degraded QUIC probe | Proxy connect/disconnect passed; fixed loopback proxy readiness, DNS, HTTP, HTTPS, TCP, UDP, relay readiness, and service cleanup passed. The verdict is `Degraded` only because Android debug QUIC probing is explicitly unsupported and `errors=[]`. |
| `ANDROID_SERIAL=38080DLJG000GX ./test-lab/scripts/run-vpn-e2e.sh --profile device --skip-start --skip-install --keep-lab --timeout-ms 12000` | Passed with expected degraded QUIC probe | VPN consent/start, TUN, protected egress, IPv4 route, DNS, HTTP, HTTPS, TCP, UDP, relay readiness, and disconnect cleanup passed. VPN mode no longer requires the fixed `127.0.0.1:1080` listener because the service may use an ephemeral authenticated internal SOCKS hop; `errors=[]`. |
| `./gradlew :app:connectedGithubDebugAndroidTest -Pripdpi.localNativeAbis=arm64-v8a -Pandroid.testInstrumentationRunnerArguments.package=com.poyka.ripdpi.e2e` | Passed | Physical Pixel E2E package finished 35/35 tests with zero failures. |
| `ANDROID_SERIAL=31130DLH2000EG ./gradlew :app:connectedGithubDebugAndroidTest -Pripdpi.localNativeAbis=arm64-v8a -Pandroid.testInstrumentationRunnerArguments.package=com.poyka.ripdpi.e2e --console=plain` on `33f60083` | Passed | Current-head Pixel 7 E2E package finished 39/39 tests with zero failures, errors, or skips. Suites covered diagnostics network flows, environment preflight, native telemetry golden smoke, network path E2E, and packet-smoke instrumentation. Artifact: `test-lab/artifacts/pixel7-e2e-current-20260521-030803-33f60083/`; `logcat-crash-ripdpi-filtered.txt` is empty. |
| `ANDROID_SERIAL=38080DLJG000GX RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-pixel8pro-full-after-autolearn-fixture-20260519 bash scripts/ci/run-android-packet-smoke.sh` | Passed | Non-rooted physical packet-smoke passed 17/17 rows: proxy packet families, VPN baseline, DoH/DoT/DNSCrypt/DoQ success and fault rows, host autolearn, remembered policy, and ws tunnel fallback. |
| `./gradlew :app:assembleGithubDebugAndroidTest -Pripdpi.skipNativeBuild=true --console=plain` on `main` | Failed before fix, then passed after `0c8f2189` | `main` had both Java and Kotlin `TestNetworkProbeReceiver` classes, causing `Redeclaration: class TestNetworkProbeReceiver`. Deleting the stale Java androidTest copy restored the Android test APK compile. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json` | Passed with blocked/manual rows | Current readiness selects physical Pixel 7, reports root blocked because `su 0 id` is unavailable, reports TalkBack blocked because it is installed but inactive, marks handover and routed netem manual, validates the example relay matrix as manual provider evidence, and blocks remote workflow confirmation because local `main` is ahead of `origin/main`. |
| Pixel 7 TalkBack operator pass under `test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/`, including `feature-gap-readiness-talkback-active.json` | Passed with blocked/manual rows | TalkBack was enabled from adb operator commands, readiness marked `manual_talkback` ready on the physical Pixel 7, Home/Settings/Advanced Settings/Diagnostics launch commands all exited 0, and device settings were restored to `enabled_accessibility_services=null`, `accessibility_enabled=0`, `touch_exploration_enabled=0`, and `stay_on_while_plugged_in=0`. The captured app-screen UI XML and screenshots are not accepted as RIPDPI UI evidence because the Pixel was locked and dumps resolved to SystemUI/keyguard instead of the app window. |
| Read-only current Pixel 7 TalkBack blocker capture under `test-lab/artifacts/talkback-pixel7-blocked-20260521-024320-7f02179c/` | Blocked for physical app-window evidence | The physical Pixel 7 still reports `accessibility_enabled=0`, `enabled_accessibility_services=null`, and `touch_exploration_enabled=0`; TalkBack is installed, but `dumpsys window` shows `mCurrentFocus=NotificationShade`, `mFocusedApp=com.poyka.ripdpi/.activities.MainActivity`, `mDreamingLockscreen=true`, and `isKeyguardShowing=true`, so no physical app-window TalkBack hierarchy can be accepted until the operator unlocks the device and enables TalkBack manually. |
| `ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/restart-lab.sh --profile device && ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/adb-install-debug.sh && ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/adb-run-probe-device.sh --mode diagnostics --out-dir test-lab/artifacts/diagnostics-pixel7-20260520-main` | Failed with topology evidence | The install succeeded on Pixel 7, but the device-profile probe used stale `192.168.1.9` from `lab-env.sh`; Pixel 7 was `192.168.1.3/24` and could not reach that host. This exposed stale lab-host reuse after host network changes. |
| `ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/adb-run-probe-device.sh --mode diagnostics --out-dir test-lab/artifacts/diagnostics-pixel7-20260520-main-current-host` after `fd7f164f` | Failed with corrected topology evidence | The wrapper ignored stale `192.168.1.9` and used current host `172.20.10.5`. The probe still failed across DNS, HTTP, HTTPS, TCP, UDP, and relay because Pixel 7 is on `192.168.1.3/24` and cannot route to the Mac's current `172.20.10.5/28` network. |
| `./test-lab/scripts/start-lab.sh --profile emulator` followed by `ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/adb-run-probe-device.sh --mode diagnostics --out-dir test-lab/artifacts/diagnostics-pixel7-20260520-main-same-lan` | Passed with expected degraded QUIC probe | The restarted local lab advertised reachable host `192.168.1.13` on the same LAN as Pixel 7 `192.168.1.3`; device-profile diagnostics passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`, while QUIC remained explicitly unsupported by the Android debug probe. |
| `ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/restart-lab.sh --profile device` plus the Pixel 7 Toxiproxy matrix in `test-lab/artifacts/toxiproxy-pixel7-20260521-013905-c4b2cdaa/` | Passed with expected structured faults | Baseline diagnostics passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`; the latency toxic returned HTTP 200 with 643 ms latency and no errors; the timeout toxic produced a recoverable HTTP `Fail` with JSON output; and the TLS reset toxic produced a recoverable HTTPS `Degraded` result while DNS, HTTP, TCP, UDP, and relay stayed healthy. |
| `ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/restart-lab.sh --profile device && ANDROID_SERIAL=31130DLH2000EG ./test-lab/scripts/adb-run-probe-device.sh --mode diagnostics --timeout-ms 12000 --out-dir test-lab/artifacts/diagnostics-pixel7-20260521-021753-b0f9ec57` | Passed with expected degraded QUIC probe | Fresh current-host Pixel 7 diagnostics passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against `192.168.1.13` with `errors=[]`; QUIC remains explicitly unsupported by the Android debug probe. Logs: `/tmp/ripdpi-restart-lab-device-b0f9ec57.log` and `/tmp/ripdpi-pixel7-diagnostics-b0f9ec57.log`. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-proxy-e2e.sh --profile device --timeout-ms 12000 --out-dir test-lab/artifacts/proxy-e2e-pixel7-current-20260521-045450-1e13bbd2 --keep-lab` | Blocked by secure keyguard after install | The current `githubDebug` APK installed on Pixel 7, then `adb-wake-unlock.sh` failed because the device remained behind secure keyguard. The artifact contains device logs plus `wake-unlock.exit=1`; `window-after-wake-unlock.txt` still reports `mDreamingLockscreen=true`, `isKeyguardShowing=true`, and `mCurrentFocus=NotificationShade`. No RIPDPI service leak was present after the blocked run. |
| `ANDROID_SERIAL=31130DLH2000EG test-lab/scripts/adb-run-probe.sh --profile device --mode diagnostics --timeout-ms 12000 --out-dir test-lab/artifacts/diagnostics-pixel7-current-20260521-045627-1e13bbd2` | Passed with expected degraded QUIC probe | Locked-device-safe current-head diagnostics passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against `192.168.1.13` with `errors=[]`; QUIC remains explicitly unsupported by the Android debug probe. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-proxy-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/proxy-e2e-emulator-current-20260521-050326-a42181d8 --keep-lab` | Passed with expected degraded QUIC probe | Current-head rooted-emulator proxy E2E passed Maestro connect/disconnect, proxy readiness, DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`; crash buffer and service-leak checks were empty after cleanup. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a test-lab/scripts/run-vpn-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/vpn-e2e-emulator-current-20260521-050456-a42181d8 --skip-start --keep-lab` | Passed with expected degraded QUIC probe | Current-head rooted-emulator VPN E2E passed Maestro connect/disconnect, VPN establishment, TUN, protected egress, IPv4 route, DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`; crash buffer and service-leak checks were empty after cleanup. |
| `ripdpi-netem-forwarder` temporary Linux container with `tc netem` and Pixel 7 diagnostics under `test-lab/artifacts/netem-forwarder-tcp-pixel7-20260521-015154-6432eeb8/` | Passed as partial routed-netem proof | Pixel 7 used host DNS/UDP helpers and routed HTTP `:28080`, HTTPS `:28443`, TCP echo `:29000`, and relay `:20080` through the Linux forwarder. Baseline, `delay 200ms 40ms`, and `loss 15%` runs all exited 0 with `errors=[]`; delay raised HTTP/HTTPS/TCP latencies to 598/1905/963 ms while `tc-delay.txt` recorded `qdisc netem ... delay 200ms 40ms`. |
| Full UDP/DNS netem-forwarder attempt under `test-lab/artifacts/netem-forwarder-pixel7-20260521-014938-6432eeb8/` | Blocked by Docker Desktop UDP publication | TCP HTTP, HTTPS, TCP echo, and relay reached the Linux forwarder, but host-to-container and Pixel-to-container UDP timed out on the published DNS/UDP ports while container-to-host UDP worked. This keeps the routed-netem row partial until a Linux VM/router namespace carries DNS and UDP as well as TCP-family traffic. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-netem-forwarder.sh --profile device --skip-start` | Passed on `aa3208a7` | The committed runner wrote `test-lab/artifacts/netem-forwarder-tcp-20260521-075959-aa3208a7/summary.tsv`; baseline, delay, and loss rows all passed with `errors=0`, DNS/HTTP/HTTPS/TCP/UDP/relay readiness true, and `tc-delay.txt` plus `tc-loss.txt` preserved the applied qdisc state. |
| `ANDROID_SERIAL=emulator-5554 ./test-lab/scripts/adb-install-debug.sh` plus rooted-emulator diagnostics under `test-lab/artifacts/rooted-emulator-netem-20260521-023043-591afc7f/` | Passed as rooted emulator on-device netem proof | The current `githubDebug` APK installed on the app-granting rooted emulator, baseline diagnostics returned `errors=[]`, `tc qdisc replace dev wlan0 root netem delay 200ms 40ms loss 1%` applied successfully, shaped-path diagnostics also returned `errors=[]` with DNS, HTTP, HTTPS, TCP, UDP, and relay readiness, and `qdisc-after-cleanup.txt` confirms the default emulator qdisc was restored. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-rooted-emulator-netem.sh --skip-start --skip-install --out-dir test-lab/artifacts/rooted-emulator-netem-runner-20260521-052444-d2760602` | Passed as repeatable rooted-emulator netem proof | The new runner verified Magisk root, captured `ip route`, captured qdisc before/apply/cleanup state, ran baseline diagnostics and shaped diagnostics, and wrote `summary.tsv` with `errors=0` plus DNS, HTTP, HTTPS, TCP, UDP, and `relayReady=true` for both runs. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_PROBE_TIMEOUT_MS=12000 test-lab/scripts/run-rooted-emulator-netem.sh --skip-install` on `59309e78` | Passed as current-head rooted-emulator netem proof | The rerun restarted the emulator-profile lab, reused the installed current debug APK, verified Magisk root, applied `tc netem delay 200ms 40ms loss 1%`, restored qdisc cleanup, and wrote `test-lab/artifacts/rooted-emulator-netem-20260521-064100-59309e78/summary.tsv` with baseline and shaped DNS, HTTP, HTTPS, TCP, UDP, and relay readiness all true. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PROBE_TIMEOUT_MS=3000 test-lab/scripts/run-mock-relay-matrix.sh --profile emulator --out-dir test-lab/artifacts/mock-relay-matrix-20260521-054535-b6b19991` | Passed as controlled local mock-relay fault matrix | The runner validated `test-lab/relay/provider-matrix.example.json`, installed the current `githubDebug` APK, and wrote a five-row `summary.tsv`: ready exited 0 with `relayReady=true`, while invalid credentials, malformed response, server reset, and timeout all exited non-zero with `relayReady=false`, one relay error, and DNS, HTTP, HTTPS, TCP, and UDP remaining true. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PROBE_TIMEOUT_MS=5000 test-lab/scripts/run-mock-relay-matrix.sh --profile device --out-dir test-lab/artifacts/mock-relay-matrix-pixel7-20260521-055211-6ac4143d` | Passed as physical Pixel 7 mock-relay fault matrix | The runner validated `test-lab/relay/provider-matrix.example.json`, restarted the device-profile lab, installed the current `githubDebug` APK on Pixel 7, and wrote a five-row `summary.tsv`: ready exited 0 with `relayReady=true`, while invalid credentials, malformed response, server reset, and timeout all exited non-zero with `relayReady=false`, one relay error, and DNS, HTTP, HTTPS, TCP, and UDP remaining true. |
| Physical handover attempt under `test-lab/artifacts/physical-handover-pixel7-20260520-main/` plus `dumpsys telephony.registry` | Blocked for cellular, Wi-Fi restored | Wi-Fi-before and Wi-Fi-after same-LAN diagnostics exited 0 and passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness, but the cellular leg is not valid release evidence because Android never exposed an active cellular default network and telephony reported voice/data `OUT_OF_SERVICE`; the adb-reverse cellular probe failed DNS/UDP and is retained only as a blocker artifact. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=indirect RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c bash scripts/ci/run-android-packet-smoke.sh` | Passed after `70c70a5c` | Pixel 7 non-rooted indirect packet-smoke completed 17 successful scenario artifact directories with no failure screenshots. The runner skipped `android_proxy_multidisorder_family`, `android_proxy_ipfrag2_family`, `android_proxy_seqovl_family`, and `android_proxy_fakerst_family` because those rows require `adb_raw_pcap` and the device profile was `physical_indirect`; VPN baseline, encrypted-DNS success/fault rows, host autolearn, remembered policy, ws tunnel fallback, TLS record, TLS random, and observable fake-segment proxy rows all reported `OK (1 test)`. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=raw RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-rooted-emulator-current-20260521-043138-3f678d4c bash scripts/ci/run-android-packet-smoke.sh` | Passed on `3f678d4c` | The Magisk-rooted `RIPDPI_Root_API34` AVD completed the full raw packet-smoke matrix with 21 scenario directories and 21 non-empty raw `device-capture.pcap` files. Seven proxy rows were accepted by raw packet validation: hostfake, fakedsplit, fakeddisorder, FakeRst, TCP IpFrag2, SeqOverlap, and MultiDisorder. Fourteen rows completed by instrumentation success, including TLS record, TLS random record, VPN tunnel baseline, encrypted-DNS success/fault rows, host autolearn, remembered policy, and ws tunnel fallback. The artifact has no `failure-screenshot.png` and no crash/assertion/failure markers; the top-level `shared/` directory is harness support data rather than a scenario. |
| `./gradlew :core:service:testDebugUnitTest --tests com.poyka.ripdpi.services.RootHelperManagerTest --rerun-tasks --no-build-cache -Pkotlin.incremental=false -Pripdpi.skipNativeBuild=true --console=plain` | Passed | Root-helper manager unit coverage verifies fallback to the next launcher when the first `su` launch throws, plus existing readiness and cleanup behavior. |
| `ANDROID_SERIAL=emulator-5554 ./gradlew :app:connectedGithubDebugAndroidTest --console=plain -Pripdpi.localNativeAbis=arm64-v8a -Pandroid.testInstrumentationRunnerArguments.class=com.poyka.ripdpi.integration.RootHelperInstrumentedTest -Pandroid.testInstrumentationRunnerArguments.ripdpi.rootHelperSmoke=true` on `ff7e0163` | Passed | The current root-helper smoke on `RIPDPI_Root_API34` ran 1/1 test with zero failures, errors, or skips. `test-lab/artifacts/root-helper-current-20260521-034956-ff7e0163/` preserves `TEST-root-helper.xml`, `test-result.textproto`, focused root-helper logcat showing extraction, `su -c exec`, socket readiness, and stop cleanup, plus post-run proof that `su -c id` returns uid 0 and no `ripdpi-root-helper` process or root-helper socket file remains. |
| `ANDROID_SERIAL=38080DLJG000GX RUNNER_TEMP=/tmp/ripdpi-appium-20260518 bash scripts/ci/run-appium-smoke.sh` | Passed | Full Appium smoke rerun after fixes: 78 passed, 18 skipped, 1 warning, 0 failures. |
| `/usr/bin/python3 -m runner.cli dry-run --matrix matrix.json --fixtures fixtures --out-dir test-lab/artifacts/tspu-dryrun-current` from the adversarial middlebox harness | Passed | Dry-run matrix produced 63 cells and a complete `verdict-report.json`: 23 blocked, 40 bypassed, 0 degraded, 0 inconclusive. |
| `/usr/bin/python3 -m unittest discover -s tests` from the adversarial middlebox harness | Passed | Adversarial middlebox emulator unit suite ran 53 tests with zero failures. |
| `ANDROID_SERIAL=38080DLJG000GX ./test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json` | Passed | Current-head readiness selects the connected physical Pixel for the Android device row, reports the running `ripdpi-linux-netem` container as a manual routed-netem prerequisite, and leaves rooted physical, TalkBack, handover, relay provider, and remote workflow rows blocked/manual. |
| `adb devices -l` and USB probe during the continuation at `d5f177ca` | Blocked for physical rows | `adb devices -l` listed no attached devices and the USB inventory matched no Pixel/Google/Android/ADB device, so physical Pixel, TalkBack, and handover rows could not be refreshed in the continuation turn. |
| `adb devices -l`, `system_profiler SPUSBDataType`, and `ioreg -p IOUSB -l -w 0` during the 2026-05-20 continuation | Blocked for physical rows | `adb devices -l` listed only `emulator-5554` and the USB inventory matched no Pixel/Google/Android/ADB device, so the connected non-rooted Pixel 8 Pro requirement remains blocked in this turn. |
| `./test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260519-pr118-no-device.json` | Passed with blocked rows | Latest readiness records no attached adb device, no rooted physical target, no TalkBack verification target, no physical handover target, routed-netem manual route proof still required, relay provider config missing, and remote workflow confirmation still blocked until the branch is committed, reviewed, merged, and rechecked. |
| `RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260520-rooted-emulator-no-pixel.json` | Passed with blocked/manual rows | Current readiness selects `emulator-5554` as an emulator Android target, blocks rooted physical, TalkBack, and physical handover rows, marks the running Docker netem container as a manual route-proof prerequisite, validates the example relay matrix as manual provider evidence, and blocks remote workflow confirmation until the review branch is published, merged, and rechecked. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=indirect RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=/tmp/ripdpi-packet-smoke-current bash scripts/ci/run-android-packet-smoke.sh` | Passed | Rooted-emulator packet smoke produced 17 scenario artifact folders with `BUILD SUCCESSFUL`; proxy hostfake/fakedsplit/fakeddisorder rows were assumption-skipped in indirect capture mode while TLS record, TLS random record, VPN baseline, encrypted-DNS success/fault, host autolearn, remembered-policy, and ws tunnel fallback rows completed. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=auto RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=/tmp/ripdpi-packet-smoke-root-current bash scripts/ci/run-android-packet-smoke.sh` | Passed | The Magisk-rooted `RIPDPI_Root_API34` AVD had `su 0 id` root and `/system/bin/tcpdump`; packet smoke completed all 17 scenarios and produced 17 raw `device-capture.pcap` artifacts plus fixture events and logcat snapshots under `/tmp/ripdpi-packet-smoke-root-current`. |
| `ANDROID_HOME=/Users/po4yka/Library/Android/sdk ANDROID_SDK_ROOT=/Users/po4yka/Library/Android/sdk ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=auto RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-rooted-emulator-20260520-fixed bash scripts/ci/run-android-packet-smoke.sh` | Passed | The fixed rooted-emulator packet-smoke matrix completed 17 scenario folders and 191 files. Fourteen rows finished with `BUILD SUCCESSFUL`; the hostfake, fakedsplit, and fakeddisorder rows returned TLS-fixture failures but were accepted only after raw `tcpdump` validation proved fake low-TTL packet patterns reached the local fixture path. No packet-smoke test was accepted solely because JUnit reported `SKIPPED`. |
| `RIPDPI_TSPU_ARTIFACT_DIR=test-lab/artifacts/tspu-dryrun-20260520-current bash scripts/ci/run-tspu-dryrun.sh` | Passed | Current continuation dry-run uses the checked-in Python resolver, ran 53 adversarial middlebox unit tests, and produced `verdict-report.json` totals: 23 blocked, 40 bypassed, 0 degraded, 0 inconclusive across 63 cells. |
| `RIPDPI_TSPU_ARTIFACT_DIR=test-lab/artifacts/tspu-dryrun-20260521-022523-591afc7f bash scripts/ci/run-tspu-dryrun.sh` | Passed | Current-head rerun used the checked-in Python resolver, ran 53 harness tests, and produced `verdict-report.json` totals: 23 blocked, 40 pass-through, 0 degraded, 0 inconclusive across 63 cells. |
| `docker run --rm --privileged ... scripts/ci/run-tspu-live-smoke.sh` on `69d1a7fe` | Passed | The TSPU live handler ran inside the Linux Docker lab without a sudo shim after the runner fix; `verdict-report.json` under `test-lab/artifacts/tspu-live-smoke-20260521-032212-69d1a7fe/` recorded 9 live nfqueue cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive cells. |
| `RIPDPI_TSPU_ARTIFACT_DIR=test-lab/artifacts/tspu-dryrun-act-local-20260521-034613-e77b3a36 scripts/ci/act-local.sh tspu-dryrun` | Passed | The local wrapper now exposes the TSPU dry-run lane as a native job; the post-commit rerun passed 53 harness tests and a 63-cell dry-run matrix with 23 blocked, 40 bypassed, 0 degraded, and 0 inconclusive. |
| `scripts/ci/act-local.sh tspu-live` on `e77b3a36` | Passed | The local wrapper now exposes the TSPU live workflow through `act`; `/tmp/ripdpi-act-tspu-live-e77b3a36.log` shows the workflow installed NetfilterQueue under the lean runner, skipped hosted artifact upload under `ACT=true`, selected smoke port `59421`, and completed 9 live nfqueue cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive. |
| `docker run --rm --privileged ... scripts/ci/run-tspu-live-smoke.sh` on `e77b3a36` | Passed | The persisted post-commit Docker artifact `test-lab/artifacts/tspu-live-smoke-act-local-20260521-034101-e77b3a36/` used dynamic smoke port `46927` and recorded 9 live cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh cli-packet-smoke tspu-live` on `8bc87c2d` | Passed | Current-head local `act` rerun wrote `/tmp/ripdpi-act-packet-tspu-8bc87c2d.log`; CLI packet-smoke passed all eight families in 375 seconds, and TSPU live passed 9 nfqueue cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive in 59 seconds. |
| Emulator Persian RTL large-font UI capture under `test-lab/artifacts/rtl-large-font-emulator-20260521-024157-7f02179c/` | Passed | `emulator-5554` ran Home, Settings, Diagnostics, and Advanced Settings with app locale `[fa]` and `font_scale=1.45`; each route focused `com.poyka.ripdpi/.activities.MainActivity`, exposed the expected screen test tag in the UI summary, had an empty crash buffer, and the artifact metadata confirms font scale and app locales were restored afterward. |
| `bash test-lab/scripts/check-relay-matrix-config.sh --config test-lab/relay/provider-matrix.example.json` | Passed | Example relay matrix schema remains valid with 14 relays, 14 required paths, and 10 required scenarios; this does not replace operator production relay evidence. |
| `CARGO_TARGET_DIR=target/relay-interoperability-c4b2cdaa bash scripts/ci/run-rust-relay-interoperability.sh` | Passed | Current-head host relay coverage passed the relay fixture stack, 232 transport interoperability tests, 19 `ripdpi-relay-core` tests, and 29 proxy-runtime relay E2E tests. |
| `ANDROID_SERIAL=emulator-5554 ./gradlew :app:connectedGithubDebugAndroidTest -Pripdpi.localNativeAbis=arm64-v8a -Pandroid.testInstrumentationRunnerArguments.class=com.poyka.ripdpi.integration.NativeBridgeInstrumentedTest,com.poyka.ripdpi.integration.ServiceLifecycleIntegrationTest -Pandroid.testInstrumentationRunnerArguments.coverage=false` | Passed | Current-head rooted arm64 emulator relay smoke passed 29/29 instrumentation tests and left `build/android-relay-emulator-smoke-c4b2cdaa/crash-after-relay-smoke.txt` empty. |
| `ANDROID_HOME=/Users/po4yka/Library/Android/sdk ANDROID_SDK_ROOT=/Users/po4yka/Library/Android/sdk ./gradlew :app:assembleRelease -Pripdpi.skipNativeBuild=true -Pripdpi.enableAbiSplits=false -Pripdpi.nativeAbiParallelism=1 --stacktrace --no-build-cache --rerun-tasks --console=plain` | Passed | Aggregate release assembly completed 569 tasks after reproducing the default split-packaging heap failure locally; CI release-verification now matches the release workflow's no-split artifact packaging shape. |
| `c4b2cdaa` current-head E2E cleanup verification | Passed | The fix prevents dormant Android E2E cleanup from starting Hilt services just to stop them; current-head emulator reruns passed `EnvironmentPreflightE2ETest`, `DiagnosticsNetworkE2ETest` 8/8, and `NetworkPathE2ETest` 7/7 with empty crash buffers, followed by `:app:ktlintCheck`, `:app:detekt`, and `git diff --check`. |
| `ANDROID_HOME=/Users/po4yka/Library/Android/sdk ANDROID_SDK_ROOT=/Users/po4yka/Library/Android/sdk ./gradlew :app:ktlintCheck :app:assembleGithubDebugAndroidTest -Pripdpi.skipNativeBuild=true -Pripdpi.localNativeAbis=x86_64 --console=plain` plus `bash scripts/ci/test-android-packet-smoke-lib.sh` | Passed | The raw-packet-only annotation compiles into the Android test APK, app ktlint passes, and the packet-smoke runner library still passes while the dedicated packet-smoke script continues to execute raw rows by explicit class selector. AndroidX Test runner 1.7.0 source in the local Gradle cache confirms `notAnnotation` is parsed and applied as an exclusion filter. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh cli-packet-smoke` | Passed | Local `act` CLI packet-smoke completed in 382s after the runner supplied Docker Desktop credentials, packet-capture capabilities, and local artifact handling for `act`. |
| `scripts/ci/act-local.sh cargo-deny rust-workspace-tests rust-loom rust-turmoil rust-fuzz-smoke rust-network-e2e` | Passed | Native local CI batch completed in 1579s with `cargo-deny`, workspace tests, loom, turmoil, fuzz smoke, and Rust network E2E green. |
| `scripts/ci/act-local.sh rust-cross-check` | Passed | Android ABI cross-target cargo checks pass locally after the runner resolves the Android SDK/NDK instead of silently skipping or failing on unset `ANDROID_HOME`. |
| `scripts/ci/act-local.sh rust-lint` | Passed | Rust fmt and clippy completed after current native lint issues were fixed. |
| `scripts/ci/act-local.sh build` | Passed | Local CI mirror build lane completed in 146s: `assembleDebug`, `testDebugUnitTest`, and `:app:verifyRoborazziGithubDebug` passed. |
| `scripts/ci/act-local.sh native-bloat` | Passed | Native bloat verification completed in 522s and matched `scripts/ci/native-bloat-baseline.json`. |
| `scripts/ci/act-local.sh release-verification` | Passed | Release-verification completed in 1015s with full `:app:assembleRelease` on local infrastructure. The run exposed Kotlin Hilt qualifier warnings that were fixed in `0747b94f`. |
| `scripts/ci/act-local.sh coverage` | Passed | Coverage lane completed in 803s; Gradle/Jacoco `coverageReport` and Rust `cargo-llvm-cov` passed with Rust line coverage at 81.51%. |
| `./gradlew :app:compileGithubDebugKotlin :app:compileGithubReleaseKotlin -Pripdpi.skipNativeBuild=true --console=plain` | Passed | Focused compile verified `0747b94f`; the previous Hilt `@ApplicationContext` default-target warnings in `ClipboardReader` and `WidgetUpdater` no longer appear. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh local-network-lab` | Failed before fix, then passed after `7966e673`; rerun passed on current `main` | The first local act attempts exposed that the wrapper did not map the local-network-lab workflow/job, the workflow assumed `RUNNER_TEMP` already existed, Ruby was missing from the lean act image, and artifact upload needed to be skipped under `ACT=true`. The current rerun after the Pixel 7 same-LAN and cellular-readiness updates passed in 49s: shell checks, signoff/readiness/relay-matrix preflights, custom-probe broadcast tests, workflow YAML parsing, Docker Compose model, emulator-profile lab startup, doctor checks, artifact archive/redaction, and cleanup all completed. |
| `python3 -m unittest scripts.tests.test_offline_analytics_pipeline && python3 -m scripts.analytics.pipeline run-all --manifest scripts/analytics/sample-corpus.json --output-dir /tmp/ripdpi-offline-analytics-20260520-main-local` | Passed | Local offline analytics workflow substitute ran 7 unit tests and produced the sample-corpus outputs `offline-records.json`, `clustered-records.json`, `drift-report.json`, `report.md`, `strategy-pack-catalog.candidate.json`, `device-fingerprint-catalog.candidate.json`, and `device-fingerprint-winner-mappings.candidate.json`. |
| `RIPDPI_FUZZ_SECONDS=30 bash scripts/ci/run-rust-fuzz-smoke.sh` | Passed | Local short Fuzz Nightly substitute exercised `packets_parse`, `packets_tls_quic`, `failure_http_response`, and `failure_field_cache` for 30s each with no sanitizer crash; generated corpus additions were discarded after the run. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" act workflow_dispatch -P "ubuntu-latest=catthehacker/ubuntu:act-latest" --container-architecture linux/arm64 --rm -j mutants -W .github/workflows/mutation-testing.yml -e /tmp/ripdpi-mutation-event-20260520.json --env ACT=true --env GITHUB_EVENT_NAME=workflow_dispatch --env RUNNER_TEMP=/tmp/runner-temp` | Failed before fixes, then passed after `ec1d9504`, `df4d87d1`, and `d34ea32f` | Local Linux `act` mutation workflow ran the scoped `ripdpi-privileged-ops` changed-line mutation set from an event with `packages=ripdpi-privileged-ops` and `in_diff=true`; the final run passed with an unmutated baseline and `3 mutants tested in 4m: 3 caught`. |
| `bash -n test-lab/scripts/check-local-workflow-evidence.sh test-lab/scripts/test-local-workflow-evidence.sh test-lab/scripts/check-feature-gap-readiness.sh test-lab/scripts/test-feature-gap-readiness.sh && test-lab/scripts/test-local-workflow-evidence.sh` plus `test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, and `test-lab/scripts/test-feature-checklist-coverage.sh` | Passed after `0eb6a345` | The local workflow evidence validator rejects missing lanes and wrong-commit manifests, accepts complete current-commit manifests, and lets readiness mark `remote_workflow_confirmation` ready only when the explicit local evidence file covers CI, CodeQL, local-network-lab, offline analytics, mutation-testing, and Fuzz Nightly. The signoff/checklist guard self-tests still pass. |
| `scripts/ci/act-local.sh rust-workspace-tests rust-cross-check` | Passed on `7d7535e0` | Current-head local CI substitute wrote `/tmp/ripdpi-ci-current-7d7535e0.log`; `rust-workspace-tests` passed architecture gates, nextest slices, 3,400 workspace tests, and the ignored startup-latency smoke, then `rust-cross-check` passed all four Android targets. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" act -n -W .github/workflows/codeql.yml -j analyze` | Passed on `7d7535e0` | CodeQL workflow wiring dry-run wrote `/tmp/ripdpi-codeql-act-dryrun-7d7535e0.log` and resolved the actions matrix, checkout, initialize, analyze, and post steps successfully. |
| `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh local-network-lab` | Passed on `7d7535e0` | Current-head local-network-lab evidence wrote `/tmp/ripdpi-local-network-lab-7d7535e0.log`; the `act` run passed guard/preflight self-tests, Docker Compose model validation, emulator-profile lab startup, host lab doctor checks with 8 pass, 0 fail, 3 skip, archive redaction, and cleanup. |
| `python3 -m unittest scripts.tests.test_offline_analytics_pipeline && python3 -m scripts.analytics.pipeline run-all --manifest scripts/analytics/sample-corpus.json --output-dir /tmp/ripdpi-offline-analytics-7d7535e0` | Passed on `7d7535e0` | Local offline analytics wrote `/tmp/ripdpi-offline-analytics-7d7535e0.log` and produced `offline-records.json`, `clustered-records.json`, `drift-report.json`, `report.md`, `strategy-pack-catalog.candidate.json`, `device-fingerprint-catalog.candidate.json`, and `device-fingerprint-winner-mappings.candidate.json`. |
| `MUTANTS_PACKAGES=ripdpi-android-diagnostics-adapter MUTANTS_JOBS=2 bash scripts/ci/run-rust-mutants.sh --in-diff "git diff HEAD^..HEAD"` | Passed on `7d7535e0` | Focused diagnostics mutation evidence wrote `/tmp/ripdpi-mutation-diagnostics-7d7535e0.log`; the unmutated baseline passed and the only generated current-diff mutant was unviable, leaving 0 missed mutants. |
| `RIPDPI_FUZZ_SECONDS=30 bash scripts/ci/run-rust-fuzz-smoke.sh` | Passed on `7d7535e0` | Current-head fuzz evidence wrote `/tmp/ripdpi-fuzz-nightly-7d7535e0.log`; `packets_parse`, `packets_tls_quic`, `failure_http_response`, and `failure_field_cache` each ran for 30s without crashes, and generated corpus/artifact files stayed out of the repo. |
| `test-lab/scripts/check-local-workflow-evidence.sh --evidence /tmp/ripdpi-local-workflow-evidence-7d7535e0.json --head 7d7535e04bc5d2828bea973ca1af44ac17fd577b` | Passed | The evidence JSON validates all required local workflow lanes for that commit: `ci:local`, `codeql:act`, `local-network-lab:act`, `offline-analytics:local`, `mutation-testing:local`, and `fuzz-nightly:local`. |
| `RIPDPI_LOCAL_WORKFLOW_EVIDENCE=/tmp/ripdpi-local-workflow-evidence-7d7535e0.json ANDROID_SERIAL=31130DLH2000EG test-lab/scripts/check-feature-gap-readiness.sh --output /tmp/ripdpi-feature-gap-readiness-7d7535e0.json` | Passed with blocked/manual rows | That readiness pass selected connected physical Pixel 7 Android 17/API 37, marked `remote_workflow_confirmation` ready from local evidence, and kept rooted physical, TalkBack activation, cellular handover, routed netem route proof, and production relay matrix blocked/manual. |
| `scripts/ci/act-local.sh rust-lint cargo-deny rust-fuzz-smoke`, `scripts/ci/act-local.sh local-network-lab`, `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" act -n -W .github/workflows/codeql.yml -j analyze`, `scripts/ci/act-local.sh cli-packet-smoke`, `test-lab/scripts/check-local-workflow-evidence.sh --evidence /tmp/ripdpi-local-workflow-evidence-b0f9ec57.json --head b0f9ec576261295982c7632a980cfa2458675119`, and `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_WORKFLOW_EVIDENCE=/tmp/ripdpi-local-workflow-evidence-b0f9ec57.json test-lab/scripts/check-feature-gap-readiness.sh --output /tmp/ripdpi-feature-gap-readiness-b0f9ec57.json` | Passed with blocked/manual rows | Current local-instrument evidence on `b0f9ec57` refreshed the fast native mirror, Docker/`act` lab, CodeQL dry-run, and full CLI packet-smoke lane. Logs: `/tmp/ripdpi-act-local-network-lab-b0f9ec57.log`, `/tmp/ripdpi-codeql-act-dryrun-b0f9ec57.log`, `/tmp/ripdpi-act-cli-packet-smoke-b0f9ec57.log`. Readiness marks `remote_workflow_confirmation` ready from local evidence and keeps rooted physical, TalkBack activation, cellular handover, routed netem route proof, and production relay matrix blocked/manual; sign-off remains blocked in `/tmp/ripdpi-feature-signoff-b0f9ec57.log`. |
| `test-lab/scripts/check-local-workflow-evidence.sh --evidence /tmp/ripdpi-local-workflow-evidence-591afc7f.json --head 591afc7f31830e63ff89a0582e7d2d73e9a3393d` plus `ANDROID_SERIAL=31130DLH2000EG RIPDPI_LOCAL_WORKFLOW_EVIDENCE=/tmp/ripdpi-local-workflow-evidence-591afc7f.json test-lab/scripts/check-feature-gap-readiness.sh --output /tmp/ripdpi-feature-gap-readiness-591afc7f.json` | Passed with blocked/manual rows | Current local workflow evidence validates all required lanes for `591afc7f`; readiness selects Pixel 7, marks `remote_workflow_confirmation` ready from local evidence, and keeps rooted physical, TalkBack activation, cellular handover, physical routed netem proof, and production relay matrix blocked/manual; sign-off remains blocked in `/tmp/ripdpi-feature-signoff-591afc7f.log`. |
| `RIPDPI_HOST_UDP_ECHO=false RIPDPI_HOST_DNS=false test-lab/scripts/start-lab.sh --profile physical` plus Docker-backed host and Pixel 7 diagnostics under `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/` | Blocked by Docker Desktop UDP publication | Docker DNS and UDP echo containers started, but host-loopback UDP echo timed out and the Pixel 7 UDP echo probe timed out while `docker logs test-lab-udp_echo-1` showed received datagrams with source `142.250.75.177`; replies therefore left toward that translated source instead of returning to the sender. DNS recovered through TCP fallback and HTTP, HTTPS, TCP, and relay stayed green, confirming this is a Docker Desktop UDP publication limitation rather than an app regression. |
| `RIPDPI_HOST_UDP_ECHO=true RIPDPI_HOST_DNS=true test-lab/scripts/start-lab.sh --profile physical` plus `ANDROID_SERIAL=31130DLH2000EG test-lab/scripts/adb-run-probe.sh --profile physical --mode diagnostics --timeout-ms 7000 --out-dir test-lab/artifacts/physical-host-dns-udp-pixel7-20260521-025413-ace161b5` | Passed with expected degraded QUIC probe | Host sanity checks reported UDP echo latency 0.5 ms and DNS UDP latency 0.4 ms; Pixel 7 diagnostics passed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against `192.168.1.13` with `errors=[]`, degrading only because Android debug QUIC probing is intentionally unsupported. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_PROBE_TIMEOUT_MS=5000 test-lab/scripts/adb-run-probe.sh --profile device --mode diagnostics --dns-port <faulted-host-dns> --udp-port <faulted-host-udp> --timeout-ms 5000 --out-dir test-lab/artifacts/host-dns-udp-and-udp-loss-pixel7-20260521-073009-3437c467` | Passed with controlled degraded UDP row | Host sanity proved DNS UDP was dropped, DNS TCP fallback answered, and UDP echo was dropped. Pixel 7 diagnostics passed DNS over TCP fallback, HTTP, HTTPS, TCP, and relay readiness; UDP echo failed with `SOCKETTIMEOUTEXCEPTION` as the only recoverable error. |
| `ANDROID_SERIAL=31130DLH2000EG RIPDPI_PROBE_TIMEOUT_MS=5000 test-lab/scripts/run-host-fault-matrix.sh --profile device --skip-start` | Passed on `b519a26a` | The committed runner wrote `test-lab/artifacts/host-fault-matrix-20260521-074805-b519a26a/summary.tsv` with baseline, DNS UDP drop, UDP echo drop, and combined DNS UDP plus UDP echo drop rows. DNS UDP loss recovered through `dns_transport=tcp`; UDP echo loss surfaced `SOCKETTIMEOUTEXCEPTION`; HTTP, HTTPS, TCP, and relay readiness stayed true in every row. |
| `gh pr view 118 --json statusCheckRollup,mergeStateStatus,headRefOid` | Passed for review-branch checks | The tested PR #118 head `0c745ec9` has 38 successful checks, 9 expected skipped schedule/manual lanes, and no failed or pending checks. Green rows include CI, CodeQL, TSPU dry-run, Android network E2E, both Android integration shards, release-verification, Local Network Lab, static analysis, coverage, native builds, Rust checks, Roborazzi, and packet smoke. `mergeStateStatus=BLOCKED`, so merge-to-main and scheduled/manual workflow confirmation remain open. |
| `RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260520-pr118-green-no-pixel.json` | Passed with blocked/manual rows | Current readiness selects `emulator-5554` as an emulator Android target, blocks rooted physical, TalkBack, and physical handover rows, marks the running Docker netem container as a manual route-proof prerequisite, validates the example relay matrix as manual provider evidence, and keeps remote workflow confirmation blocked until the green PR #118 branch is merged and fresh main-branch workflows are collected. |
| `RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260520-current-no-pixel.json` | Passed with blocked/manual rows | Current readiness selects `emulator-5554` as an emulator Android target, blocks rooted physical, TalkBack, and physical handover rows, marks the running Docker netem container as a manual route-proof prerequisite, validates the example relay matrix as manual provider evidence, and keeps remote workflow confirmation blocked because the review branch still is not merged to `main`. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_PACKET_SMOKE_CAPTURE_MODE=raw RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-20260520-emulator-raw bash scripts/ci/run-android-packet-smoke.sh` | Passed | The Magisk-rooted `RIPDPI_Root_API34` AVD completed all 17 Android packet-smoke rows and produced raw on-device `device-capture.pcap` artifacts. Fifteen rows finished with `BUILD SUCCESSFUL`; fakedsplit and fakeddisorder returned the expected TLS fixture timeout and were accepted only after raw packet-pattern validation proved low-TTL TLS payload reached the fixture path. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=raw RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-root-verified-20260520-main RIPDPI_PACKET_SMOKE_SCENARIO_FILTER=android_proxy_seqovl_family bash scripts/ci/run-android-packet-smoke.sh` | Passed after `7e98c387` | The rooted emulator produced `android_proxy_seqovl_family/device-capture.pcap`, and the runner validated an overlapped TCP payload sequence before the post-SYN stream cursor. |
| `ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=raw RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-root-verified-20260520-main RIPDPI_PACKET_SMOKE_SCENARIO_FILTER=android_proxy_fakerst_family bash scripts/ci/run-android-packet-smoke.sh` | Passed after `7e98c387` | The rooted emulator produced `android_proxy_fakerst_family/device-capture.pcap`, and the runner validated a low-TTL TCP RST toward the TLS fixture. |
| `ANDROID_HOME=/Users/po4yka/Library/Android/sdk ANDROID_SDK_ROOT=/Users/po4yka/Library/Android/sdk ANDROID_SERIAL=emulator-5554 RIPDPI_LOCAL_NATIVE_ABI=arm64-v8a RIPDPI_PACKET_SMOKE_CAPTURE_MODE=raw RIPDPI_PACKET_SMOKE_ARTIFACT_DIR=test-lab/artifacts/android-packet-smoke-root-packet-owned-20260520-main-final RIPDPI_PACKET_SMOKE_SCENARIO_FILTER=android_proxy_multidisorder_family bash scripts/ci/run-android-packet-smoke.sh` and the matching `android_proxy_ipfrag2_family` run | Passed after `337b68cc` | MultiDisorder produced `BUILD SUCCESSFUL` and the runner validated 3+ TCP payload segments with out-of-order sequence delivery; TCP IpFrag2 produced `BUILD SUCCESSFUL` and the runner validated a same-ID IPv4 TCP fragment pair toward the TLS fixture. |
| `./test-lab/scripts/restart-lab.sh --profile emulator`, `./test-lab/scripts/adb-install-debug.sh`, and `./test-lab/scripts/adb-run-probe-emulator.sh --mode diagnostics --out-dir test-lab/artifacts/diagnostics-emulator-20260520-current` | Passed with expected degraded QUIC probe | Emulator diagnostics probe verified DNS, HTTP, HTTPS, TCP, UDP, and relay readiness against the Docker lab. Verdict is `Degraded` only because the Android debug probe explicitly reports `QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE`; `errors=[]`. |
| `ANDROID_SERIAL=emulator-5554 MAESTRO_BIN=/Users/po4yka/.maestro/bin/maestro ./test-lab/scripts/run-proxy-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/proxy-e2e-emulator-20260520-current` | Passed with expected degraded QUIC probe | Maestro connected and disconnected local proxy mode on the rooted emulator; debug probe verified proxy readiness plus DNS, HTTP, HTTPS, TCP, UDP, and relay readiness. Verdict is `Degraded` only because Android debug QUIC probing is unsupported; `errors=[]`. |
| `ANDROID_SERIAL=emulator-5554 MAESTRO_BIN=/Users/po4yka/.maestro/bin/maestro ./test-lab/scripts/run-vpn-e2e.sh --profile emulator --timeout-ms 12000 --out-dir test-lab/artifacts/vpn-e2e-emulator-20260520-current` | Passed with expected degraded QUIC probe | Maestro accepted VPN consent, verified active VPN state, ran the lab probe with `vpnEstablished=true`, `tunActive=true`, `egressProtected=true`, and `ipv4RouteInstalled=true`, then disconnected cleanly. Verdict is `Degraded` only because Android debug QUIC probing is unsupported; `errors=[]`. |
| `adb -s emulator-5554 shell settings put secure enabled_accessibility_services com.google.android.marvin.talkback/com.google.android.marvin.talkback.TalkBackService` plus UI hierarchy dumps under `test-lab/artifacts/talkback-emulator-20260520-current/` | Passed | TalkBack is active on the emulator with touch exploration enabled. UI summaries cover Home mode buttons/statuses/tabs, Settings rows and warnings, Advanced Settings checked toggles and inputs, and Diagnostics dashboard tabs/progress/status actions with stable text or content descriptions. |
| `RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json` | Passed with blocked/manual rows | Current readiness selects `emulator-5554` as an emulator Android target, marks `manual_talkback` ready, blocks rooted physical and physical handover rows, marks the running Docker netem container as a manual route-proof prerequisite, validates the example relay matrix as manual provider evidence, and keeps remote workflow confirmation blocked because the review branch still is not merged to `main`. |
| `test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260520-talkback-emulator-no-pixel.json` | Blocked as expected | The guard no longer blocks on `manual_talkback`, but still blocks completion on rooted physical, physical handover, routed netem route proof, production relay execution, completion-audit partial rows, and merge-to-main remote workflow confirmation. |
| `test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260520-pr118-green-no-pixel.json` | Blocked as expected | The guard still blocks completion on rooted physical, TalkBack, physical handover, routed netem route proof, production relay execution, completion-audit partial rows, and merge-to-main remote workflow confirmation. |
| `test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260520-current-no-pixel.json` | Blocked as expected | The guard still blocks completion on rooted physical, TalkBack, physical handover, routed netem route proof, production relay execution, completion-audit partial rows, and merge-to-main remote workflow confirmation. |
| `test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260520-pixel7-after-packet-owned.json` | Blocked as expected | The guard blocks completion on audit partial rows, non-rooted Pixel 7, inactive Pixel 7 TalkBack, manual physical handover, manual routed-netem route proof, manual provider-backed relay execution, and remote workflow confirmation because local `main` is ahead of `origin/main`. |
| `test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/feature-gap-readiness-talkback-active.json` | Blocked as expected | The guard no longer blocks on `manual_talkback` for the active Pixel 7 readiness artifact, but still blocks on the audit Partial rows plus rooted physical, handover, routed netem, relay provider, and remote workflow confirmation. |
| `docker exec ripdpi-linux-netem ... tc qdisc replace dev eth0 root netem loss 1%` | Passed as container capability check | The Linux container has `tc`, `nft`, `CAP_NET_ADMIN`, and `CAP_NET_RAW`; loss applied and cleared on `eth0` in `test-lab/artifacts/netem-container-capability-20260519-current.txt`. This does not prove routed Pixel traffic through netem. |
| `docker exec ripdpi-linux-netem sh -lc 'id && command -v tc && command -v nft && tc qdisc show dev eth0 && nft list ruleset >/tmp/ripdpi-nft-ruleset.txt && wc -c /tmp/ripdpi-nft-ruleset.txt'` | Passed as container capability check | The Linux netem container is running as root with `tc`, `nft`, a visible `eth0` qdisc, and an 869-byte nft ruleset. This still does not prove routed Pixel traffic through netem. |
| `ANDROID_SERIAL=38080DLJG000GX ./test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260519-netem-aware.json` | Blocked | Guard correctly blocks release sign-off while rooted physical, TalkBack, cellular handover with IPv4 and IPv6 coverage, routed netem path proof, relay provider, and remote workflow evidence are incomplete. |
| `./test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260519-pr118-no-device.json` | Blocked as expected | Current no-device readiness blocks Android device, rooted physical, TalkBack, physical handover, routed netem, relay provider, and remote workflow rows, so the active goal cannot be marked complete. |
| `./test-lab/scripts/check-feature-test-signoff.sh --readiness test-lab/artifacts/feature-gap-readiness-20260520-rooted-emulator-no-pixel.json` | Blocked as expected | Current rooted-emulator/no-Pixel readiness blocks rooted physical, TalkBack, physical handover, and remote workflow confirmation, and keeps routed netem plus relay provider evidence manual; the active goal cannot be marked complete. |
| `gh workflow run ci.yml --ref main` | Not run locally | Remote workflow evidence still required after review branch publication. |
| `gh workflow run local-network-lab.yml --ref main -f run_vpn_emulator_lane=false` | Not run locally | Remote local-network-lab workflow evidence still required after review branch publication. |
| `gh workflow run offline-analytics.yml --ref main -f private_corpus_path=''` | Not run locally | Remote offline analytics workflow evidence still required after review branch publication. |
| `gh workflow run mutation-testing.yml --ref main -f packages='' -f in_diff=false` | Not run remotely; local `act` substitute passed | The scoped local Linux `act` mutation workflow covers the mutation-testing lane for the changed privileged packet paths; collect a GitHub-hosted rerun only if release policy requires hosted confirmation beyond local `act` evidence. |
| `gh workflow run fuzz-nightly.yml --ref main -f fuzz_seconds=1800` | Not run remotely; short local substitute passed | The 30s local fuzz loop passed for all four configured fuzz targets, but full-duration Fuzz Nightly workflow evidence remains required where release policy requires the scheduled/manual remote run. |
| CodeQL push workflow | Partial remote evidence | PR #118 CodeQL is green and the post-PR #117 `main` CodeQL run is green; CodeQL is push-triggered, so fresh `main` evidence is still required after PR #118 merges. |
| Checklist section coverage audit | Passed | 22 checklist sections, 248 checklist items, 22 evidence rows mapped below. |

## Checklist Section Coverage

| Checklist section | Status | Evidence | Remaining work |
| --- | --- | --- | --- |
| How to Use | Covered locally | Evidence rows name artifacts, commands, commits, and blockers for the local pass. | None for local evidence recording; release-only rows stay open below. |
| Test Dimensions | Partial | Current pass covers debug physical arm64 from the earlier non-rooted Pixel and the current Pixel 7, current-head Pixel 7 instrumentation E2E, rooted emulator raw capture, rooted-emulator on-device netem diagnostics, active TalkBack on the emulator, local fixture DNS modes, proxy/VPN runtime modes, adversarial middlebox dry-run, relay-matrix schema validation, netem container capability, and persisted settings flows. | Rooted physical, physical TalkBack, cellular handover, IPv4-only, IPv6-only, physical routed netem traffic path, production relay, and remote workflow dimensions remain. |
| Core Smoke Matrix | Partial | App unit lane, Maestro proxy/VPN E2E, Appium smoke, current-head physical Pixel 7 instrumentation E2E, and full physical Android packet smoke passed. | Rooted physical, cellular handover, IPv4/IPv6, production relay, routed netem, and remote workflow evidence remain. |
| App Shell, Navigation, and Settings | Partial | Appium cold launch, tab navigation, settings navigation, config validation, physical E2E navigation, emulator TalkBack hierarchy coverage, and emulator Persian RTL large-font Home/Settings/Diagnostics/Advanced Settings captures passed. | Physical TalkBack app-window evidence and remote UI workflow evidence remain. |
| Proxy Service | Covered locally | Maestro proxy E2E, Appium connected proxy state, local proxy cleanup, and proxy packet-smoke families passed. | None for the local non-rooted Pixel proxy slice; production relay provider coverage is tracked under relay rows. |
| VPN Service | Partial | Maestro VPN E2E, Appium connected VPN state, current-head Pixel 7 instrumentation E2E, and physical VPN packet-smoke rows passed. | Cellular handover, IPv4-only, IPv6-only, private-DNS, limited-path, rooted physical, routed netem, and production relay evidence remain. |
| DNS and Resolver Resilience | Covered locally | Physical packet smoke covered DoH, DoT, DNSCrypt, DoQ, success and fault rows, plus fixture-root trust and resolver-endpoint telemetry; the Pixel 7 host fault matrix now proves DNS UDP loss recovers through DNS-over-TCP fallback while preserving HTTP, HTTPS, TCP, and relay readiness. | None for local encrypted-DNS smoke; physical network variation remains under network matrix rows. |
| Packet Strategy Features | Partial | Android packet smoke covered proxy strategy families and VPN tunnel behavior on the earlier Pixel 8 Pro and current Pixel 7, the rooted-emulator pass produced baseline raw on-device pcaps, fake-segment rows are validated by raw packet pattern on rooted targets instead of JUnit skips, the rooted raw extension verified SeqOverlap and FakeRst by pcap pattern, `337b68cc` verified MultiDisorder and TCP IpFrag2 by pcap pattern, and the adversarial middlebox dry-run produced 63 pcap cells. | Rooted physical, routed netem, IPv4/IPv6, generator, and real-provider release rows remain. |
| Relay and Tunneling Paths | Partial | Mock/local relay readiness passed in device-profile E2E, ws tunnel fallback passed in physical packet smoke, and the local mock-relay matrix now covers ready, invalid-credential, malformed-response, server-reset, and timeout diagnostics on both rooted emulator and non-root Pixel 7 without breaking DNS, HTTP, HTTPS, TCP, or UDP probes. | Operator-provided production relay matrix and provider-backed proxy/VPN/diagnostics/restart/fault/handover rows remain. |
| Diagnostics Workflows | Partial | Appium diagnostics actions, scan results, strategy audit report, current-head Pixel 7 instrumentation E2E, Pixel 7 same-LAN diagnostics, rooted-emulator on-device netem diagnostics, emulator TalkBack diagnostics-screen hierarchy evidence, and adversarial middlebox dry-run passed. | Physical routed netem diagnostics, cellular handover diagnostics, production relay diagnostics, and remote workflow evidence remain. |
| Autolearn and Remembered Networks | Covered locally | Physical packet smoke passed host autolearn and remembered-policy rows using fixture DNS and app-process traffic probes. | None for local non-rooted Pixel autolearn/remembered-policy smoke. |
| Browser and HTTP Stack | Covered locally | Appium browser/HTTP-stack flows and support-bundle actions passed in the local smoke set. | None for local UI/browser smoke; remote workflow evidence remains under CI rows. |
| Root Helper and Privileged Operations | Partial | Root-helper manager fallback and shutdown unit coverage passed, the opt-in rooted instrumentation smoke passed on the app-granting Magisk `RIPDPI_Root_API34` AVD with no helper leak after cleanup, rooted-emulator packet smoke produced raw `tcpdump` pcaps for the baseline scenarios, SeqOverlap and FakeRst have rooted packet-action pcaps, and MultiDisorder plus TCP IpFrag2 now have rooted packet-owned TCP pcaps with explicit validators. Readiness correctly reports the physical Pixel as non-rooted. | Rooted physical device evidence remains unless the app-granting rooted-emulator substitution is explicitly accepted for the release row. |
| Logging, History, Export, and Privacy | Covered locally | Appium support bundle and diagnostics share/save actions passed, and archive/redaction unit coverage was included in the green unit lane. | None for local UI/export smoke; release remote workflows remain under CI rows. |
| UI, Compose, Localization, and Accessibility | Partial | Appium UI flows, active emulator TalkBack hierarchy evidence, emulator Persian RTL large-font captures, physical Pixel 7 TalkBack active-service readiness, locale/lint-backed resource coverage, and existing Compose tests passed in local gates. | Physical-device TalkBack app-window capture remains blocked until the Pixel is unlocked and TalkBack is manually enabled; remote UI workflow evidence remains where release policy requires it. |
| Test-Lab and Automation Tooling | Partial | Readiness, signoff guard, artifact path checks, Appium, Maestro, physical packet smoke, rooted-emulator packet smoke with raw capture, rooted-emulator on-device netem diagnostics, the repeatable rooted-emulator netem runner, active emulator TalkBack capture, skipped packet-smoke row rejection, stale physical probe host detection, Pixel 7 same-LAN direct diagnostics, relay example validation, mock-relay fault-matrix automation on emulator and Pixel 7, repeatable host DNS/UDP fault-matrix automation on Pixel 7, repeatable TCP-family netem forwarder automation on Pixel 7, netem container capability, local-network-lab through act, local workflow evidence validation, adversarial middlebox dry-run, and Docker-backed TSPU live smoke all executed locally. | Cellular live-path coverage, routed netem VM/router carrying Pixel traffic, and remote local-network-lab workflow evidence remain where release policy still requires remote confirmation. |
| CI, Release, and Supply Chain | Partial | Local hooks, unit tests, script tests, native checks, evidence guardrails, the tested PR #118 head `0c745ec9` green PR rollup, local `act`/native CI mirrors through `d34ea32f`, the current `act` local-network-lab rerun, the local offline analytics unit/sample-corpus workflow substitute, the 30s local Fuzz Nightly-style substitute, and the scoped local Linux `act` mutation-testing workflow are green for build, native-bloat, release-verification, coverage, rust-cross-check, rust-lint, the native Rust batch, CLI packet smoke, local-network-lab, offline analytics, short fuzz, mutation testing, the rooted raw strategy extension, the root-helper capability/preflight fix, and the packet-owned TCP fix. | Full-duration Fuzz Nightly and final remote release workflow evidence, including hosted mutation-testing confirmation if release policy requires it beyond local `act`, remain where remote confirmation is required. |
| Runtime Mode by DNS by Relay | Partial | Proxy/VPN local modes and encrypted-DNS resolver combinations were exercised without production relay. | Production relay matrix across runtime modes and DNS fallback rows remains. |
| Runtime Mode by Packet Strategy | Partial | Proxy packet strategy families and VPN tunnel packet-smoke rows passed on the non-rooted Pixel. | Root-only strategies, IPv4/IPv6 variants, and routed adversarial-network rows remain. |
| Relay by Runtime Mode | Partial | Mock/local relay readiness, local mock-relay invalid-credential/reset/timeout/malformed-response diagnostics on emulator and Pixel 7, and ws tunnel fallback passed in local device-profile and packet-smoke rows. | Provider-backed relay rows for proxy, VPN, diagnostics, restart, invalid credentials, reset, timeout, malformed response, DNS fallback, and handover remain. |
| Network Fault Matrix | Partial | Adversarial middlebox dry-run, Docker-backed TSPU live smoke, netem container capability, repeatable Pixel 7 TCP-family netem-forwarder proof, rooted-emulator on-device netem diagnostics, the repeatable rooted-emulator netem runner, and the Pixel 7 host DNS/UDP fault matrix passed. | Physical routed netem Pixel path carrying DNS/UDP, cellular handover, IPv4-only, IPv6-only, private DNS, limited path, and QUIC-drop live-path evidence remain. |
| Feature Definition of Done | Partial | Local artifacts, issue-fix commits, readiness, and signoff guard evidence are recorded. | Required readiness rows and remote workflow evidence must be ready before release sign-off. |

## Issues Found and Fixed

| Commit | Finding | Verification |
| --- | --- | --- |
| `8325bf97` | Maestro E2E was blocked by Android notification permission on fresh debug installs. | Proxy E2E passed on Pixel. |
| `e3366777` | VPN-local sessions started from the remote card could not be stopped from the visible local-mode active card. | Focused unit test and VPN E2E disconnect passed with no foreground service leak. |
| `8b9872da` | Restoring the VPN-local stop policy regressed proxy local-mode stop behavior. | Full app unit lane passed. |
| `293613a3` | Privacy/disclosure Compose tests used stale or overly broad text selectors. | Focused and full app unit lanes passed. |
| `fb9d8b00` | The adversarial dry-run script wrote relative artifact outputs under a different working directory and then checked the wrong path. | Relative artifact dry-run passed and produced `verdict-report.json`. |
| `31ce8e33` | Appium strategy-winner test asserted a child action before scrolling it into the viewport. | Focused test, strategy-audit file, and full Appium smoke passed. |
| `3d4a94ff` | Encrypted-DNS resolver sockets inside VPN mode were not protected before opening upstream connections, and fault telemetry lost the resolver endpoint label. | `ripdpi-tunnel-core` DNS-intercept tests, native architecture health, and physical DoT fault packet smoke passed. |
| `fbbd5a36` | Physical non-rooted packet smoke could not reliably exercise VPN rows because instrumentation-process probes bypassed the app VPN and stale androidTest classes could be installed. | Full physical Pixel packet smoke passed all 17 rows in `test-lab/artifacts/android-packet-smoke-pixel8pro-full-after-autolearn-fixture-20260519`. |
| `c1ce7a8e` | Feature-gap readiness falsely reported no Android device when the Pixel and rooted emulator were both attached. | `test-lab/scripts/test-feature-gap-readiness.sh` passed and clean-tree readiness marked `android_device` ready without `ANDROID_SERIAL`. |
| `1f3a5623` | Root-helper launch stopped after the first `su` launcher threw, and the new rooted smoke needed to avoid touching shell-only AOSP emulator `su` unless app-granting root is explicitly requested. | Forced `RootHelperManagerTest` passed, and `RootHelperInstrumentedTest` passed with a deliberate skip on the clean shell-rooted API 34 AVD. |
| `90c1946c` | macOS Docker Desktop returned UDP/DNS replies from an unusable source path for physical Wi-Fi device-profile probes. | Host UDP echo and host DNS helpers passed locally; Pixel proxy/VPN/diagnostics probes now show UDP and DNS success. |
| `47ea6211` | VPN E2E required the fixed debug proxy listener even though VPN mode may use an ephemeral authenticated internal SOCKS hop. | Focused debug-probe unit tests passed, and Pixel VPN/proxy E2E passed with explicit mode-specific readiness preconditions. |
| `4488a7a6` | Root-helper instrumentation hid the exact app-context `su` failure behind a generic skip, making rooted-emulator readiness ambiguous. | `:app:ktlintCheck`, `git diff --check`, and a fresh no-build-cache `RootHelperInstrumentedTest` run passed; the rooted AVD artifact now reports `error=13, Permission denied` for app-context `su` attempts. |
| `ded3ac55` | Feature-gap readiness reported a generic Darwin routed-netem blocker even when the Docker netem container was running with `tc`, hiding the actual missing evidence. | `test-feature-gap-readiness.sh`, `test-feature-test-signoff.sh`, `bash -n`, and current-head readiness passed; `routed_netem_vm` now reports manual route-proof work with the running `ripdpi-linux-netem` container. |
| `af4891d5` | The root helper bound its socket as `root:root` with an Android SELinux label that the app could not use, then leaked detached helper children when Magisk separated the root process from the `su` launcher. | `RootHelperManagerTest`, `cargo test -p ripdpi-root-helper`, `cargo clippy -p ripdpi-root-helper --all-targets -- -D warnings`, `:core:service:ktlintCheck`, `:core:service:detekt`, architecture health, and the opt-in `RootHelperInstrumentedTest` on the Magisk-rooted AVD passed; post-run process inspection found no `ripdpi-root-helper` process or socket file. |
| `e6a57057` | The evidence ledger lagged the current PR head after the Android network E2E, mutation harness, diagnostics gating, and TSPU dry-run stabilization commits landed. | Current continuation checks verified no physical adb/USB device was available, lab doctor passed 10/0/1, rooted-emulator Android packet smoke passed in indirect mode, TSPU dry-run passed, relay example schema validation passed, and PR #117 checks were green at `e6a57057`. |
| `e6c6fefd` | The Android emulator helper resolved `/opt/homebrew` as the SDK root when `adb` came from Homebrew, so local emulator provisioning could not find the actual macOS SDK emulator or avdmanager binaries. | `scripts/ci/test-android-emulator-helpers.sh`, live resolver checks for `/Users/po4yka/Library/Android/sdk`, and the rooted-emulator Android packet-smoke raw-capture run passed. |
| `77ffbd27` | Local lab scripts trusted the first `python3` on PATH, but the macOS framework shim exited successfully without running scripts, which could hide fixture failures or keep host DNS/UDP helpers from starting. | The Local Network Lab feature sign-off guard block, feature gap readiness preflight block, host DNS helper test, host UDP echo helper test, lab doctor, archive redaction, and `bash -n` passed with the checked-in resolver. |
| `93f2109b` | Remote workflow evidence named CI and lab lanes but did not explicitly name CodeQL, while the guard and checklist require CodeQL as a push-triggered remote lane. | `test-lab/scripts/test-feature-test-signoff.sh` and `test-lab/scripts/test-feature-checklist-coverage.sh` passed after the evidence update. |
| `d5f177ca` | Remote workflow evidence still described PR #117 as the active review branch after PR #117 merged and PR #118 became the current QA branch. | `test-lab/scripts/test-feature-test-signoff.sh`, `test-lab/scripts/test-feature-checklist-coverage.sh`, and `git diff --check` passed; PR #118 CI, CodeQL, Local Network Lab, and rerun release-verification are green at `d5f177ca`. |
| `dda5f7a8` | The TSPU dry-run script still used ambient `python3`, so the macOS framework shim could exit successfully without creating `verdict-report.json`. | `RIPDPI_TSPU_ARTIFACT_DIR=test-lab/artifacts/tspu-dryrun-20260520-current bash scripts/ci/run-tspu-dryrun.sh` passed with 53 unit tests and 63 dry-run cells. |
| `69d1a7fe` | The TSPU live smoke runner assumed a sudo-backed host and could not run directly as root inside the privileged Docker/VM environment used for local release testing. | `bash -n scripts/ci/run-tspu-live-smoke.sh`, direct macOS skip, and Docker-backed `scripts/ci/run-tspu-live-smoke.sh` passed; the post-commit artifact `test-lab/artifacts/tspu-live-smoke-20260521-032212-69d1a7fe/verdict-report.json` recorded 9 live cells with 6 blocked, 3 bypassed, 0 degraded, and 0 inconclusive. |
| `e77b3a36` | The local workflow wrapper did not expose the TSPU dry-run or live workflow lanes, hosted artifact uploads failed under `act`, the lean runner hit Python externally-managed-environment behavior while installing NetfilterQueue, and fixed smoke ports collided with already-running local lab services under host networking. | `bash -n`, `python3 -m py_compile`, workflow YAML parsing, `scripts/ci/act-local.sh --list`, `git diff --check`, `scripts/ci/act-local.sh tspu-dryrun`, `scripts/ci/act-local.sh tspu-live`, and the post-commit Docker live smoke passed. |
| `70c70a5c` | The Android packet-smoke runner ignored scenario `requiredCapability`, so a non-root Pixel 7 `physical_indirect` run tried the raw-PCAP-only MultiDisorder proxy row and failed when the service halted without raw socket and TCP_REPAIR privileges. | `bash -n`, `scripts/ci/test-android-packet-smoke-lib.sh`, `:app:compileGithubDebugAndroidTestKotlin`, `:app:runKtlintCheckOverAndroidTestSourceSet --rerun-tasks`, `:app:ktlintCheck`, `git diff --check`, commit hooks, and the clean-head Pixel 7 packet-smoke artifact `test-lab/artifacts/android-packet-smoke-pixel7-current-20260521-041352-70c70a5c/` passed; raw-PCAP-only rows were skipped on `physical_indirect`. |
| `1618e19a` | Rooted-emulator packet smoke counted hostfake, fakedsplit, and fakeddisorder rows as successful when instrumentation reported JUnit skips, leaving fake-segment behavior unvalidated. | `bash -n`, `git diff --check`, targeted hostfake/fakedsplit/fakeddisorder reruns, and the full rooted-emulator packet-smoke matrix under `test-lab/artifacts/android-packet-smoke-rooted-emulator-20260520-fixed` passed; fake-segment rows were accepted only after raw packet-pattern validation. |
| `623accba` | CI release-verification assembled all release flavors with ABI split APK packaging enabled, which reproduced locally as a Java heap OOM in aggregate `:app:assembleRelease` packaging. | `.github/workflows/ci.yml` now disables ABI split packaging and serializes native ABI handling for release-verification; `git diff --check`, YAML parsing, and local aggregate `:app:assembleRelease -Pripdpi.skipNativeBuild=true -Pripdpi.enableAbiSplits=false -Pripdpi.nativeAbiParallelism=1 --stacktrace --no-build-cache --rerun-tasks --console=plain` passed. |
| `8de1b4d3` | The generic Android network E2E package lane picked up raw-packet-only hostfake, fakedsplit, and fakeddisorder smoke methods after skip rejection was tightened, so the non-rooted CI emulator failed against the host-loopback TLS fixture instead of leaving those rows to the raw packet-smoke runner. | `PacketSmokeInstrumentedTest` now tags those methods with `@RawPacketValidationOnly`, `run-android-e2e-emulator.sh` excludes that annotation from the package lane, and `bash -n`, `git diff --check`, `:app:ktlintCheck :app:assembleGithubDebugAndroidTest`, and `bash scripts/ci/test-android-packet-smoke-lib.sh` passed. |
| `0c8f2189` | `main` carried a stale Java `TestNetworkProbeReceiver` beside the Kotlin implementation, so the Android test APK failed to compile with a duplicate class after PR work moved the receiver to Kotlin. | `./gradlew :app:assembleGithubDebugAndroidTest -Pripdpi.skipNativeBuild=true --console=plain` failed before the deletion and passed after the stale Java copy was removed. |
| `fd7f164f` | Physical-device probes reused stale `MACBOOK_LAN_IP` from `test-lab/artifacts/lab-env.sh` after the Mac changed networks, sending Pixel 7 diagnostics to `192.168.1.9` even though the current host IP was `172.20.10.5`. | `bash -n test-lab/scripts/adb-run-probe.sh test-lab/scripts/test-feature-custom-probe-broadcast.sh`, `bash test-lab/scripts/test-feature-custom-probe-broadcast.sh`, `git diff --check`, and a Pixel 7 diagnostics rerun passed the wrapper behavior by warning and switching to the current host IP before reporting the real topology block. |
| `a90f5c81` | The fuzz workspace lock was stale, so the local Rust fuzz-smoke lane could not be used as release evidence with `--locked`. | `CARGO_TARGET_DIR=/tmp/ripdpi-target-fuzz-smoke bash scripts/ci/run-rust-fuzz-smoke.sh` passed after refreshing `native/rust/fuzz/Cargo.lock`. |
| `2c7f0a0e` | Autolearn host-store state could remain unflushed when the proxy accept loop shut down, risking lost learned-host persistence after restart cycles. | `CARGO_TARGET_DIR=/tmp/ripdpi-target-proxy-shutdown cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-proxy-runtime --test supervisor_restart proxy_restart_cycles_all_exit_cleanly -- --exact` passed. |
| `f3e6007b` | The local `act` CLI packet-smoke job lacked package prerequisites, packet-capture container capabilities, and `act`-safe artifact upload handling. | `PATH="/Applications/Docker.app/Contents/Resources/bin:$PATH" scripts/ci/act-local.sh cli-packet-smoke` passed. |
| `ce364049` | Current native clippy failed on stale idioms and warnings across the Rust workspace. | `scripts/ci/act-local.sh rust-lint` passed after the lint cleanups. |
| `b50b3190` | The local workflow runner could mask native-job failures and the Android cross-check lane depended on an ambient `ANDROID_HOME`, making local CI evidence unreliable on macOS. | `scripts/ci/act-local.sh rust-cross-check`, `scripts/ci/act-local.sh build`, `scripts/ci/act-local.sh native-bloat`, `scripts/ci/act-local.sh release-verification`, and `scripts/ci/act-local.sh coverage` passed. |
| `0747b94f` | Local release-verification exposed future Kotlin Hilt qualifier target warnings in `ClipboardReader` and `WidgetUpdater`. | `./gradlew :app:compileGithubDebugKotlin :app:compileGithubReleaseKotlin -Pripdpi.skipNativeBuild=true --console=plain` passed without the warnings. |
| `7966e673` | The local `act` wrapper could not run the Local Network Lab workflow, and the workflow assumed hosted-runner details that were absent under the lean act image. | `scripts/ci/act-local.sh local-network-lab` passed in 61s after adding the workflow/job mapping, creating `RUNNER_TEMP`, provisioning Ruby only when absent, and skipping artifact upload under `ACT=true`. |
| `1bc1ac12` | Proxy raw-packet preflight probed before the service registered the root helper, and the helper capability surface treated a shallow `TCP_REPAIR` toggle as sufficient even when replacement-socket handoff failed later. | `cargo test -p ripdpi-proxy-runtime-adapter raw_packet_requirements`, `cargo test -p ripdpi-privileged-ops`, Android `:app:compileGithubDebugAndroidTestKotlin -Pripdpi.localNativeAbis=arm64-v8a`, and the commit hook's architecture/native-contract/rustfmt/workspace-clippy gates passed. |
| `7e98c387` | Rooted raw packet smoke did not have dedicated PCAP assertions for sequence overlap and fake RST strategy families, and exploratory MultiDisorder/TCP IpFrag2 rows would have been false positives in this emulator topology. | `bash -n scripts/ci/run-android-packet-smoke.sh`, `jq empty scripts/ci/packet-smoke-scenarios.json`, Android `:app:compileGithubDebugAndroidTestKotlin -Pripdpi.localNativeAbis=arm64-v8a`, and rooted-emulator `android_proxy_seqovl_family` plus `android_proxy_fakerst_family` raw packet-smoke reruns passed. |
| `337b68cc` | TCP IpFrag2 and MultiDisorder root-helper strategies used replacement-socket handoff on Android, which failed in the rooted emulator topology with `EADDRNOTAVAIL` and prevented packet-owned TCP strategies from completing the TLS fixture flow. | `bash -n scripts/ci/run-android-packet-smoke.sh`, `jq empty scripts/ci/packet-smoke-scenarios.json`, `git diff --check`, `cargo check -p ripdpi-privileged-ops`, `cargo test -p ripdpi-privileged-ops`, Android `:app:compileGithubDebugAndroidTestKotlin -Pripdpi.localNativeAbis=arm64-v8a`, commit-hook architecture/native-contract/rustfmt/workspace-clippy gates, and rooted-emulator `android_proxy_multidisorder_family` plus `android_proxy_ipfrag2_family` raw packet-smoke reruns passed. |
| `1c1a8fbc` | Feature-gap readiness treated a visible Android cellular transport as enough for manual handover even when the physical Pixel reported data service `OUT_OF_SERVICE`. | `bash -n test-lab/scripts/check-feature-gap-readiness.sh test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, `test-lab/scripts/test-feature-checklist-coverage.sh`, `git diff --check`, and clean-tree readiness under `test-lab/artifacts/feature-gap-readiness-20260520-after-cellular-state-fix.json` passed; signoff now blocks on the explicit out-of-service cellular message. |
| `ec1d9504` | `cargo-mutants` v27 treats `--in-diff` as a diff-file path, so the local wrapper's command-string form could silently fail instead of running the changed-line mutation set. | `bash -n scripts/ci/run-rust-mutants.sh scripts/tests/test_run_rust_mutants.sh`, `bash scripts/tests/test_run_rust_mutants.sh`, `MUTANTS_PACKAGES=ripdpi-privileged-ops MUTANTS_JOBS=2 MUTANTS_OUTPUT_DIR=target/mutants-output-20260520-main-local bash scripts/ci/run-rust-mutants.sh --in-diff "git diff origin/main...HEAD" --list`, and `git diff --check` passed. |
| `df4d87d1` | The mutation-testing workflow could not run under the lean local `act` image because CMake/libclang prerequisites were missing and artifact upload tried to use unavailable GitHub runtime tokens. | YAML parsing and the scoped local `act` mutation workflow passed setup and the unmutated Linux baseline after adding ACT-only prerequisites and skipping artifact upload under `ACT=true`. |
| `d34ea32f` | Linux-only privileged packet paths still allowed body-replacement mutants to survive because tests did not force the non-empty raw/protect work path for TCP IpFrag2, MultiDisorder, and TCP_REPAIR probing. | `cargo fmt --manifest-path native/rust/Cargo.toml --package ripdpi-privileged-ops`, `cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-privileged-ops`, `git diff --check`, commit-hook architecture/native-contract/rustfmt/workspace-clippy gates, and the scoped local Linux `act` mutation workflow passed with `3 mutants tested in 4m: 3 caught`. |
| `0eb6a345` | Feature-gap readiness could only block on missing GitHub workflow confirmation and had no strict way to consume the local `act`/VM/emulator workflow evidence requested for faster local release checks. | `bash -n`, `test-lab/scripts/test-local-workflow-evidence.sh`, `test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, `test-lab/scripts/test-feature-checklist-coverage.sh`, `git diff --check`, and commit-hook no-baseline/secret/large-file gates passed. |
| `efb9b858` | The fuzz smoke lane reused the checked-in fuzz corpus/artifact paths, so local release evidence could mutate `native/rust/fuzz/corpus` or race with Docker/`act` checkouts. | `bash -n scripts/ci/run-rust-fuzz-smoke.sh`, `RIPDPI_FUZZ_SECONDS=1 bash scripts/ci/run-rust-fuzz-smoke.sh`, `git diff --check`, and clean `git status` after the run passed. |
| `7826dc67` | Root-helper probe registration used a boolean ownership flag across an unsafe boundary, which the unsafe-boundary scanner rejected. | `cargo fmt --all --check`, `python3 scripts/ci/check_unsafe_boundaries.py`, `cargo test -p ripdpi-proxy-runtime-adapter raw_packet_requirements`, and `bash scripts/ci/run-rust-lint.sh` passed. |
| `94263f43` and `7d7535e0` | The diagnostics duplicate-start test could race with a fast loopback scan and pass or fail for the wrong reason instead of proving a scan was already active. | The test now uses a synchronized slow local HTTP server; focused `duplicate_start_throws_illegal_state`, `cargo fmt --all --check`, `git diff --check`, workspace hooks, and focused diagnostics mutation reruns passed with no missed mutants. |
| `cca4845a` and `cec63264` | Android cross-checks inherited host Darwin compiler/linker flags and stale BoringSSL CMake cache state, so local `act`/native CI could fail before reaching Android ABI validation. | `bash -n scripts/ci/android-emulator-helpers.sh scripts/ci/run-rust-cross-check.sh scripts/ci/run-rust-native-checks.sh`, `python3 -m py_compile scripts/ci/verify_native_bloat.py`, `git diff --check`, `scripts/ci/run-rust-cross-check.sh`, and the current `scripts/ci/act-local.sh rust-workspace-tests rust-cross-check` pass verified all four Android targets. |
| `cc77ba1d` | Feature-gap readiness reported TalkBack inactive but did not surface secure-keyguard state, and a locked target with TalkBack active could be misread as ready even though physical app-window and UI-driven TalkBack verification cannot run behind keyguard. | `bash -n test-lab/scripts/check-feature-gap-readiness.sh test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-gap-readiness.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, live Pixel 7 readiness and expected-blocking signoff under `/tmp/ripdpi-feature-gap-readiness-cc77ba1d.json` and `/tmp/ripdpi-feature-signoff-cc77ba1d.log`, `git diff --check`, commit hooks, and `scripts/ci/act-local.sh local-network-lab` passed. |
| `89472ae0` | The feature signoff guard parsed under Homebrew Bash but failed from macOS system Bash 3.2 wrappers because quoted Python heredocs were embedded inside process substitutions, which made local wrapper evidence fragile. | `/bin/bash -n test-lab/scripts/check-feature-test-signoff.sh`, `test-lab/scripts/test-feature-test-signoff.sh`, direct `/bin/bash` expected-blocking signoff, `bash -lc` expected-blocking signoff, `test-lab/scripts/check-local-workflow-evidence.sh --evidence /tmp/ripdpi-local-workflow-evidence-89472ae0.json --head 89472ae084b8f8a84d54841e6f4405c841b857be`, and `git diff --check` passed. |
| `f7e85f07` | Physical same-LAN fault probes had no deterministic host-side DNS/UDP drop controls, so local DNS fallback and UDP-loss diagnostics could not be forced without depending on external network conditions. | `test-lab/scripts/test-host-fault-controls.sh`, `test-lab/scripts/test-host-udp-echo.sh`, `test-lab/scripts/test-host-dns.sh`, `bash -n`, `python3 -m py_compile`, and `git diff --check` passed. |
| `f8b3bade` | Host fault daemons changed working directory before resolving relative PID and log paths, so daemonized DNS/UDP helpers could lose their local artifact files. | `test-lab/scripts/test-host-fault-controls.sh`, `test-lab/scripts/test-host-udp-echo.sh`, `test-lab/scripts/test-host-dns.sh`, `bash -n`, `python3 -m py_compile`, `git diff --check`, and commit hooks passed. |
| `3437c467` | The host DNS TCP helper assumed `recv(2)` returned the complete DNS-over-TCP length prefix, so a physical Pixel split-prefix read closed the fallback connection and made combined DNS UDP-drop plus UDP-loss diagnostics fail for the wrong reason. | `test-lab/scripts/test-host-dns.sh`, `test-lab/scripts/test-host-fault-controls.sh`, `test-lab/scripts/test-host-udp-echo.sh`, `:app:compileGithubDebugKotlin`, focused `DebugLocalNetworkProbeTest`, `:app:ktlintCheck`, `:app:detekt`, `git diff --check`, `:app:installGithubDebug`, and `test-lab/artifacts/host-dns-udp-and-udp-loss-pixel7-20260521-073009-3437c467/probe-device-diagnostics.json` passed; the probe now records DNS `transport="tcp"` with UDP degraded by the controlled timeout. |

## Current Open Gaps

| Gap | Current evidence | Required next evidence |
| --- | --- | --- |
| Rooted physical behavior | Pixel 7 readiness says `rooted_physical_device` is blocked because `su 0 id` is unavailable; `RIPDPI_Root_API34` provides app-granting Magisk root, the current opt-in root-helper lifecycle smoke passes there with post-run process/socket cleanup proof, and the current full rooted-emulator raw packet-smoke artifact verifies hostfake, fakedsplit, fakeddisorder, FakeRst, TCP IpFrag2, SeqOverlap, and MultiDisorder packet patterns with raw pcaps. | Run the rooted physical device section from the manual evidence template on a device with app-granting root, or explicitly accept the rooted-emulator substitution for the release row. |
| TalkBack evidence freshness | Emulator readiness marks `manual_talkback` ready from the active-service artifact and UI hierarchy summaries under `test-lab/artifacts/talkback-emulator-20260520-current/`; Pixel 7 readiness under `test-lab/artifacts/talkback-pixel7-20260520-after-packet-owned/feature-gap-readiness-talkback-active.json` marks physical TalkBack ready while active and confirms settings were restored afterward. The current read-only Pixel blocker artifact under `test-lab/artifacts/talkback-pixel7-blocked-20260521-024320-7f02179c/`, the latest proxy E2E blocker under `test-lab/artifacts/proxy-e2e-pixel7-current-20260521-045450-1e13bbd2/`, and `/tmp/ripdpi-feature-gap-readiness-3437c467.json` show the Pixel remains behind secure keyguard with TalkBack inactive, so UI-driven Maestro and physical TalkBack app-window evidence cannot proceed until operator unlock and activation. | Unlock the Pixel 7 and rerun the physical TalkBack app-window pass plus UI-driven proxy/VPN E2E, or explicitly accept emulator UI hierarchy coverage plus physical active-service readiness for the release row. |
| Physical network matrix | Pixel 7 same-LAN device-profile diagnostics passed against host `192.168.1.13` with DNS, HTTP, HTTPS, TCP, UDP, and relay readiness green; Wi-Fi-before and Wi-Fi-after probes in the handover artifact also passed; the 2026-05-21 Toxiproxy matrix proves local diagnostics produce structured latency, timeout, and TLS-reset fault artifacts; the host-backed physical run under `test-lab/artifacts/physical-host-dns-udp-pixel7-20260521-025413-ace161b5/` reconfirmed DNS, HTTP, HTTPS, TCP, UDP, and relay readiness with `errors=[]`; the controlled host-fault probe under `test-lab/artifacts/host-dns-udp-and-udp-loss-pixel7-20260521-073009-3437c467/` proves DNS UDP loss falls back through TCP while UDP echo loss reports a structured degraded transport result; and the repeatable matrix under `test-lab/artifacts/host-fault-matrix-20260521-074805-b519a26a/` now covers baseline, DNS UDP drop, UDP echo drop, and combined DNS UDP plus UDP echo drop on Pixel 7. Cellular handover remains blocked because the phone reports voice/data `OUT_OF_SERVICE`, and adb-reverse packet-smoke still does not replace live cellular/routed traffic evidence. | Use a physical device/SIM with cellular data in service, then perform cellular handover, Wi-Fi return, IPv4-only, IPv6-only, private-DNS, and limited-path runs and attach probe JSON. |
| Routed netem | Readiness sees the running `ripdpi-linux-netem` container and marks `routed_netem_vm` manual. The 2026-05-21 netem-forwarder artifact and the repeatable `aa3208a7` runner prove Pixel HTTP, HTTPS, TCP echo, and relay diagnostics can traverse a Linux `tc` container under delay/loss, but Docker Desktop UDP publication kept DNS/UDP outside that container; the current Docker-backed repro under `test-lab/artifacts/docker-udp-dns-pixel7-20260521-025313-ace161b5/` confirms published UDP replies are misdirected after source translation. The current rooted-emulator artifact under `test-lab/artifacts/rooted-emulator-netem-20260521-064100-59309e78/` proves on-device `tc netem` can shape emulator DNS, HTTP, HTTPS, TCP, UDP, and relay diagnostics with `errors=[]` and cleanup, but it is not a physical Pixel routed-path substitute. | Place a Linux VM/router namespace in the Pixel traffic path, then run full routed netem VPN and diagnostics probes under packet loss and QUIC-drop scenarios with DNS, UDP, TCP, HTTPS, and relay all carried by the routed path. |
| Relay provider matrix | The example relay matrix validates through readiness and schema checks, current-head host relay interoperability passes, the rooted arm64 emulator relay smoke passes, and the controlled mock-relay matrix verifies ready, invalid-credential, malformed-response, server-reset, and timeout diagnostics on emulator and Pixel 7; no operator-owned provider-backed execution evidence has been collected. | Provide an operator-owned relay provider matrix and run proxy, VPN, diagnostics, restart, invalid credential, reset, timeout, malformed response, DNS fallback, and handover rows. |
| Remote workflow confirmation | `main` now includes the local fixes through `3437c467`, local `act`/native CI mirrors are green, CodeQL workflow wiring dry-run passed, current local-network-lab and offline analytics substitutes pass, the short Fuzz Nightly-style substitute passes, focused diagnostics mutation testing has no missed mutants, current relay interoperability and Android relay smoke pass, current CLI packet-smoke passed under `act`, `scripts/ci/act-local.sh tspu-dryrun` and `scripts/ci/act-local.sh tspu-live` pass, Docker-backed TSPU live smoke passes, the clean-head Pixel 7 packet-smoke capability-filter run passes, the full rooted-emulator raw packet-smoke rerun passes, current rooted-emulator proxy/VPN E2E passes, current rooted-emulator netem passes, mock-relay fault-matrix diagnostics pass on the rooted emulator and Pixel 7, current locked-TalkBack readiness passes the local-network-lab `act` lane, the current `/tmp/ripdpi-act-packet-tspu-8bc87c2d.log` rerun passes packet-smoke plus TSPU live under `act`, the current Pixel 7 combined DNS-UDP/UDP-echo fault probe passes, and `/tmp/ripdpi-local-workflow-evidence-3437c467.json` covers every required workflow lane for the latest validated QA evidence commit. Readiness marks `remote_workflow_confirmation` ready from explicit local evidence. | None for the requested local-instrument path; collect hosted workflow results only where release policy requires remote confirmation beyond local `act`/VM/emulator substitutes. |

## Next Concrete Runs

1. Run rooted physical device checks on a rooted phone, or explicitly accept the app-granting Magisk `RIPDPI_Root_API34` emulator substitution with the current SeqOverlap/FakeRst/MultiDisorder/TCP IpFrag2 packet-action artifacts.
2. Unlock the Pixel 7 and rerun the physical TalkBack app-window pass, or explicitly waive that app-window requirement and accept the emulator UI hierarchy plus Pixel 7 active-service readiness artifact; do not enable TalkBack from repository scripts.
3. Use a physical Pixel/SIM with cellular data in service, then run the physical network matrix with cellular handover, Wi-Fi return, IPv4-only, IPv6-only, private-DNS, and limited-path evidence.
4. Put the Pixel traffic path through the routed netem VM/router and run packet-loss plus QUIC-drop VPN and diagnostics probes; the rooted-emulator on-device netem row is now proven but does not close the physical routed-path row.
5. Provide the relay provider matrix and run all relay provider rows across proxy, VPN, diagnostics, restart, invalid credentials, reset, timeout, malformed response, DNS fallback, and handover.
6. Collect hosted main-branch workflow evidence for CI, CodeQL, local-network-lab, offline analytics, mutation-testing, Fuzz Nightly, and TSPU only where release policy still requires remote confirmation beyond the current accepted local workflow evidence JSON.
