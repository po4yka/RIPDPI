# Feature Test Evidence - 2026-05-14

This is the current evidence ledger for `docs/feature-test-checklist.md`. It
records what was verified in the local application test pass and what still
requires a dedicated device, network, relay, or remote CI environment.

This file is not a release sign-off. Rows marked `Gap` or `Partial` are still
open checklist work.

## Tested Build and Devices

| Item | Evidence |
| --- | --- |
| Git history | Evidence commits are recorded on the current branch; use `git log --oneline` for exact hashes |
| Physical device | Pixel 8 Pro, Android 16 / API 36, non-rooted |
| Emulator smoke | API 34 AVD fresh-start UI smoke; API 35 Persian large-font forced-RTL UI smoke |
| Release APK inspected | `app/build/outputs/apk/github/release/app-github-arm64-v8a-release.apk` |
| Lab artifacts | `test-lab/artifacts/vpn-e2e-20260514-013348/probe-device-vpn.json`, `test-lab/artifacts/private-dns-vpn-maestro-20260514-081801/probe-device-vpn.json`, `test-lab/artifacts/vpn-relay-ready-20260515-020054/probe-device-vpn.json`, `test-lab/artifacts/proxy-e2e-fixed-20260515-021920/probe-device-proxy.json`, `test-lab/artifacts/diagnostics-relay-ready-20260515-022702/probe-device-diagnostics.json`, `test-lab/artifacts/probe-device-diagnostics.json`, `test-lab/artifacts/proxy-smoke-20260514-030750/probe-device-proxy.json`, `test-lab/artifacts/api27-proxy-smoke-20260514-033259/probe-emulator-proxy.json`, `test-lab/artifacts/api31-proxy-smoke-20260514-033411/probe-emulator-proxy.json`, `test-lab/artifacts/api35-proxy-smoke-20260514-033526/probe-emulator-proxy.json`, `test-lab/artifacts/api35-fa-large-font-ui-20260514-034335/summary.txt`, `test-lab/artifacts/locale-home-de-20260514/home-summary.txt`, `test-lab/artifacts/locale-home-es-20260514/home-summary.txt`, `test-lab/artifacts/locale-home-zh_CN-20260514/home-summary.txt`, `test-lab/artifacts/locale-main-routes-20260514/de/summary.txt`, `test-lab/artifacts/locale-main-routes-20260514/es/summary.txt`, `test-lab/artifacts/locale-main-routes-20260514/zh_CN/summary.txt`, `test-lab/artifacts/config-accessibility-label-20260514/summary.txt`, `test-lab/artifacts/accessibility-label-audit-20260514/summary.txt`, `test-lab/artifacts/release-log-verbosity-20260514-040812/summary.txt`, `test-lab/artifacts/proxy-port-conflict-20260514-041518/summary.txt`, `test-lab/artifacts/proxy-process-kill-live-relaunch-fixed-20260514-042706/summary.txt`, `test-lab/artifacts/vpn-process-kill-20260514-054810/kill-summary.txt`, `test-lab/artifacts/reinstall-retention-after-summary.txt`, `test-lab/artifacts/owned-stack-browser-route-20260514-044413/summary.txt`, `test-lab/artifacts/owned-stack-browser-fetch-20260514-045421/summary.txt`, `test-lab/artifacts/owned-stack-browser-private-query-20260514-050327/summary.txt`, `test-lab/artifacts/local-network-lab-refresh-20260514-080841/summary.txt`, `test-lab/artifacts/test-lab-artifacts-20260513T221424Z.tar.gz`, `test-lab/artifacts/test-lab-artifacts-20260514T040845Z.tar.gz` |
| Nested-route locale artifacts | `test-lab/artifacts/nested-locale-routes-20260514/summary.txt`, `test-lab/artifacts/nested-locale-routes-fixed-20260514/summary.txt`, `test-lab/artifacts/nested-locale-dns-residual-fixed-20260514/summary.txt` |
| Migrated-install artifacts | `test-lab/artifacts/legacy-migration-v006-to-current-20260514-073437/summary.txt`, `test-lab/artifacts/legacy-migration-v006-to-current-fixed-20260514-074003/summary.txt` |
| Private DNS artifacts | Negative attempts: `test-lab/artifacts/private-dns-vpn-20260514-074905/`, `test-lab/artifacts/private-dns-vpn-adb-20260514-075236/`, `test-lab/artifacts/private-dns-vpn-normal-adb-20260514-075802/`, `test-lab/artifacts/private-dns-vpn-normal-remote-adb-20260514-075939/`, `test-lab/artifacts/private-dns-vpn-maestro-20260514-081011/`, `test-lab/artifacts/private-dns-vpn-maestro-20260514-081333/`, `test-lab/artifacts/private-dns-vpn-maestro-20260514-081559/`; passing run: `test-lab/artifacts/private-dns-vpn-maestro-20260514-081801/summary.json` |

## Command Evidence

| Checklist area | Command or artifact | Result |
| --- | --- | --- |
| Static analysis | `./gradlew staticAnalysis -Pripdpi.skipNativeBuild=true --no-daemon` | Pass on code HEAD `73e4fbc3` before subsequent docs-only evidence updates; 518 actionable tasks, `BUILD SUCCESSFUL in 2m 13s`, no failures |
| Current debug APK rebuild | `./gradlew :app:assembleGithubDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon` | Pass on local HEAD `89deca61`; `BUILD SUCCESSFUL in 4m 8s`. This rebuilt the current `app-github-universal-debug.apk` with real `arm64-v8a` native libraries before the fresh Maestro and Appium reruns. |
| Merged-origin architecture and release-gate checks | `gh run view 25875963396 --json jobs,headSha,conclusion,status,workflowName,url`; `python3 scripts/ci/check_architecture_health.py --check`; `python3 scripts/ci/check_native_hotspot_budgets.py`; `./gradlew :core:diagnostics-data:ktlintMainSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :core:diagnostics-data:ktlintDebugSourceSetCheck :core:diagnostics-data:testDebugUnitTest -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew staticAnalysis -Pripdpi.skipNativeBuild=true --no-daemon`; `python3 scripts/ci/check_fleet_fixtures.py`; `python3 scripts/ci/check_fleet_release_gates.py`; `python3 scripts/ci/check_dns_ipv6_killswitch_gates.py` | Pass locally after the current remote-gate refresh. The latest remote CI run still covers only `origin/main` at `342a169a` and failed `architecture-health`, `verify-roborazzi`, `rust-workspace-tests`, and `gradle-static-analysis`, so it is not sign-off evidence for local HEAD. The local refresh reproduced the architecture-health failure as a new `DetectionResolverNetworkStack.kt` `readUnsignedShort()` long-function indicator, then this commit converts that helper to an explicit block body. Architecture health now returns 31 current indicators, 31 baseline indicators, 0 new indicators, and 0 worsened entries. Native hotspot budgets checked 183 files with 0 over budget. Focused diagnostics-data ktlint/test checks and full `staticAnalysis` passed. Fleet fixture, fleet release cadence, and DNS/IPv6/kill-switch gate scripts passed in the prior local release-gate slice. |
| Current local CI-equivalent Roborazzi and Rust workspace refresh | `./gradlew :app:verifyRoborazziGithubDebug -Pripdpi.skipNativeBuild=true -Pripdpi.includeRoborazziUnitTests=true --tests 'com.poyka.ripdpi.ui.screenshot.*' --no-daemon`; `bash scripts/ci/run-rust-workspace-tests.sh` | Pass on local HEAD `5e34139c`. Roborazzi completed with `BUILD SUCCESSFUL in 4m 31s`. The Rust workspace script passed membership, hotspot-budget, architecture-checker, contract, fixture, tunnel, Android adapter, and full workspace nextest phases; the main nextest summary was 2840 tests run, 2840 passed, 16 skipped, and the ignored startup-latency smoke added 1 passed test. This clears the stale remote `verify-roborazzi` and `rust-workspace-tests` failure classes locally, but no remote workflow covers the local commits yet. |
| Diagnostics-data focused regression | `./gradlew :core:diagnostics-data:ktlintDebugSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :core:diagnostics-data:testDebugUnitTest -Pripdpi.skipNativeBuild=true --no-daemon` | Pass on merged local HEAD `8807b2c9`; ktlint task was skipped because there are no debug-source files, and `testDebugUnitTest` completed with `BUILD SUCCESSFUL in 30s` |
| Release verification build | `./gradlew :app:assembleGithubRelease -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon` | Pass |
| Build logic checks | `./gradlew :build-logic:convention:check --no-daemon` | Pass |
| Physical connected tests | `app/build/outputs/androidTest-results/connected/debug/TEST-Pixel 8 Pro - 16-_app-.xml` | 86 tests, 0 failures, 0 errors, 21 environment skips; includes `ServiceLifecycleIntegrationTest.proxyServiceUsesResolvedCommandLinePreferences` and related proxy/VPN lifecycle tests |
| Root detector tests | `core/service/build/test-results/testDebugUnitTest/TEST-com.poyka.ripdpi.services.RootDetectorTest.xml` | 4 tests, 0 failures |
| Root helper manager tests | `core/service/build/test-results/testDebugUnitTest/TEST-com.poyka.ripdpi.services.RootHelperManagerTest.xml` | 4 tests, 0 failures |
| Test-lab doctor | `test-lab/artifacts/doctor.json` | 11 pass, 0 fail, 0 skip. Latest local Docker lab run started the emulator profile, ran doctor, archived artifacts, passed archive redaction, and stopped cleanly. |
| Evidence artifact path audit | `test-lab/scripts/test-feature-artifact-paths.sh` | Pass: 52 distinct referenced artifact paths were checked and 0 were missing. |
| Checklist section coverage audit | `test-lab/scripts/test-feature-checklist-coverage.sh`; `test-lab/scripts/test-feature-checklist-coverage-fixtures.sh` | Pass: 22 checklist sections, 248 checklist items, 22 evidence rows, and 0 missing section mappings. The fixture self-test also confirms the verifier rejects stale checklist item baselines plus missing, duplicate, extra, and internally inconsistent evidence rows. |
| Feature-gap readiness preflight | `test-lab/scripts/check-feature-gap-readiness.sh`; `test-lab/scripts/test-feature-gap-readiness.sh`; `test-lab/artifacts/feature-gap-readiness.json`; `test-lab/artifacts/feature-gap-readiness-refresh.json` | Pass as a read-only readiness artifact on the local docs head. The self-test verifies required JSON rows, parity with the sign-off guard's machine-readable required-row list, stable branch-level remote workflow wording, the ruleset-aware review-branch path, unknown remote-compare failure wording, and valid relay-matrix manual status. The current May 15, 2026 refresh confirms the current Pixel 8 Pro is attached on Android 16 / API 36; root-helper coverage is blocked because `su 0 id` does not return root; manual TalkBack is blocked because TalkBack is installed but Bitwarden is the active accessibility service; Wi-Fi and cellular transports are visible but handover still needs a human or external harness; routed netem is blocked on Darwin; production relay coverage needs `RIPDPI_RELAY_MATRIX_CONFIG`; remote workflow confirmation is blocked because the local branch is ahead of `origin/main` and no fresh workflow covers it. |
| Feature-test sign-off guard | `test-lab/scripts/check-feature-test-signoff.sh`; `test-lab/scripts/test-feature-test-signoff.sh`; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/local-network-lab.yml")'` | Expected fail in the current local environment. The May 15, 2026 refresh still blocks sign-off because the completion audit is not marked complete, it explicitly says not complete, `Partial` rows remain, root/TalkBack/relay/routed-netem/remote-workflow readiness rows are blocked, and physical network handover is manual. The self-test covers guard help text, machine-readable required-row output, manual evidence template row parity, audit/evidence blocker and next-action parity, guard pass, blocked-audit/readiness failure, required `manual` readiness rows, missing required readiness rows, duplicate required rows, invalid statuses, malformed readiness JSON, and missing-readiness-artifact error handling; the local-network-lab workflow now runs that self-test after shell syntax validation. |
| Remote workflow confirmation runbook | `test-lab/README.md`; `docs/testing.md`; `docs/feature-test-manual-evidence-template.md` | Covered locally as operator instructions only. The test-lab README and testing guide list the hosted workflow commands for post-merge confirmation, account for the live ruleset path through a review branch and pull request checks before `main`, note that CodeQL is push-triggered rather than manually dispatched, and document the final `check-feature-test-signoff.sh --readiness` handoff for an operator-reviewed readiness JSON. They also require that any required row changed to `ready` be backed by a named artifact, run ID, transcript, or lab archive in the manual evidence template. The manual evidence template records review branch, pull request, required checks/reviews, the canonical readiness rows, final sign-off guard command/result, run IDs, commit SHA, conclusions, and artifact links. Actual remote execution remains blocked until the release owner approves review-branch publication and fresh workflows cover the local commits. |
| Local-network-lab validation-only block | Feature verifier scripts, custom probe broadcast dry-run, relay matrix positive/negative validation, `.github/workflows/local-network-lab.yml` YAML parse, and `docker compose -f test-lab/docker-compose.yml config` | Pass locally without starting the Docker lab or mutating device/network state. Covers the validation-only portion of the `local-network-lab` workflow after adding the feature artifact, checklist, readiness, and sign-off self-tests. |
| Relay matrix manifest preflight | `test-lab/scripts/check-relay-matrix-config.sh --config test-lab/relay/provider-matrix.example.json`; `test-lab/scripts/check-relay-matrix-config.sh --list-required-paths`; `test-lab/scripts/check-relay-matrix-config.sh --list-required-scenarios`; `test-lab/scripts/test-relay-matrix-config.sh`; `RIPDPI_RELAY_MATRIX_CONFIG=test-lab/relay/provider-matrix.example.json test-lab/scripts/check-feature-gap-readiness.sh --output /tmp/ripdpi-readiness-with-relay.json`; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/local-network-lab.yml")'` | Pass. The example manifest covers all 14 relay paths from the checklist and all 10 required provider-backed scenarios without live endpoints or literal sensitive values. The validator now exposes machine-readable required relay path and scenario lists, and the self-test verifies the manual evidence template's Provider Relay Matrix rows stay in parity with the canonical relay path list. The fixture self-test rejects duplicate relay IDs, relay kind/ID mismatches, duplicate/unknown/non-string scenarios, literal URL/userinfo refs, and sensitive-looking literal values. The readiness preflight switches `production_relay_matrix` from `blocked` to `manual` when a valid matrix config is supplied, while still requiring actual provider-backed execution. |
| VPN E2E Maestro precondition | `bash -n test-lab/scripts/run-vpn-e2e.sh`; `HOME=/tmp/ripdpi-no-maestro-home PATH=/usr/bin:/bin:/usr/sbin:/sbin RIPDPI_LAB_PROFILE=device ./test-lab/scripts/run-vpn-e2e.sh --skip-start --skip-install --out-dir /tmp/ripdpi-missing-maestro-fast-fail-expected`; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/local-network-lab.yml")'` | Pass for the fixed precondition: script exits 2 before install/probe when Maestro is missing from `PATH`, `MAESTRO_BIN`, and the default home install location, and `--skip-maestro` is not explicit; output tells the operator to install Maestro, set `MAESTRO_BIN`, add it to `PATH`, or manually connect VPN mode before using `--skip-maestro`. The local-network-lab workflow now enforces the same isolated fast-fail guard in nightly/script validation. |
| Custom debug-probe endpoint broadcast | `./test-lab/scripts/adb-run-probe.sh --profile custom --mode diagnostics --host lab.example.test --dns-server dns.example.test --dns-port 1053 --dns-hostname ok.test --http-url http://lab.example.test:8080/get --https-url https://lab.example.test:8443/ --tcp-host tcp.example.test --tcp-port 9000 --udp-host udp.example.test --udp-port 9001 --relay-endpoint relay.example.test:10080 --require-vpn-active false --require-proxy-ready false --print-broadcast`; `bash -n test-lab/scripts/adb-run-probe.sh`; `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/local-network-lab.yml")'` | Pass. The dry-run output includes the expected `DEBUG_PROBE` broadcast extras for custom lab host, DNS server/hostname/port, HTTP/HTTPS URLs, TCP and UDP hosts/ports, relay endpoint, and disabled VPN/proxy preconditions. The local-network-lab workflow now checks this path without requiring an attached device or live lab, so future cellular or routed-VM runs can use the same wrapper instead of hand-written broadcasts. |
| Debug-probe relay readiness client | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.debug.DebugLocalNetworkProbeTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:ktlintDebugSourceSetCheck :app:ktlintTestSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon` | Pass. The Android debug probe now uses the configured mock-relay endpoint, performs the JSON readiness handshake, emits `relayReady=true/false`, treats relay readiness failure as a hard probe failure, and unit-tests endpoint parsing plus malformed/auth-failure responses. This fixes the prior app-side evidence gap where `relay_endpoint` was accepted by the wrapper but probe JSON always reported `relayReady=null`. |
| Physical mock-relay VPN/proxy probes | `PATH="$HOME/.maestro/bin:$PATH" ./test-lab/scripts/run-vpn-e2e.sh --profile device --skip-install --out-dir test-lab/artifacts/vpn-relay-ready-20260515-020054 --timeout-ms 7000`; `PATH="$HOME/.maestro/bin:$PATH" ./test-lab/scripts/run-proxy-e2e.sh --profile device --skip-install --out-dir test-lab/artifacts/proxy-e2e-fixed-20260515-021920 --timeout-ms 7000`; `./gradlew :core:service:testDebugUnitTest --tests 'com.poyka.ripdpi.services.ServiceStopSelfTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `bash -n test-lab/scripts/run-proxy-e2e.sh` | Pass on Pixel 8 Pro. VPN and proxy probes both reported `relayReady=true`, `proxyReady=true`, DNS/HTTP/HTTPS/TCP/UDP success, no errors, and degraded-only QUIC unsupported. The new proxy runner uses Maestro to connect/disconnect the local card, resumes live automation without resetting service state, and fails if `dumpsys activity services` shows a leftover RIPDPI foreground service after disconnect. |
| Physical mock-relay diagnostics probe | `./test-lab/scripts/start-lab.sh --profile device`; `./test-lab/scripts/adb-run-probe.sh --profile device --mode diagnostics --timeout-ms 7000 --relay-endpoint 192.168.1.9:10080 --out-dir test-lab/artifacts/diagnostics-relay-ready-20260515-022702`; `./test-lab/scripts/stop-lab.sh` | Pass on Pixel 8 Pro. Diagnostics mode reported `relayReady=true`, DNS/HTTP/HTTPS/TCP/UDP success, `proxyReady=false`, `vpnEstablished=false`, no errors, and degraded-only QUIC unsupported. This covers the mock-relay diagnostics runtime path without requiring an active proxy or VPN service. |
| VPN E2E physical probe | `test-lab/artifacts/vpn-e2e-20260514-013348/probe-device-vpn.json` | DNS, HTTP, HTTPS, TCP, UDP pass; QUIC marked unsupported by debug probe |
| Private DNS physical VPN probe | `settings put global private_dns_mode hostname`; `settings put global private_dns_specifier dns.google`; `PATH="$HOME/.maestro/bin:$PATH" ./test-lab/scripts/run-vpn-e2e.sh --profile device --out-dir test-lab/artifacts/private-dns-vpn-maestro-20260514-081801 --timeout-ms 7000` | Pass with degraded QUIC support on Pixel 8 Pro. The run applied OS private DNS `dns.google`, seeded debug automation state, drove the VPN card with Maestro, established Android VPN transport (`vpnEstablished=true`, `tunActive=true`, `egressProtected=true`, `ipv4RouteInstalled=true`), and passed DNS, HTTP, HTTPS, TCP, and UDP. QUIC was not attempted by the Android debug probe (`QUIC_UNSUPPORTED_ANDROID_DEBUG_PROBE`). Cleanup restored private DNS to `null`/`null`, force-stopped the app, and stopped the Docker lab. |
| Appium smoke subset | `appium driver install uiautomator2`; `/tmp/ripdpi-appium-venv/bin/python -m pip install -r appium/requirements.txt`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_01_cold_launch.py tests/test_02_tab_navigation.py tests/test_03_settings_navigation.py -q`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_06_config_mode_selection.py tests/test_07_dns_settings.py tests/test_09_permission_handling.py tests/test_13_dns_encrypted.py -q`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_10_mode_editor.py tests/test_12_settings_preferences.py tests/test_14_history_screen.py tests/test_16_onboarding.py tests/test_17_advanced_controls.py tests/test_18_host_pack_dialog.py tests/test_19_about_screen.py -q`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_21_app_customization.py tests/test_22_connected_proxy_state.py tests/test_23_permission_banners.py tests/test_24_diagnostics_actions.py tests/test_25_dns_dnscrypt.py tests/test_26_config_mode_chips.py tests/test_27_mode_editor_chain_dsl.py tests/test_28_biometric_prompt.py -q` | Pass on Pixel 8 Pro: 3-test launch/navigation subset, 7-test Config/DNS/permission subset, 13-test mode-editor/settings/history/onboarding/advanced/host-pack/about subset with 9 passed and 4 explicit fixture gaps skipped, and 12-item customization/connected-permission/diagnostics/DNSCrypt/config-chain/biometric slice with 10 passed and 2 biometric emulator-state skips. Covers Appium session creation, debug automation launch, raw Compose resource-id lookup plus Android UiScrollable lookup, cold Home render, bottom-tab navigation, Settings-to-Advanced navigation, current Config preset cards, plain DNS editing, built-in DoH resolver selection, custom DoH/DoT/DNSCrypt fields, start-configured VPN permission dialog, long-form Mode editor fields, valid strategy-chain DSL editing, app-customization shape sheet, permission recommendation dismissal, diagnostics share/save actions, backup-PIN-required biometric flow, History filter clearing, onboarding completion, visible advanced toggle/edit controls, and About links. The later focused Appium advanced/host-pack rerun below clears the TLS fingerprint and host-pack visibility skips; remaining explicit Appium skips are environment/fixture rows such as biometric prompt routing and active scan state. Cleanup force-stopped the app and left no Appium server process. |
| Appium onboarding skip rerun | `./gradlew :app:assembleGithubDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/pages/onboarding_page.py`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_16_onboarding.py -q`; `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.ui.screens.onboarding.OnboardingScreenTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass on Pixel 8 Pro: onboarding skip and complete flows both passed with no skips after raising the intro top-action layer above the pager and exposing Skip as a Material text button. Focused Compose onboarding selector tests also passed. |
| Appium advanced and host-pack reachability rerun | `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/pages/base_page.py appium/pages/advanced_settings_page.py appium/tests/test_17_advanced_controls.py appium/tests/test_18_host_pack_dialog.py`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_17_advanced_controls.py tests/test_18_host_pack_dialog.py -q` | Pass on Pixel 8 Pro: 5 tests passed, 0 skipped, 1 Appium Python deprecation warning. A raw `uiautomator dump` scroll confirmed `advanced-option-tls-fingerprint-profile` and `host-pack-preset-youtube` are present; the Appium scroll helper now reaches the full Advanced Settings LazyColumn, including TLS fingerprint dropdown selection and host-pack confirm/dismiss dialogs. |
| Appium current install rerun | `appium -v`; `appium driver list --installed`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk RUNNER_TEMP=/tmp/ripdpi-appium-current bash scripts/ci/run-appium-smoke.sh`; final command after fixes: `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk RUNNER_TEMP=/tmp/ripdpi-appium-full-fixed2 bash scripts/ci/run-appium-smoke.sh` | Pass. Appium 3.4.2 with UiAutomator2 7.3.0 is installed. The first current installed full-suite rerun found two harness issues: `test_history_search_and_filter` assumed filter chips still existed after typing a no-match query against reset History state, and `test_support_bundle_visible` tapped export without waiting for Android's chooser/documents surface, leaving that external activity on top of the next Diagnostics launch (`78 passed, 16 skipped, 1 failed, 1 error` in 2106.34s). Focused repros isolated both behaviors. After updating the History assertion to accept the empty-state branch and updating the support-bundle test to wait for and close the external export surface, the focused support/empty-state repro passed with `1 passed, 1 skipped, 1 warning` in 38.91s. The final full Appium suite passed on Pixel 8 Pro with `79 passed, 17 skipped, 1 warning` in 1846.96s (0:30:46). Appium generated `appium/appium-report.html` and left no server process running. Covers launch/navigation, Config/DNS/permission, onboarding, Advanced Settings, host-pack dialogs, diagnostics actions/report/audit, logs, support, History, theme, live-service preset, activation windows, background guidance, and all 7 workflow journeys. |
| Appium live-preset and fixture-gated UI slice | `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_44_live_service_preset.py tests/test_45_activation_windows.py tests/test_46_background_guidance_banners.py -q`; `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/pages/advanced_settings_page.py appium/tests/test_45_activation_windows.py appium/pages/home_page.py appium/pages/settings_page.py appium/tests/test_46_background_guidance_banners.py`; focused reruns of `tests/test_45_activation_windows.py -q`, `tests/test_45_activation_windows.py::test_activation_payload_size_fields_visible -q -rs`, and `tests/test_46_background_guidance_banners.py -q -rs`; final combined rerun of `tests/test_44_live_service_preset.py tests/test_45_activation_windows.py tests/test_46_background_guidance_banners.py -q -rs` | Pass on Pixel 8 Pro with Appium 3.4.2 and UiAutomator2 7.3.0. The initial live-preset/background-guidance/activation slice passed 5 tests with 6 explicit fixture skips and one Appium Python deprecation warning in 125.32s. After retargeting activation-window tests to current round and payload-size dimensions, the isolated payload-size rerun passed in 44.57s and the full activation-window file passed all 3 tests with one Appium Python deprecation warning in 129.50s. After retargeting background-guidance tests to standalone granted-permission guidance and the biometric-locked backup-PIN warning/editor state, the background-guidance file passed all 5 tests with one Appium Python deprecation warning in 66.97s. The final combined 44-46 slice passed all 11 tests with one Appium Python deprecation warning in 199.55s, covering live-service Home/Diagnostics/Settings loads, activation-window controls, background guidance, backup-PIN warning/editor, Settings customization navigation, and the default no-error-snackbar state. |
| Appium diagnostics, logs, support, and theme slice | `./gradlew :app:assembleGithubDebug`; `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/conftest.py appium/pages/logs_page.py appium/pages/settings_page.py appium/tests/test_29_diagnostics_detail_sheets.py appium/tests/test_30_history_filters_details.py appium/tests/test_33_empty_states.py`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_31_logs_filters.py -q`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_29_diagnostics_detail_sheets.py tests/test_30_history_filters_details.py tests/test_31_logs_filters.py tests/test_32_support_bundle.py tests/test_33_empty_states.py tests/test_34_connection_transitions.py tests/test_35_theme_selection.py -q`; current installed-Appium rerun of the same 29-35 target list after commit `850ef536`; focused clean-reset rerun of `tests/test_30_history_filters_details.py::test_history_empty_states tests/test_33_empty_states.py`; focused support/empty-state repro after export-surface cleanup | Pass on Pixel 8 Pro: focused Logs filter rerun passed 2 tests; full 29-35 slice passed with 8 tests, 3 explicit persisted-device-data skips, and one Appium Python deprecation warning. The current rerun also passed with 8 tests, 3 explicit persisted-device-data skips, and one Appium Python deprecation warning. After debug automation reset started clearing diagnostics runtime/history tables, the focused empty-state rerun passed both History empty-state rows and left only diagnostics empty-state as a fixture skip (`2 passed, 1 skipped, 1 warning`). The support-bundle test now waits for the Android export surface by `driver.current_package` and closes chooser/documents packages in a `finally`; the focused support-bundle plus diagnostics-empty-state repro passed with `1 passed, 1 skipped, 1 warning` in 38.91s. Covers current Diagnostics Dashboard and Tools sections, History filter clearing and clean empty state, reachable Logs subsystem/severity filters, support-bundle launch cleanup, diagnostics empty-state guard when active fixture content is present, connection toggle stability, and theme dropdown selection by visible text fallback. |
| Appium diagnostics tail slice | `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/pages/base_page.py appium/pages/diagnostics_page.py appium/pages/history_page.py appium/tests/test_36_scan_section_idle.py appium/tests/test_39_strategy_audit_report.py appium/tests/test_41_diagnostics_search_filter.py appium/tests/test_44_live_service_preset.py`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_36_scan_section_idle.py tests/test_37_approach_mode_switching.py tests/test_38_scan_run_and_results.py tests/test_39_strategy_audit_report.py tests/test_40_diagnostics_detail_sheets.py tests/test_41_diagnostics_search_filter.py tests/test_42_history_filters.py tests/test_43_probe_conflict_dialog.py tests/test_44_live_service_preset.py tests/test_45_activation_windows.py tests/test_46_background_guidance_banners.py -q`; current installed-Appium rerun of the same 36-46 target list after fixing page-object drift; focused rerun of `tests/test_42_history_filters.py -q -rs` | Pass on Pixel 8 Pro: previous full 36-46 slice passed with 15 tests, 22 explicit scan-state/environment skips, and one Appium Python deprecation warning. The current rerun first exposed stale raw-scan, Tools scroll, History severity-filter, live Home, and Settings customization assumptions; after updating those page objects/tests, the full tail rerun passed with 12 tests, 25 explicit fixture skips, and one Appium Python deprecation warning. A later focused History rerun fixed dynamic connection-row selection and detail-sheet detection, passing all 4 History filter/detail tests with one Appium Python deprecation warning in 84.24s. Covers Scan profile summary, Diagnostics Tools actions, approach mode switching, History filter chips with dynamic seeded values, live service preset launches, settings/customization navigation, and background guidance absence/presence handling; scan-progress, scan-result, diagnostics detail-sheet, conflict-dialog, and active-scan rows still skip when the local fixture lacks the required active scan data or optional settings. |
| Appium deterministic diagnostics report slice | `appium --version`; `$HOME/.maestro/bin/maestro --version`; `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/lib/launch_contract.py appium/tests/test_38_scan_run_and_results.py appium/tests/test_39_strategy_audit_report.py`; `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.automation.DebugAutomationControllerTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:assembleGithubDebug --no-daemon`; `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/test_38_scan_run_and_results.py tests/test_39_strategy_audit_report.py -q -rs` | Pass on Pixel 8 Pro with Appium 3.4.2, UiAutomator2 7.3.0, and Maestro 2.5.1 available at `~/.maestro/bin/maestro`. The final focused Appium run passed 6 tests with 2 explicit precondition skips and one Appium Python deprecation warning in 212.60s. It covers scan state transition/cancel, deterministic strategy report summary, resolver recommendation actions, full-matrix toggle, audit assessment, and winning TCP/QUIC path cards through the new `diagnostics_report_demo` debug preset. |
| Appium workflow journeys | `/tmp/ripdpi-appium-venv/bin/python -m py_compile appium/tests/workflows/conftest.py appium/pages/config_page.py appium/pages/dns_settings_page.py appium/pages/home_page.py appium/pages/settings_page.py appium/pages/mode_editor_page.py appium/tests/workflows/test_wf01_onboarding_to_connect.py appium/tests/workflows/test_wf02_config_preset_connect.py appium/tests/workflows/test_wf03_permission_repair.py appium/tests/workflows/test_wf04_dns_roundtrip.py appium/tests/workflows/test_wf05_diagnostics_flow.py appium/tests/workflows/test_wf06_config_edit_validation.py appium/tests/workflows/test_wf07_settings_roundtrip.py`; focused `tests/workflows/test_wf07_settings_roundtrip.py -q`; final `PYTHON_BIN=/tmp/ripdpi-appium-venv/bin/python APPIUM_APK_PATH=$PWD/app/build/outputs/apk/github/debug/app-github-universal-debug.apk bash scripts/ci/run-appium-smoke.sh tests/workflows/ -q`; fresh installed-tool rerun of the same command after Maestro orientation hardening | Pass on Pixel 8 Pro with Appium 3.4.2 / UiAutomator2 7.3.0: all 7 workflow journeys passed with one Appium Python deprecation warning. The fresh rerun passed 7 tests in 213.62s. Covers onboarding completion into Home, Config mode selection into Home action, configured-mode permission repair, DNS plain-address persistence, Diagnostics Dashboard-to-Scan-to-Tools traversal and share/save action reachability, Mode editor invalid-port validation and save, and Settings WebRTC plus DNS persistence across navigation. |
| Maestro smoke pack | `PATH="$HOME/.maestro/bin:$PATH" bash scripts/ci/run-maestro-smoke.sh` | Pass on Pixel 8 Pro with Maestro 2.5.1 after normalizing every committed flow to portrait orientation. Covers cold Home launch, current Home mode-card primary action selector, bottom-nav Settings navigation, Advanced Settings navigation, diagnostics retention edit/save flow, and local-mode start/stop smoke using current mode-card resource IDs. The fresh rerun exposed that landscape device state clipped Home mode actions below the visible viewport; the flows now start with `setOrientation: PORTRAIT` and assert the visible local-mode primary action. A fresh post-diagnostics-preset rerun also passed all four committed flows through `~/.maestro/bin` resolution. After merging `origin/main` locally and rebuilding the current debug APK from `89deca61`, the four-flow smoke rerun passed again. After the local Maestro/Appium install was confirmed again on May 15, 2026, `PATH="$HOME/.maestro/bin:$PATH" bash scripts/ci/run-maestro-smoke.sh` passed all four committed flows again on the attached Pixel 8 Pro. |
| Maestro default-install fallback | `command -v maestro`; `$HOME/.maestro/bin/maestro --version`; `PATH="/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin" bash scripts/ci/run-maestro-smoke.sh`; `./test-lab/scripts/run-vpn-e2e.sh --profile device --skip-install --timeout-ms 5000 --keep-lab`; `./test-lab/scripts/stop-lab.sh` | Pass on Pixel 8 Pro with Maestro 2.5.1 installed at `~/.maestro/bin/maestro` and absent from the shell `PATH`. The smoke runner completed all four committed Maestro flows through the fallback path. The test-lab VPN orchestrator also resolved the fallback binary, drove the VPN card, established Android VPN transport, and passed DNS, HTTP, HTTPS, TCP, UDP, proxy readiness, TUN, IPv4 route, and egress-protection checks in `test-lab/artifacts/vpn-e2e-20260514-142314/probe-device-vpn.json`; the lab was stopped after the run. |
| Diagnostics-only probe | `test-lab/artifacts/probe-device-diagnostics.json` | DNS, HTTP, HTTPS, TCP, UDP pass; QUIC marked unsupported by debug probe |
| Test-lab archive redaction | `test-lab/artifacts/test-lab-artifacts-20260513T221424Z.tar.gz`; `test-lab/artifacts/test-lab-artifacts-20260514T214558Z.tar.gz` | Archive contains redacted logcat, dumpsys, lab env, probe artifact; latest archive redaction check passed after the current Docker lab doctor run |
| Release APK leak check | `zipinfo`, `apkanalyzer`, `dexdump` string scans | No test-lab, generated lab data, debug probe asset, instrumentation, or packet-smoke strings found |
| Offline analytics unit tests | `python3 -m unittest scripts.tests.test_offline_analytics_pipeline` | 7 tests, OK |
| Offline analytics sample corpus | `python3 -m scripts.analytics.pipeline run-all --manifest scripts/analytics/sample-corpus.json --output-dir /tmp/ripdpi-offline-analytics-20260514` | Pass |
| Mock relay failure-mode tests | `go test ./...` in `test-lab/relay/mock-relay` | Pass; Go unit tests cover ready handshake, auth-failure response, and malformed-response mode for local lab readiness/failure injection. |
| Rust relay interoperability | `scripts/ci/run-rust-relay-interoperability.sh` | Pass; local-network fixture stack 15/15, relay transport crates 58/58, relay-core 19/19, and nested proxy runtime `network_e2e` 29/29 passed with `RIPDPI_RUN_NESTED_PROXY_E2E=1`. Coverage includes VLESS, xHTTP, Hysteria2, TUIC, ShadowTLS, MASQUE, Cloudflare origin, NaiveProxy, relay runtime config validation, local SOCKS/TCP/TLS/UDP/QUIC fixture round-trips, chained upstream routing, and upstream reset/drop fault classification. |
| Netem script namespace validation | `docker run --rm --cap-add NET_ADMIN --cap-add NET_RAW -v "$PWD:/repo" -w /repo alpine:3.20 sh -lc 'apk add --no-cache bash iproute2 iptables sudo >/dev/null; NETEM_DEV=eth0 test-lab/chaos/netem/apply-loss.sh 10%; tc qdisc show dev eth0; NETEM_DEV=eth0 test-lab/chaos/netem/apply-quic-drop.sh 9443; iptables -S INPUT; iptables -S OUTPUT; NETEM_DEV=eth0 test-lab/chaos/netem/clear.sh; tc qdisc show dev eth0'` | Pass in a disposable Linux container namespace. `apply-loss.sh` produced `qdisc netem ... loss 10%`, `apply-quic-drop.sh` inserted UDP 9443 INPUT/OUTPUT drop rules, and `clear.sh` returned `eth0` to `qdisc noqueue` with no matching UDP 9443 rules. This validates script mechanics only; it does not replace the routed Linux VM path that must carry Android/device traffic. |
| Native bloat check | `python3 scripts/ci/verify_native_bloat.py --report-json /tmp/ripdpi-native-bloat-20260514.json --report-md /tmp/ripdpi-native-bloat-20260514.md` | Pass against checked-in baseline |
| Autolearn and remembered-network focused tests | `./gradlew :core:data:testDebugUnitTest :core:diagnostics-data:testDebugUnitTest :core:diagnostics:testDebugUnitTest :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.data.HostAutolearnSettingsTest' --tests 'com.poyka.ripdpi.data.NetworkStrategyMemoryTest' --tests 'com.poyka.ripdpi.data.diagnostics.RememberedNetworkPolicyStoreTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsStrategyProbeRecommendationPersistenceTest' --tests 'com.poyka.ripdpi.activities.SettingsUiStateTest' --tests 'com.poyka.ripdpi.activities.SettingsUiStateAssemblerTest' --tests 'com.poyka.ripdpi.ui.screens.settings.AdvancedSettingsSectionsTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 257 tasks, 134 focused tests, 0 failures; XML confirms host-autolearn settings normalization, network fingerprint identity hashing, remembered-policy source round-trip and validation transitions, validated recommendation persistence, mismatched recommendation rejection, host-autolearn runtime/blocked UI state, remembered-network counts, and clear-button UI callbacks. |
| Automatic probe scheduler and boundary-source tests | `./gradlew :core:diagnostics:testDebugUnitTest --tests 'com.poyka.ripdpi.diagnostics.AutomaticProbeSchedulerTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsBoundarySourcesTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 132 tasks, 21 focused tests, 0 failures; XML confirms transport-switch and link-refresh launch eligibility, cooldown and active-scan suppression, settings/captive/unvalidated/stale-telemetry rejection, validated remembered-policy exact-match suppression, failed-connection telemetry launch, remembered-policy `clearAll` reset of DNS path preferences, persisted automatic background metadata decoding, and remembered-policy audit decoding. |
| Native host-autolearn policy tests | `cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-runtime-policy autolearn -- --nocapture` | Pass, 31 focused tests, 0 failures; output confirms max-host eviction, per-network-scope learned and blocked host stores, persisted reload, legacy record loading without new block metadata, fingerprint-mismatch invalidation, filtered telemetry/system hosts, allowed user domains, preferred-group promotion, penalty expiry, block expiry, and persist-debounce behavior. |
| Host-autolearn export/import boundary | `./gradlew :core:data:testDebugUnitTest --tests 'com.poyka.ripdpi.data.AppSettingsJsonFeatureMapperTest.host autolearn json exports knobs but excludes learned host store' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :core:data:ktlintTestSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 70 tasks for the focused test plus ktlint pass; XML confirms settings JSON imports/exports the host-autolearn configuration knobs and excludes learned-host runtime store fields/path data. |
| Remembered-policy runtime history tests | `./gradlew :core:diagnostics:testDebugUnitTest --tests 'com.poyka.ripdpi.diagnostics.RuntimeHistoryMonitorTest.handover policy rotation keeps previous remembered policy neutral' --tests 'com.poyka.ripdpi.diagnostics.RuntimeHistoryMonitorTest.used remembered policy records failure when session ends before proof' --tests 'com.poyka.ripdpi.diagnostics.RuntimeHistoryMonitorTest.observed remembered policy is validated after proved successful session' --tests 'com.poyka.ripdpi.diagnostics.RuntimeHistoryMonitorTest.fallback to non remembered policy keeps remembered policy audit on session' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 132 tasks, 4 focused tests, 0 failures; XML confirms remembered-policy exact-match application metadata, handover rotation accounting, success validation after proof, failure accounting when proof is missing, and fallback audit retention. |
| Proxy and relay unit tests | `./gradlew :core:service:testDebugUnitTest :core:engine:testDebugUnitTest :app:testGithubDebugUnitTest --tests ... -Pripdpi.skipNativeBuild=true --no-daemon` | 127 focused tests, 0 failures across proxy supervisors, relay runtime config, relay UI validation, MASQUE credentials, Cloudflare publish/geohash, NaiveProxy policy, and proxy JSON/preference mapping |
| Diagnostics workflow, history, and export unit tests | `./gradlew :core:diagnostics:testDebugUnitTest :core:diagnostics-data:testDebugUnitTest :app:testGithubDebugUnitTest --tests ... -Pripdpi.skipNativeBuild=true --no-daemon` | 146 focused tests, 0 failures across home composite, scan workflow/controller/coordinator, execution policy, archive exporter/renderer, detail/share services, history store, ViewModel, copy, and share intents |
| Diagnostics workflow focused policy tests | `./gradlew :core:diagnostics:testDebugUnitTest :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsScanExecutionCoordinatorTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsExecutionPolicyTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsHomeWorkflowServiceTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsScanWorkflowTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsStrategyRecommendationWorkflowInvariantTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsSummaryProjectorTest' --tests 'com.poyka.ripdpi.ui.screens.diagnostics.DiagnosticsScreenTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 244 tasks, 83 focused tests, 0 failures; XML confirms RAW_PATH policy forcing, automatic/manual admission restrictions, hidden-probe progress isolation, DNS-corrected reprobe wait/failure paths, resolver override/reprobe recommendations, home audit finalization/fallback paths, full-matrix recommendation non-persistence, audit assessment metadata, selected target cohort metadata, and diagnostics UI conflict/candidate/full-matrix states. |
| Retention and archive-failure focused tests | `./gradlew :core:diagnostics-data:testDebugUnitTest :core:diagnostics:testDebugUnitTest :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.data.diagnostics.DiagnosticsHistoryStoresRoomTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsArchiveComponentsTest' --tests 'com.poyka.ripdpi.diagnostics.DiagnosticsBootstrapperTest' --tests 'com.poyka.ripdpi.activities.DiagnosticsArchiveCopyTest' --tests 'com.poyka.ripdpi.activities.DiagnosticsViewModelTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 252 tasks, 0 failures; XML confirms history retention trims stale scan/probe/snapshot/telemetry/native-event/export/bypass/DNS rows, archive cache cleanup keeps only managed fresh archives, archive generation failure clears busy state with a negative message, and save-copy failure raises `IOException` when the destination stream cannot be opened |
| UI state and locale unit tests | `./gradlew :app:testGithubDebugUnitTest --tests ... -Pripdpi.skipNativeBuild=true --no-daemon` | 220 focused tests, 0 failures across Home, Settings, History, Diagnostics, Onboarding, shell controller, locale config, and related UI state factories/models |
| Settings schema, migration, and import/export unit tests | `./gradlew :core:data:testDebugUnitTest :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.data.AppSettingsJsonTest' --tests 'com.poyka.ripdpi.data.RoutingProtectionSettingsTest' --tests 'com.poyka.ripdpi.data.DnsResolverConfigTest' --tests 'com.poyka.ripdpi.data.NetworkStrategyMemoryTest' --tests 'com.poyka.ripdpi.activities.SettingsViewModelBootstrapperTest' --tests 'com.poyka.ripdpi.activities.SettingsUiModelsTest' --tests 'com.poyka.ripdpi.ui.screens.settings.StrategyConfigImportTest' --tests 'com.poyka.ripdpi.ui.screens.settings.StrategyConfigScreenTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 241 tasks, 64 focused tests, 0 failures; XML confirms current settings JSON round-trip, missing-field default fill, unknown-key tolerance, unsupported format rejection, legacy routing-preset migration, DNS defaults, remembered-network storage parsing, settings bootstrap defaults, strategy config import validation, and import/export UI callbacks |
| Browser, HTTP, DNS, TLS, and session native tests | `cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-diagnostics-http -p ripdpi-diagnostics-tls -p ripdpi-diagnostics-dns -p ripdpi-diagnostics-classification -p ripdpi-packets -p ripdpi-session -- --nocapture` | 490 tests, 0 failures across HTTP response parsing/classification, redirects, error-page fingerprints, TLS alert/profile handling, DNS anomaly/redirect detection, packet field extraction, SSH banner classification, QUIC parsing, and session triggers |
| Native parser fault matrix | `cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-diagnostics-parsers -p ripdpi-diagnostics-http -p ripdpi-diagnostics-tls -p ripdpi-failure-classifier -p ripdpi-packets -- --nocapture` | Pass, 390 tests, 0 failures; includes malformed HTTP status and headers, missing header delimiter, non-UTF-8 headers, HTTP/2 rejection in failure classification, invalid TLS/JA3 inputs, truncated TLS SNI payloads, QUIC truncated/incomplete packets, parser no-panic property tests, and classifier isolation for non-matching transport/strategy errors. |
| Home owned-stack browser handoff | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.activities.MainHomeDiagnosticsUiStateTest' --tests 'com.poyka.ripdpi.activities.MainHomeDiagnosticsActionsTest' --tests 'com.poyka.ripdpi.activities.TransportSpecificRemediationSupportTest' --tests 'com.poyka.ripdpi.ui.screens.home.HomeScreenTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:installGithubDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; `test-lab/artifacts/owned-stack-browser-route-20260514-044413/summary.txt` | Focused tests passed after wiring Home owned-stack remediation targets through `HomeDiagnosticsLatestAuditUiState`, the Home remediation ladder, Compose click handlers, and navigation. The updated debug APK installed on Pixel 8 Pro; launching the `owned_stack_browser` route through debug automation rendered `owned_stack_browser-screen`, `RIPDPI Browser`, and the HTTPS URL field with no app fatal or ANR markers. |
| Home owned-stack service-restart regression | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.activities.MainViewModelTest.owned stack home remediation survives service restart on same network' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 227 tasks, 1 focused test, 0 failures; XML confirms an owned-stack Home remediation remains active with the expected target URL across a VPN service running-to-halted-to-running transition when the network fingerprint is unchanged. |
| Owned-stack browser device fetch | `test-lab/artifacts/owned-stack-browser-fetch-20260514-045421/summary.txt` plus captured XML/screenshot/logcat | Pixel 8 Pro launched the owned-stack browser route, entered `https://example.org`, and tapped `Open in RIPDPI browser`; the result screen stayed on `owned_stack_browser-screen`, displayed status `200`, `example.org`, and backend text, with no owned-stack error banner and no app fatal or ANR markers. Cleanup confirmed no app process, proxy listener, or TUN output remained. |
| Owned-stack browser private-query log redaction | `test-lab/artifacts/owned-stack-browser-private-query-20260514-050327/summary.txt` plus captured XML/screenshot/logcat | Pixel 8 Pro launched the owned-stack browser route, entered `https://example.org/?ripdpi_secret=codex_private_query_20260514_050327`, and loaded it with status `200`; the result UI contained the query token as expected, but logcat scans for the token, `ripdpi_secret`, and the query URL returned no lines. Cleanup confirmed no app process, proxy listener, or TUN output remained. |
| Owned-stack browser client policy | `./gradlew :core:service:testDebugUnitTest --tests 'com.poyka.ripdpi.services.OwnedStackBrowserServiceTest' --tests 'com.poyka.ripdpi.services.OwnedTlsClientFactoryTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 169 tasks, 0 failures; focused tests cover platform HTTP engine selection for cached ECH-capable authorities, H2-only retry after QUIC-capable platform failure, native fallback when confirmed ECH is required but not cached, HTTPS-only URL normalization, TLS browser-family profile rotation, and ECH template metadata. |
| Device ECH readiness probe | `./gradlew :app:connectedGithubDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.poyka.ripdpi.integration.EchReadinessProbeInstrumentedTest -Pandroid.testInstrumentationRunnerArguments.ripdpi.runNetworkTests=1 -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon` | Pass on Pixel 8 Pro / Android 16, 315 tasks, 1 instrumented test, 0 failures, 0 skips; XML confirms `cloudflareEchNegotiatesWhenNetworkTestsEnabled`, including HTTPS RR fetch and native ECH negotiation. |
| Theme contrast unit tests | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.ui.theme.RipDpiColorContrastTest' -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 236 tasks, 2 focused tests, 0 failures; XML confirms light and dark secondary text stays readable against the theme background. |
| Dynamic-color source guard | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.ui.DesignSystemSourceRulesTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:ktlintTestSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon` | Pass, 236 tasks, 5 focused tests, 0 failures; XML confirms the app theme does not opt into platform dynamic color palettes, so there is no active device dynamic-palette mode in the current app surface. Test-source ktlint passed. |
| Kotlin strategy unit tests | `./gradlew :app:testGithubDebugUnitTest :core:diagnostics:testDebugUnitTest :core:service:testDebugUnitTest --tests ... -Pripdpi.skipNativeBuild=true --no-daemon` | 91 focused tests, 0 failures across strategy config UI, import/route flows, strategy pack logic, host-pack presets, packet-smoke network support, diagnostics strategy probe presenters/coordinator/service, recommendation invariants, and strategy pack repository/service logic |
| Native strategy and desync tests | `cargo test --manifest-path native/rust/Cargo.toml -p ripdpi-strategy-config -p ripdpi-strategy-http -p ripdpi-strategy-ipv6 -p ripdpi-strategy-lua -p ripdpi-strategy-registry -p ripdpi-strategy-trait -p ripdpi-strategy-udp -p ripdpi-strategy-window -p ripdpi-desync -p ripdpi-desync-runtime -- --nocapture` | 300 tests, 0 failures across TCP desync planning/runtime, TTL/OOB/fake ordering/fallbacks, config reload and YAML/TOML parsing, HTTP header transforms, IPv6 extension injection, Lua VM/script compile coverage, registry materialization, strategy trait contracts, UDP length mutation, and window clamp planning |
| Physical proxy smoke and host SOCKS curl | `test-lab/scripts/adb-run-probe.sh --profile device --mode proxy --timeout-ms 10000 --out-dir test-lab/artifacts/proxy-smoke-20260514-030750`; `adb forward tcp:11080 tcp:1080`; `curl --socks5-hostname 127.0.0.1:11080 http://192.168.1.9:8080/get` | Debug app launched via automation UI, local proxy service started from the Home screen, probe verdict `Degraded` only because QUIC is unsupported by the Android debug probe; proxy ready, DNS/HTTP/HTTPS/TCP/UDP pass, errors empty; host curl returned HTTP 200 through forwarded SOCKS5 |
| Physical proxy port conflict | `test-lab/artifacts/proxy-port-conflict-20260514-041518/summary.txt` plus captured `home-before.xml`, `home-after.xml`, screenshots, and `logcat-after-start.txt` | Pixel 8 Pro held port 1080 open with `toybox nc`; starting Local proxy mode from Home left the mode inactive, restored the `Enable` action, and showed `Failed to start Proxy: ... Address already in use (os error 98)`; native logs recorded the bind error plus `Proxy status: Disconnected -> Failed` and `Failed -> Disconnected`; cleanup confirmed no app process or listener remained |
| Physical proxy process-kill recovery | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.automation.DebugAutomationControllerTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:installGithubDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; `test-lab/artifacts/proxy-process-kill-live-relaunch-fixed-20260514-042706/summary.txt` | Focused unit test passed; updated debug APK installed on Pixel 8 Pro; Local proxy mode started with UI status `Active`; killing the app process with `run-as` caused Android to restart the sticky proxy service in a new process, port 1080 remained listening, and live automation relaunch preserved UI status `Active` plus the `Disable` action; no fatal log lines; cleanup confirmed no app process or listener remained |
| Physical VPN process-kill relaunch | `test-lab/scripts/restart-lab.sh --profile device`; `./gradlew :app:installGithubDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; `test-lab/artifacts/vpn-process-kill-20260514-054810/kill-summary.txt` plus captured UI XML, dumpsys, and logcat artifacts | Pixel 8 Pro granted notification permission, accepted the system VPN consent dialog, and `dumpsys activity services` confirmed foreground `RipDpiVpnService`. Killing PID `3694` with `run-as` left no app process after 35s; relaunch created PID `4105` and Home rendered both local and remote modes as `Inactive`, so the app did not report a false running VPN state. Cleanup force-stopped the app and removed the service record. |
| Physical same-version reinstall retention | `adb install -r app/build/outputs/apk/github/debug/app-github-arm64-v8a-debug.apk`; `test-lab/artifacts/reinstall-retention-after-summary.txt`; `test-lab/artifacts/reinstall-retention-logcat.txt` | Pixel 8 Pro reinstalled the same debug APK over a seeded profile without clearing app data; relaunch rendered Home instead of onboarding and preserved the local profile summary `tcp: split(host+1) - Encrypted DNS · Cloudflare (DoH)`. Logcat scan found no app fatal, ANR, SQLite, DataStore, or corruption markers. |
| Physical migrated-install retention | Built `v0.0.6` in `/tmp/ripdpi-v006-migration.g6bkhk` with `./gradlew :app:assembleDebug -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; installed and seeded `app/build/outputs/apk/debug/app-arm64-v8a-debug.apk`, then installed current `app/build/outputs/apk/github/debug/app-github-arm64-v8a-debug.apk` over it; captured `test-lab/artifacts/legacy-migration-v006-to-current-fixed-20260514-074003/summary.txt` | Pixel 8 Pro upgraded from `versionName=0.0.6` / versionCode ending in `6` to `versionName=0.0.7` / versionCode ending in `7`; the seeded settings-ready profile survived, Home rendered instead of onboarding, the Cloudflare profile summary remained visible, and logcat contained no app fatal or ANR markers after the Room migration-order fix. |
| API 27, 31, and 35 emulator proxy smokes | `sdkmanager --install system-images;android-27;google_apis;arm64-v8a`; repaired API 31 and API 35 images with `sdkmanager --uninstall/--install`; created `RIPDPI_API27`, `RIPDPI_API31`, and `RIPDPI_API35`; ran `adb-run-probe.sh --profile emulator --mode proxy --timeout-ms 10000` on each | API 27 / Android 8.1, API 31 / Android 12, and API 35 / Android 15 each booted from a clean emulator image, installed the debug APK, launched Home through automation, started local proxy from the UI, and passed proxy readiness plus DNS/HTTP/HTTPS/TCP/UDP; each verdict was `Degraded` only because QUIC is unsupported by the Android debug probe |
| Persian large-font forced-RTL UI smoke | API 35 emulator with `settings put system font_scale 1.5`, `settings put global debug.force_rtl 1`, `cmd locale set-app-locales com.poyka.ripdpi --user 0 --locales fa`; captured Home, Config, Diagnostics, and Settings XML/screenshots under `test-lab/artifacts/api35-fa-large-font-ui-20260514-034335/` | App locale reported `[fa]`; Home, Config, Diagnostics, and Settings root nodes plus bottom navigation nodes rendered; screenshots captured for each screen; no app fatal exception or ANR markers in the captured logcat tail |
| German, Spanish, and Chinese Home locale smoke | `cmd locale set-app-locales com.poyka.ripdpi --user 0 --locales de`; same for `es` and `zh-CN`; captured `test-lab/artifacts/locale-home-de-20260514/home-summary.txt`, `test-lab/artifacts/locale-home-es-20260514/home-summary.txt`, and `test-lab/artifacts/locale-home-zh_CN-20260514/home-summary.txt` | Pixel 8 Pro rendered Home in German, Spanish, and zh-CN with localized mode cards, primary/configure buttons, bottom navigation, and permission guidance; summaries show long German/Spanish labels and compact Chinese labels stayed bounded within their controls on Home. App locale was restored to `en`; no app fatal or ANR markers were captured. |
| German, Spanish, and Chinese main-route locale smoke | Debug automation launched `config`, `diagnostics`, and `settings` start routes under `de`, `es`, and `zh-CN`; captured summaries, XML, screenshots, and bounds checks under `test-lab/artifacts/locale-main-routes-20260514/` | All nine route/locale captures contained the expected `config-screen`, `diagnostics-screen`, or `settings-screen` root id; text/control bounds checks passed for every capture, including compact Chinese and longer German/Spanish labels on the top-level non-Home routes. App locale was restored to `en`; no app fatal or ANR markers were captured. |
| Config action-row accessibility labels | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.ui.screens.config.ConfigScreenTest.local bypass action rows expose accessibility labels' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:ktlintGithubDebugSourceSetCheck :app:ktlintTestSourceSetCheck -Pripdpi.skipNativeBuild=true --no-daemon`; rebuilt and installed debug APK, then captured `test-lab/artifacts/config-accessibility-label-20260514/summary.txt` | Focused Compose test passed and verifies the Config DNS and packet-settings action rows expose localized content descriptions. Rebuilt Pixel 8 Pro XML for Spanish Config shows `config-dns-settings` with `Ajustes de DNS` and `config-local-bypass-desync` with `Método de desync` directly on the focusable/clickable nodes; no app fatal or ANR markers were captured. |
| Top-level accessibility label audit | Debug automation captured Home, Config, Diagnostics, and Settings under `en`, `de`, `es`, and `zh-CN`; verifier wrote `test-lab/artifacts/accessibility-label-audit-20260514/summary.txt` | All 16 captures contained the expected route root id, had no enabled actionable controls without a text or content-description label in the subtree, and had no app fatal or ANR markers. Several controls still derive labels from descendant text rather than a direct node description, so this is automated accessibility-tree evidence, not a substitute for a manual TalkBack pass. |
| Nested/detail route locale and accessibility audit | Debug automation captured 14 nested/detail routes under all 7 supported locales and wrote `test-lab/artifacts/nested-locale-routes-20260514/summary.txt`; after fixes, reruns wrote `test-lab/artifacts/nested-locale-routes-fixed-20260514/summary.txt` and `test-lab/artifacts/nested-locale-dns-residual-fixed-20260514/summary.txt` | Initial audit found partially visible mode-editor switches, log chips, DNS cards, and detection routes without sufficient direct accessibility labels or route tags. The fixed run passed 96/98 combinations; the residual German/Persian DNS rerun passed 2/2 with expected route roots, bounded controls, zero unlabeled actionable controls, and no fatal or ANR markers. |
| Installed release log verbosity | `./gradlew :app:testGithubDebugUnitTest --tests 'com.poyka.ripdpi.RipDpiAppLoggingTest' -Pripdpi.skipNativeBuild=true --no-daemon`; `./gradlew :app:assembleGithubRelease -Pripdpi.nativeAbisOverride=arm64-v8a -Pripdpi.nativeAbiParallelism=1 --no-daemon`; installed rebuilt release APK on API 35 emulator, cleared app data/logcat, launched via launcher intent, and captured `test-lab/artifacts/release-log-verbosity-20260514-040812/app-logcat.txt` | Unit test confirmed debug builds keep WorkManager verbose logging while release builds use `WARN`; release R8 strips remaining WorkManager debug/verbose calls; rebuilt release APK launched on API 35; scan found `app_owned_debug_verbose_lines=0` and `sensitive_marker_lines=0`. Broad process logcat still contains platform startup `D/V` lines outside app-owned tags |

## Checklist Section Coverage

| Checklist section | Status | Evidence | Remaining work |
| --- | --- | --- | --- |
| How to use | Partial | Changed areas identified as test-lab, root detection, diagnostics archive redaction, Android build packaging, native test stability | Full release-candidate matrix is not complete |
| Test dimensions | Partial | Debug, release verification build, physical arm64, API 27 emulator, API 31 emulator, API 34 emulator, API 35 emulator, API 36 physical, VPN, proxy, diagnostics-only, local dual-stack lab service set; OS private DNS VPN-mode run established Android VPN transport and passed DNS/HTTP/HTTPS/TCP/UDP with expected debug-probe QUIC degradation | Rooted physical device; cellular, handover, IPv4-only, IPv6-only, captive or limited network |
| Core smoke matrix | Partial | Physical VPN E2E, API 27/31/35 emulator proxy smokes, physical proxy smoke with host SOCKS curl, diagnostics probe, connected tests, release APK inspection, static analysis, Roborazzi and unit gates from this pass | Full relay matrix and GitHub Actions confirmation after push |
| App shell, navigation, and settings | Partial | API 34 fresh-start UI smoke reached onboarding, Home, and Settings; API 35 Persian large-font forced-RTL smoke rendered Home, Config, Diagnostics, and Settings; German, Spanish, and zh-CN top-level route smokes rendered Home, Config, Diagnostics, and Settings; all 7 locales rendered 14 nested/detail routes with expected route roots, bounded controls, zero unlabeled actionable controls after fixes, and no fatal or ANR markers; connected UI tests passed with no failures; focused Home, Settings, Onboarding, shell-controller, settings-state, theme contrast, and dynamic-color source-guard tests passed; settings schema/import tests cover defaults, legacy migration, and import/export validation; proxy process-kill relaunch kept the UI aligned with the restarted service; VPN process-kill relaunch returned Home to inactive rather than a false running state; same-version reinstall retained app data and profile state on device; migrated install from v0.0.6 to current retained the seeded settings profile and rendered Home without Room migration crashes | Manual TalkBack checks |
| Proxy service | Partial | Service lifecycle connected tests passed, including resolved command-line preferences; physical proxy smoke proved local SOCKS5 readiness plus DNS/HTTP/HTTPS/TCP/UDP through the lab; host `curl --socks5-hostname` returned HTTP 200 through `adb forward`; API 27/31/35 emulator proxy smokes passed the same lab probes; physical port-conflict run confirmed the app reports the bind error and returns to inactive instead of claiming readiness; physical process-kill run confirmed sticky restart keeps the listener and UI active; focused proxy supervisor, startup-failure, runtime-coordinator, auto-apply, JSON, and preference tests passed | Relay-on proxy combinations |
| VPN service | Partial | Physical VPN probe established TUN, proxy readiness, protected egress, IPv4 route, DNS/HTTP/HTTPS/TCP/UDP success; notification and permission flow instrumentation passed; physical process-kill relaunch verified no false running UI state after a real foreground `RipDpiVpnService` process kill; Maestro-backed private-DNS VPN run established Android VPN transport under OS private DNS and passed DNS/HTTP/HTTPS/TCP/UDP with expected debug-probe QUIC degradation | Cellular handover, IPv4-only, IPv6-only, always-on/lockdown, relay-on VPN, Unix socket protection fallback runtime path |
| DNS and resolver resilience | Partial | Diagnostics and VPN probes resolved local DNS; focused native tests cover resolver metadata, encrypted-DNS endpoint selection, DNS anomaly signals, malformed pointers, CNAME redirects, TTL divergence, and oracle quorum behavior; OS private DNS `dns.google` VPN-mode run established Android VPN transport and passed DNS/HTTP/HTTPS/TCP/UDP | DoH, DoT, DNSCrypt, DoQ provider success on device; handover behavior |
| Packet strategy features | Partial | Packet-smoke instrumentation report present with 0 failures; Kotlin strategy UI/probe/service tests passed; native desync, registry, config, HTTP, IPv6, Lua, UDP, and window strategy tests passed; native packet and root-helper crates passed targeted tests; release APK inspected for debug-only test strings | Full per-family runtime matrix across IPv4, IPv6, TCP, UDP, QUIC, DTLS, Lua rawsend, and rooted privileged strategies |
| Relay and tunneling paths | Partial | Mock relay readiness verified by test-lab doctor; mock relay Go tests cover ready, auth-failure, and malformed-response modes; Android debug-probe tests now cover the client readiness handshake and `relayReady` result emission; archive includes deterministic lab state; focused relay unit tests cover runtime config resolution, subprocess supervision, UI validation, MASQUE credentials, Cloudflare publish/geohash, and NaiveProxy policy; `scripts/ci/run-rust-relay-interoperability.sh` passed local-network fixture, transport crate, relay-core, and nested proxy runtime E2E coverage for local relay paths and fault classification | Production relay paths still need provider-backed runtime validation: VLESS Reality, VLESS xHTTP, WARP, Cloudflare Tunnel, MASQUE, Hysteria2, TUIC v5, ShadowTLS v3, NaiveProxy, WebTunnel, obfs4, Snowflake, Google Apps Script path |
| Diagnostics workflows | Partial | Physical diagnostics-only probe passed DNS/HTTP/HTTPS/TCP/UDP; connected diagnostics tests passed; focused diagnostics tests cover home composite, scan workflow/controller/coordinator, execution policy, RAW_PATH policy forcing, command-line-settings restrictions, hidden-probe conflict/progress isolation, resolver override and reprobe decisions, full-matrix recommendation non-persistence, audit assessment/cohort metadata, ViewModel, share/copy, archive exporter, and archive renderer behavior | Full matrix audit runtime, RAW_PATH with VPN stop behavior, and device-level cancellation/UI busy-state confirmation |
| Autolearn and remembered networks | Partial | Focused unit tests cover host filtering settings, remembered-network policy storage, network fingerprint identity hashing, recommendation persistence invariants, scheduler handover/link-refresh eligibility and suppression, remembered-policy exact-match suppression, reset-to-store clear of DNS path preferences, persisted background metadata decoding, host-autolearn runtime UI state, remembered-network clear-button UI callbacks, native max-host eviction, per-network-scope learned and blocked host stores, persisted reload, legacy record loading without new block metadata, fingerprint-mismatch invalidation, telemetry/system host filtering, preferred-group promotion, penalty expiry, block expiry, persist debounce, settings export/import boundary for learned-host runtime state, remembered-policy application metadata, handover rotation accounting, success validation, failure accounting, and fallback audit retention | Device-level handover trigger and real-network remembered recommendation apply path |
| Browser and HTTP stack | Partial | Connected tests and parser tests present in the physical Android test report; secure HTTPS probe succeeded; focused native tests cover HTTP response parsing/classification, redirects, TLS alert/profile handling, SSH banner classification, packet field extraction, parser fault handling, and session triggers; Home owned-stack remediation now carries the target URL into the browser route and survives same-network service restart; the owned-stack browser route renders and fetches `https://example.org` with status 200 on Pixel 8 Pro; private-query browser fetch kept the synthetic query token out of captured logcat; focused service tests cover H2-only retry eligibility and unit-level ECH policy gating; Pixel 8 Pro instrumented ECH readiness probe confirmed HTTPS RR fetch plus native ECH negotiation | Positive Android platform HttpEngine ECH gating on an Android 17-capable device |
| Root helper and privileged operations | Partial | Non-rooted physical degradation confirmed; API 34 rooted emulator shell showed `su 0 id` works; root detector and helper-manager unit tests passed; native IPC and privileged-operation tests passed | Rooted physical device app lifecycle, helper APK extraction through app startup, helper readiness, actual privileged send operations, helper stop cleanup |
| Logging, history, export, and privacy | Covered locally | Diagnostics archive redaction was fixed and verified; release APK omits lab/test artifacts; archive contains redacted logcat; focused diagnostics history, archive exporter/renderer, detail/share, copy, and share-intent tests passed; focused retention/archive-failure run confirmed stale diagnostics rows are trimmed, archive cache cleanup is scoped to managed archives, archive generation failure clears busy state with an error message, and save-copy failure throws `IOException`; installed release log scan found no app-owned debug/verbose lines and no sensitive markers | None for the local logging/export/privacy slice |
| UI, Compose, localization, and accessibility | Partial | Roborazzi gates passed earlier in this pass; locale key diff, locale config, and README selector checks passed; API 34 UI smoke had no fatal logcat markers; API 35 Persian large-font forced-RTL smoke captured Home, Config, Diagnostics, and Settings screenshots/XML with no app fatal logcat markers; German, Spanish, and zh-CN top-level route smokes rendered localized labels within Home, Config, Diagnostics, and Settings controls, with route-id and bounds checks passing; Config action rows now expose localized labels on the focusable/clickable nodes; top-level accessibility-tree audit found no unlabeled enabled actionable controls across 16 route/locale captures; nested/detail route audit covered 98 route/locale combinations across all 7 locales after fixes, with an additional 2-route residual DNS rerun passing; Appium smoke subsets passed cold Home render, bottom-tab navigation, Settings-to-Advanced navigation, current Config cards, DNS mode/protocol controls, VPN permission dialog, Mode editor long-form fields, onboarding skip and completion, backup-PIN-required biometric flow, History filter clearing, visible advanced controls, TLS fingerprint dropdown selection, host-pack confirm/dismiss dialogs, About links, and all 7 workflow journeys through external UiAutomator2 resource-id and Android UiScrollable lookup; focused Home, Diagnostics, History, Settings, Onboarding, shell, UI-state, theme light/dark contrast, dynamic-color source-guard, DNS card semantics, and detection route-tag tests passed | Manual TalkBack run; remaining Appium skips are environment/fixture-specific rows such as biometric prompt routing and active scan state |
| Test-lab and automation tooling | Partial | `doctor.sh`, `start.sh`, `stop.sh`, UDP echo, mock relay ready/auth-failure/malformed-response tests, toxiproxy delay/reset, archive redaction, shell/YAML/Compose validation all passed; `test-feature-checklist-coverage.sh` verifies every checklist section has structured evidence, and `test-feature-checklist-coverage-fixtures.sh` covers verifier negative paths; `check-feature-gap-readiness.sh` records read-only readiness for root, TalkBack, handover, routed netem, relay matrix, and remote workflow gaps, and `test-feature-gap-readiness.sh` verifies required JSON rows, stable remote-workflow wording, and valid relay-matrix manual status; `check-feature-test-signoff.sh` now fails fast when the completion audit is incomplete or required readiness rows remain blocked/manual, and `test-feature-test-signoff.sh` verifies the guard pass/fail/error paths in the local-network-lab workflow; `check-relay-matrix-config.sh` validates the operator-provided relay matrix shape before provider-backed runs and emits machine-readable required path/scenario lists, and `test-relay-matrix-config.sh` covers duplicate relay IDs, kind/ID mismatches, duplicate/invalid scenarios, literal endpoint refs, sensitive literal values, and Provider Relay Matrix template parity; `run-vpn-e2e.sh` now fails fast when Maestro is unavailable and `--skip-maestro` is not explicit, seeds the debug automation launch contract before Maestro, and drives stable Compose test-tag ids; `run-proxy-e2e.sh` now gives proxy mode the same lab/probe orchestration and asserts no RIPDPI foreground service remains after Maestro disconnect; `adb-run-probe.sh --print-broadcast` validates custom endpoint overrides for routed/public lab hosts without requiring an attached device, and the Android debug probe now turns configured relay endpoints into `relayReady` evidence; the committed Maestro smoke pack passes on the physical Pixel 8 Pro after retargeting to current mode-card and bottom-nav resource IDs; Appium runner now selects current debug APKs, supports targeted pytest args, handles local downgrade installs, starts Appium 3 with `uiautomator2:adb_shell`, infers local Android SDK env, cleans up external document/settings activities between tests, supports start-configured permission launch, reaches the full Advanced Settings LazyColumn, and passed launch/navigation, Config/DNS/permission, onboarding, advanced/host-pack, diagnostics/logs/support/theme, diagnostics-tail, and all 7 workflow journeys on the physical device; cached Docker base images are reused before pulling, which kept the local lab refresh green during Docker Hub timeout; local-network-lab workflow syntax parses after adding the precondition, custom-broadcast, readiness-preflight, and relay-matrix checks; netem apply/drop/clear scripts passed in a disposable Linux namespace; `test-lab/chaos/netem/README.md` now documents the routed Linux VM evidence run, packet-loss probes, QUIC-drop probes, cleanup, and manual evidence fields; `test-lab/README.md` now gives operator steps for rooted physical device, physical network matrix, provider relay matrix, and TalkBack evidence capture | Linux netem routed VM packet-loss scenario and GitHub Actions run remain unverified locally |
| CI, release, and supply chain | Partial | Local static analysis, release assemble, build logic check, native bloat check, offline analytics unit/sample pipeline, cargo-deny, packet smoke, coverage, Roborazzi, Rust workspace tests, and release APK inspection passed in this pass | Remote CodeQL, remote CI matrix, remote offline analytics workflow, mutation-testing scope confirmation |
| Runtime mode by DNS by relay | Partial | Proxy mode with lab DNS and relay-off path covered; VPN with local DNS and relay-off path covered; diagnostics-only with local DNS covered; test-lab mock relay readiness and app-side `relayReady=true` reporting now covered on physical proxy, VPN, and diagnostics runs | Proxy plain override, proxy encrypted DNS, and production provider runs |
| Runtime mode by packet strategy | Partial | Instrumented packet-smoke, Kotlin strategy workflow tests, native packet tests, and native strategy/desync tests passed; QUIC unsupported is reported as degraded by Android debug probe | Runtime matrix across proxy, VPN, diagnostics, and rooted strategies |
| Relay by runtime mode | Partial | Mock relay service readiness plus app-side debug-probe relay-readiness reporting; physical proxy, VPN, and diagnostics debug-probe runs confirmed `relayReady=true` in their runtime paths | Production relay paths remain open |
| Network fault matrix | Partial | Toxiproxy delay and reset scenarios applied and cleared; probe reports degraded QUIC unsupported rather than stale success; proxy process-kill restart verified on device; VPN process-kill relaunch verified no false running UI state; Linux netem scripts applied UDP loss and QUIC-drop rules and cleared them inside a container namespace; routed-netem runbook now names the required Linux VM/router topology checks, VPN/diagnostics probe commands, cleanup, and evidence fields | UDP packet loss in routed VM, malformed HTTP, TLS alert, handover, DNS timeout/reset across proxy/VPN/diagnostics |
| Feature definition of done | Partial | Direct tests, runtime smoke, failure injection, redaction, docs, and CI gates covered for the local test-lab/root/build-packaging fixes | Manual and provider-backed release matrix remains open |

## Bugs Found and Fixed During This Pass

| Finding | Fix commit |
| --- | --- |
| Test-lab doctor could report success when services were dead because wrapper logic suppressed failures | `c1c4eed4 fix(test-lab): fail doctor on dead services` |
| Emulator root detection missed AOSP `su 0 id` behavior | `46c2b1fb fix(service): support emulator su detection` |
| Diagnostics support archive redaction did not cover endpoint-like logcat fields | `d220cc47 fix(diagnostics): redact logcat archive endpoints` |
| Release APK included `DebugProbesKt.bin` from coroutine debug probes | `ce7e4cc6 fix(build): exclude coroutine debug probe asset` |
| Installed release app emitted WorkManager debug scheduler lines in the app-owned log scan | `bfa8cf0d fix(app): suppress release WorkManager debug logs` |
| Debug automation `SERVICE_PRESET=live` overwrote real sticky-service state to idle during process-kill relaunch verification | `0e7b312d fix(app): preserve live automation service state` |
| Home owned-stack remediation could not open the RIPDPI browser route because the Home UI dropped `OPEN_OWNED_STACK_BROWSER` actions and did not preserve the target URL | `8d8f3c7b fix(app): open owned stack browser from home` |
| Config action rows could expose a focusable/clickable node without a direct localized accessibility label when the row was partially visible at the bottom of the viewport | This commit adds localized action-row semantics and a focused regression test |
| Nested/detail route audit found partially visible mode-editor switches, log chips, DNS option cards, and detection settings routes without sufficient direct accessibility labels or route tags | This commit adds direct component semantics, localized detection settings copy, route-tag regression tests, and device rerun evidence across all supported locales |
| Upgrading a seeded v0.0.6 install to the current APK crashed on startup because the diagnostics Room builder enabled destructive migration and then reset upgrade fallback by calling the downgrade-only fallback | This commit keeps the production diagnostics database on a single destructive-migration policy, adds a version-2 builder regression test, and records the fixed device upgrade pass |
| `run-vpn-e2e.sh` continued after missing Maestro, which let VPN-mode smokes spend time building/installing only to fail later with `VPN_NOT_ACTIVE` instead of naming the missing connect-flow precondition | This commit makes Maestro required unless `--skip-maestro` is explicit, documents the manual-connect requirement, and records the fast-fail verification |
| `start-lab.sh` retried Docker Hub pulls even when required base images were already cached, causing avoidable local-network-lab refresh failures during registry timeouts | This commit checks the local Docker image cache before pulling base images when the temporary Docker credential config is active |
| Maestro VPN flows targeted obsolete `Connect`/`Disconnect` labels and, after the Home mode-card redesign, the local card started proxy mode instead of Android VPN transport | This commit uses stable Home mode-card test-tag resource ids, targets the VPN card for VPN E2E, and primes the debug automation launch contract with `SERVICE_PRESET=live` before Maestro starts the real service |
| Appium smoke could not run locally because the runner used a stale APK path, rejected local debug downgrades, missed Python dependency validation, started Appium 3 without the scoped `uiautomator2:adb_shell` feature, lacked Android SDK env export, used an obsolete Appium Python import path, and expected the removed `home-connection-button` Home selector | This commit updates the runner, compatibility imports, resource-id lookup fallback, and Home page object selectors, then records a passing three-test Appium smoke subset |
| Additional Appium Config/DNS/permission tests were stale after the Config screen redesign, DNS mode/protocol card workflow, and permission flow changes; a full Appium attempt also showed external document/settings activities could cover the app between tests | This commit adds DNS mode/protocol test tags, closes common external activities in the Appium fixture, supports start-configured launch requests for the VPN permission dialog, updates stale Config/DNS/permission Appium tests, and records a passing seven-test physical-device subset |
| Additional Appium mode-editor/settings/history/onboarding/advanced/host-pack/about tests were stale after long-form screen growth, biometric backup-PIN gating, History filter semantics, and fixture-specific advanced/host-pack visibility | This commit adds UiScrollable-backed page-object lookup, updates the affected tests to current visible flows, and records a 13-test physical-device subset with 9 passed and 4 explicit fixture-gap skips |
| Appium Advanced Settings scrolling used too-small incremental swipes, so reachable late-form controls were treated as fixture-hidden skips | This commit expands the bounded Appium LazyColumn scroll step and records a focused advanced/host-pack rerun with 5 passed and 0 skipped |
| Appium Advanced Settings toggle lookup still used the shorter scroll helper, so the current installed Appium run could not reach `advanced-toggle-tcp-fast-open` on the long form | This commit switches advanced toggle interactions to the bounded incremental scroll helper and records a focused passing rerun plus the full onboarding/advanced/host-pack slice |
| Additional Appium customization, permission, diagnostics, DNSCrypt, strategy-chain, and biometric tests were stale after bottom-sheet semantics, permission-summary tags, Tools-section scrolling, DNS protocol selection, chain DSL syntax, and emulator biometric routing behavior changed | This commit adds the `biometric_locked` data preset, visible-text and incremental-scroll Appium helpers, updates affected tests to current selectors/workflows, and records a 12-item physical-device slice with 10 passed and 2 biometric emulator-state skips |
| Logs screen filters could be clipped below the viewport because the page body was not vertically scrollable and the log stream consumed the remaining height | This commit makes the Logs content scroll vertically, bounds the stream height, updates the Appium severity-filter lookup, and records a passing Logs filter rerun |
| Support bundle export left Android's chooser activity on top of later Appium launches | This commit force-stops `com.android.intentresolver` during fixture cleanup and allows a longer ready-tag timeout for cold starts |
| Additional Appium diagnostics/history/settings tests were stale after the Diagnostics Dashboard/Tools redesign, persisted device telemetry/history, and Material dropdown semantics | This commit updates the affected assertions and page objects, treats retained device data as explicit skips, and records the passing 29-35 physical-device slice |
| Appium diagnostics tail tests expected inline profile cards, an `overview` section, always-idle scan state, and synthetic `all` History filter chips that no longer match the current UI/state model | This commit retargets the tests to the active profile summary, Dashboard section, explicit non-idle scan skips, dynamic History filter chips, and scrolled Tools actions, then records the passing 36-46 physical-device slice |
| Appium diagnostics tail tests still treated absent scan actions, absent event severity chips, live Home mode-card shape, and landscape Settings scrolling as hard failures instead of fixture-dependent states | This commit adds explicit fixture skips for unavailable raw-scan/event-severity controls, uses long-form scroll helpers for Tools and customization rows, accepts any visible Home mode card in the live-service preset, and records a passing 36-46 rerun |
| Maestro smoke flows expected launch arguments to deep-link into Settings and Advanced Settings and still targeted the legacy Home connection button instead of current mode-card actions | This commit drives Maestro through visible bottom-nav and Settings controls, retargets Home/start-stop assertions to current mode-card resource IDs, and records a passing physical-device smoke pack |
| The onboarding Skip action was visible but not actionable through Appium or raw taps because the intro scaffold pager was layered above the top action row for hit testing and accessibility | This commit keeps the top action above the pager, exposes Skip as a Material text button, adjusts the Appium fallback tap to the actual top-action position, and records a passing focused onboarding rerun |
| Maestro was installed in the default `~/.maestro/bin/maestro` location but not on `PATH`, so `run-maestro-smoke.sh` and `run-vpn-e2e.sh` still rejected a valid local Maestro install | This commit resolves Maestro from `MAESTRO_BIN`, `PATH`, or `~/.maestro/bin/maestro`, updates operator docs, and records passing smoke and VPN E2E reruns through the fallback path |
| Appium workflow journeys were stale after the Home mode-card redesign, Config preset removal, configured-mode permission launch path, DNS card workflow, Diagnostics Dashboard/Tools redesign, Mode editor long-form growth, and Settings landscape list-position retention | This commit updates the workflow fixture/page objects/tests to current route tags, long-form scroll behavior, configured-mode launch arguments, DNS mode selection, Diagnostics Tools actions, Mode editor proxy validation, and Settings route resets, then records a passing 7-journey physical-device workflow run |
| Maestro smoke flows inherited the device's landscape orientation, which clipped the Home mode action below the visible viewport and made the cold-launch selector fail despite a valid installed app | This commit sets portrait orientation before every committed Maestro flow and retargets cold launch to the visible local-mode action, then records a passing four-flow smoke rerun |
| Full Appium rerun exposed stale DNS custom-form assumptions and a diagnostics cold-start timing edge | This commit makes the DNS Appium page object wait for protocol-specific custom fields, centers the lower DNSCrypt protocol card before clicking it, scrolls to the custom save action before asserting visibility, and gives the diagnostics empty-state launch a local 60s ready timeout. Targeted rerun passed the custom DoH and DNSCrypt cases and skipped the diagnostics empty-state row for persisted device data; the full Appium suite then passed with 68 tests, 28 explicit skips, and one warning. |
| Appium activation-window tests targeted obsolete hour/day-of-week dimensions and checked only currently visible nodes, so reachable activation controls were reported as fixture skips | This commit retargets the tests to current round and payload-size dimensions, scrolls before activation visibility checks and save clicks, and records a focused activation rerun with 3 passed and one warning. |
| Appium background-guidance and backup-PIN tests used a battery-review state where Home intentionally merges background guidance into the permission recommendation banner, and the backup-PIN row expected warning and saved-PIN clear controls in the same fixture | This commit targets standalone background guidance with granted permissions, scrolls before guidance and backup-PIN lookups, checks the biometric-locked warning/editor state, and records a focused rerun with 5 passed and one warning. |
| Appium History detail coverage used hard-coded connection id `0`, while the seeded History rows now expose UUID-backed resource IDs and the bottom-sheet container tag is not exported through the UIAutomator tree | This commit taps the first dynamic connection row by prefix, treats stable detail content as the sheet visibility fallback, and records a focused History rerun with 4 passed and one warning. |
| Appium strategy-audit coverage still skipped report UI because debug launches could seed settings but not a deterministic completed diagnostics report, and the Diagnostics UI ignored the persisted active profile on initial render | This commit adds the `diagnostics_report_demo` preset, seeds a completed full-matrix report with resolver recommendation data, initializes the Diagnostics profile selection from persisted settings, and records a focused report/audit rerun with 6 passed and 2 explicit precondition skips. |
| Remote CI on `origin/main` exposed native hotspot-budget failures in `ripdpi-monitor-engine` plus stale Logs Roborazzi goldens | `a3424cdf` splits monitor-engine contract fixture and runtime recording helpers out of over-budget files, refreshes the two Logs screen goldens, and records passing hotspot, architecture-health, monitor-engine compile, contract-fixture, and focused Roborazzi verification. |
| Latest installed Appium full-suite attempts exposed another Advanced Settings long-form scroll miss and a transient diagnostics route launch timeout | `7cf8da1f` makes bounded Appium scrolling search back upward after exhausting forward swipes, avoids a visible-then-stale lookup race, retries automation launch once on readiness timeout, and records passing focused reruns for the repaired dropdown and diagnostics launch paths. |
| Clean Appium launches reset settings but left persisted diagnostics runtime/history rows in place, so History empty-state coverage skipped on populated devices | This commit adds a debug-only diagnostics history reset store, clears runtime/history tables before seeding reset-state automation presets, preserves diagnostics profiles/catalog metadata, and records a focused Appium empty-state rerun with the History rows passing. |
| Debug probe accepted and forwarded `relay_endpoint` but the Android probe JSON always emitted `relayReady=null`, so mock relay parity could not be proven from app-side probe artifacts | This commit adds the debug-only relay readiness handshake, emits `relayReady=true/false`, treats relay handshake failure as a hard probe failure, and adds focused parser/verdict tests. |
| Proxy relay-ready smoke had no reusable runner, and the initial ad hoc Maestro disconnect assertion matched a different card's generic `Inactive` text instead of proving the local proxy card disconnected. The service host also did not fall back to plain `stopSelf()` when `stopSelfResult(startId)` returned `false`. | This commit adds `run-proxy-e2e.sh`, tightens the proxy Maestro flows around the local card's connected state, asserts no leftover RIPDPI foreground service after disconnect, and adds a focused service stop-self fallback unit test. |
| Current Appium History search/filter coverage typed a no-match query in reset state and then assumed connection mode filter chips still existed | This commit treats the empty-state branch as valid when no History connection rows match the search, preserving filter-chip assertions for populated fixtures and recording the focused plus full-suite installed-tool reruns. |
| Current Appium support-bundle coverage tapped export and ended before Android's chooser/documents surface appeared, so the async external activity could cover the next Diagnostics launch | This commit waits for the external export package, closes chooser/documents surfaces in `finally`, and records the focused support-bundle plus diagnostics-empty-state repro and the final full-suite installed-tool rerun. |
| Current remote-gate refresh reproduced the `origin/main` architecture-health failure locally because `DetectionResolverNetworkStack.kt::readUnsignedShort()` used a multi-line expression body that the architecture scanner measured through the following class body | This commit converts `readUnsignedShort()` to a small block-bodied helper, preserving DNS wire parsing behavior while giving the architecture scanner an explicit function boundary. Focused diagnostics-data ktlint/test checks, architecture health, native hotspot budgets, full `staticAnalysis`, CI-equivalent Roborazzi verification, and the Rust workspace test script passed after the fix. |

## Current Open Gaps

These are not product pass/fail findings yet; they are missing environments or
unexecuted manual rows from `docs/feature-test-checklist.md`.

- Rooted physical device coverage for helper extraction, helper startup,
  privileged packet operations, readiness timeout, and cleanup.
- Physical cellular, Wi-Fi-to-cellular, cellular-to-Wi-Fi, IPv4-only,
  IPv6-only, captive, and limited-path network runs.
- Full production relay provider matrix across proxy, VPN, diagnostics,
  restart, invalid credential, reset, timeout, malformed response, DNS fallback,
  and network handover.
- Manual accessibility coverage for TalkBack.
- Linux routed VM netem packet-loss scenario; the operator runbook exists in
  `test-lab/chaos/netem/README.md`, but the run still needs a routed Linux
  VM/router carrying device traffic.
- Remote GitHub Actions, CodeQL, offline analytics, and mutation workflow
  confirmation for the local commits ahead of `origin/main`.

## Next Concrete Runs

1. Run a rooted physical-device pass for root-helper startup and privileged
   operations.
2. Run a manual network pass for cellular, handover, IPv4-only, and IPv6-only.
   Keep the Maestro/Appium path available for repeating private-DNS coverage
   after route, resolver, or VPN startup changes.
3. Execute the provider-backed relay matrix, using the passing mock-relay proxy
   and VPN debug-probe runs as the local baseline, then one production relay at
   a time.
4. Run manual TalkBack verification and attach the transcript or screen
   recording reference.
5. Run the routed Linux VM netem scenario with device traffic carried through
   the routed namespace or VM, then attach the lab archive.
6. Publish a review branch, complete the required pull request checks/reviews,
   merge to `main`, then verify remote CI, CodeQL, offline analytics, and
   mutation workflows for the merged commit.
