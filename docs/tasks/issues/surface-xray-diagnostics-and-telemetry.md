---
id: OUT-1786264762917694
title: Surface Xray diagnostics and telemetry
kind: feature
status: done
area: outbound
priority: medium
owner: unassigned
parent: EPC-1786264762917329
blocked_by: []
spec_mode: required
openspec_change: out-1786264762917694-surface-xray-diagnostics-and-telemetry
created: 2026-04-24
updated: 2026-08-28
status_detail: Exact source 9b18e5122 passed local APK/emulator acceptance and hosted CI 33199013272; protected PR455 integration as baeaf98ca is verified.
closed_at: "2026-08-28T19:44:16Z"
closed_reason: All acceptance criteria and required evidence passed.
evidence_summary: "Exact source9b18e5122: CI33199013272 passed44 jobs+17 intentional skips; API27/33/35, release/debug, linked Xray and real API35 Xray TUN acceptance passed. Protected PR455 merged as baeaf98ca; merge tree equals tested source. Local API35 ARM64 lifecycle24/24 and real Xray2/2 passed; verified APK/AAR/ELF evidence retained."
---

## Summary

Expose Xray provider state in Home, Diagnostics, exports, and service telemetry.

## Motivation

The app should make Xray failures diagnosable without turning user profiles or live endpoints into logs. Existing diagnostics already distinguish native proxy, relay, WARP, and tunnel state; Xray needs the same typed treatment.

## Scope

- In scope: runtime snapshot fields, Xray version, readiness, listener state, outbound health, config-validation errors, ping/stat probes where safe, and redacted export summaries.
- Out of scope: full packet capture of tunneled traffic and endpoint disclosure in logs or task notes.

## Acceptance criteria

- [x] Home connection stages identify Xray provider readiness and provider failures distinctly from tunnel failures. — `XrayConnectionStage` (Validating → StartingEngine → ListenerReady → ProbingOutbound → Connected, with a `ProviderFailed` branch) plus `XrayProviderFailureClass` (protect-loop / DNS-loop separated from a dead server); `fromSnapshot`/`canTransition` derivation covered by `XrayProviderDiagnosticsTest`. RENDERED (`:app`): `HomeXrayProviderBanner` surfaces the stage on Home with a provider-DISTINCT tone (`XrayProviderTone`/`WarningBanner` Info/Warning/Restricted — protect-loop & DNS-loop use `Restricted`, NEVER the tunnel destructive `Error` treatment); a compact `XrayProviderSettingsStatusRow` mirrors it on Settings. Both consume the live `ServiceTelemetrySnapshot.xrayProviderSnapshot` (Home via `MainUiState.xrayProviderSnapshot`, Settings via `SettingsViewModel.xrayProviderSnapshot`) and are Roborazzi-locked over all five fixtures (`XrayProviderStatusScreenshotTest`, light+dark). DEVICE-PENDING: live snapshots populated from a *real* running engine.
- [x] Diagnostics can run a provider-path check through the active Xray mode (wired + CI-tested with fakes; live device run still OPEN). — `XrayProviderDiagnosticsProbeRunner` (`:core:service`) runs Version + ListenerReadiness + WrapperPing in-process against the ACTIVE running provider via `XrayNativeBridge`, with `StatApi` HARD-GATED to a not-applicable result (never invoked in-process); driven through `XrayProviderSessionController.runProbes()`. The `:app` Diagnostics Tools surface triggers it user-only via `DiagnosticsViewModel.runXrayProviderProbe()` → `DiagnosticsXrayProviderController` → the process-wide `XrayProviderProbeCoordinator` (registered by the active session, returns null when no Xray session is bound), mirroring the `DiagnosticsDpiToolsController.run*()` pattern (no auto/reachability scan); the `XrayProviderStatusCard` renders the stage + failure class + probe results. Probe runner + session controller + coordinator are unit-tested offline with `FakeXrayNativeBridge` (`XrayProviderDiagnosticsProbeRunnerTest`, `XrayProviderSessionControllerTest`) and the card is Roborazzi-locked over all five fixtures (`XrayProviderStatusScreenshotTest`). DEVICE-PENDING: the probe results against a *real* running libXray engine (real `version()`/`listenerReady()`/`isAlive()` reads) require the gomobile `.so` on a device — the host JVM never classloads the real bridge.
- [x] Export/share summaries redact profile credentials and live endpoints. — `XrayProviderTelemetrySummaries` routes every endpoint/secret through `XrayProfileRedactor`; verified by `XrayProviderDiagnosticsTest` (offline).
- [x] Xray API/stat probing is used only when enabled safely for the Android runtime topology. — `StatApi` probe kind is typed and flagged child-process-only (never in-process for the Android TUN topology); the safe set is Version/WrapperPing/ListenerReadiness. Type-level gate verified offline; the in-process-safety claim itself is enforced by the `:core:service` runtime and is device-verified, not run here.
- [x] Regression fixtures cover provider healthy, config invalid, protect failure, DNS-loop suspected, and outbound unreachable states. — `XrayProviderDiagnosticsFixtures` (all five states) asserted by `XrayProviderDiagnosticsTest` (15 tests green offline).

## Progress

**2026-05-30** — Diagnostics/telemetry data substrate landed (commit `feat(xray): surface typed Xray provider diagnostics and redacted telemetry`) in `:core:data:runtime-state`: `XrayProviderSnapshot`, the typed readiness/listener/outbound/failure-class axes, `XrayConnectionStage` Home staging, the user-triggered probe-kind/report types, redacted telemetry summaries, and the five-state regression fixtures — 15 unit tests green offline. Remaining (blocked on toolchain/hardware): live snapshot population (libXray `Ping`/`XrayVersion`, real listener readiness, stat API) lands in `:core:service` and the Home/Diagnostics Compose surfaces in `:app`; both are device/gomobile-verified and were not run on the offline toolchain.

## Design notes

If Xray metrics/API mode is not safe in-process, prefer wrapper `Ping`, `XrayVersion`, listener readiness, and existing tunnel telemetry for the first build.

## Risks / open questions

- Provider diagnostics can accidentally become a reachability scanner. Keep it user-triggered and tied to the active profile.

## Links

- [[Epic - Xray provider mode]]
- [[Run Xray as managed VPN relay runtime]]
- ripdpi-android-xray-provider-plan-2026-04-24

## Work log

- 2026-06-05: Data substrate fully landed in `core/data/runtime-state` (XrayProviderSnapshot, XrayConnectionStage, XrayProviderFailureClass, XrayProviderProbeKind/ProbeResult/ProbeReport, XrayProviderTelemetrySummaries, XrayProviderDiagnosticsFixtures — 15 unit tests); criteria 1/3/4/5 verified. Criterion 2 (live probe population via libXray/gomobile in `:core:service`) and Home/Diagnostics Compose surfaces (`:app`) are absent from source — blocked on gomobile/libXray packaging task.
- 2026-06-11 (offline re-verify): `:core:data:runtime-state:testDebugUnitTest` green — `XrayProviderDiagnosticsTest` (15), 0 failures. Criteria 1/3/4/5 stay code-complete (typed staging, redacted summaries, in-process-safe probe gate, five-state fixtures); criterion 2 (live probe population in `:core:service` + Home/Diagnostics Compose) stays OPEN on the AAR/device gate — see `docs/native/libxray-unblock-checklist.md`. Status stays `blocked`.
- 2026-06-15 (`:core:service` live-population backend landed + CI-verified with fakes): The provider-path probe and live-snapshot population layer now exist in `:core:service` and are wired into the VPN session. New: `XrayProviderSnapshotDeriver` (pure derivation of a privacy-safe `XrayProviderSnapshot` from orchestrator `VpnProviderState` + bridge `version()`/`listenerReady()`/`isAlive()` + render findings + last protect-failure, failure detail through `XrayProfileRedactor.redactText`), `XrayProviderDiagnosticsProbeRunner` (Version/ListenerReadiness/WrapperPing in-process, StatApi not-applicable), `XrayProviderSessionController` (route build from the durable profile, secret-bearing config held only across `orchestrator.start`, secret-free snapshot stream), `XrayManagedTunnel` (`TunnelUpstream.Xray` → loopback `LocalProxyEndpoint` over `VpnTunnelRuntime`; Native is a guard error), `XrayProviderRouteBuilder` (renders from `DurableXrayProfileStore`; `Rejected` carries only typed findings, never the secret config), and `VpnServiceXrayProtectController` (direct-JNI `VpnService.protect(int)` → same `VpnProtectFailureMonitor` as the native `ProtectSocketFdProtector`, fail-closed). Additive `ServiceTelemetrySnapshot.xrayProviderSnapshot` (runtime DTO, no proto/schema bump) is threaded `VpnRuntimeTelemetryReporter → ServiceStatusReporter → RuntimeTelemetryProjection`, populated on the EXISTING telemetry loop (no new timer) and only when the Xray provider is active. `VpnRuntimeCompositionCoordinator.startComposedRuntime` branches on the durable selection: Xray → `XrayProviderSessionController` (protect-first, ready-then-tunnel); Native → EXISTING path BYTE-IDENTICAL. Verified offline: `:core:service:testDebugUnitTest` (1128 tests, 0 failures — incl. `XrayProviderSnapshotDeriverTest`, `XrayProviderDiagnosticsProbeRunnerTest`, `XrayProviderSessionControllerTest`, `XrayManagedTunnelTest`, `XrayProviderRouteBuilderTest`, `VpnServiceXrayProtectControllerTest`, and the coordinator-wiring `ServiceSessionModuleTest`), `:core:data:runtime-state:testDebugUnitTest` (111, 0 — incl. `DurableXrayProfileStoreTest` round-trip + no-plaintext-leak), `:core:engine-api:testDebugUnitTest` (52, 0), `:core:data:catalog:testDebugUnitTest` (green), and an offline-stub compile of `:core:engine` (`-Pripdpi.prebuiltXrayAarDir=/tmp/ripdpi-empty`, `ripdpi.linkXray` unset) — proving no host-JVM dependency on the real gomobile `.so`. STILL OPEN (device-pending): real Xray VPN egress, live snapshots from a running engine, and the Home/Diagnostics/Settings Compose rendering (`:app` phase). Status stays `blocked` on the device-egress + `:app`-rendering gate.
- 2026-06-15 (`:app` provider rendering landed + Roborazzi-locked): The Home/Diagnostics/Settings Compose surfaces now render the typed Xray provider state DISTINCTLY from tunnel failures. New (`:app`): `XrayProviderStatusPresentation` (stage/failure-class → provider tone + label mapping; provider failures use `WarningBanner` Info/Warning/Restricted, NEVER the tunnel destructive `Error`), `XrayProviderStatusCard` (Diagnostics card: stage + failure class + engine/listener/outbound rows + config findings + user-triggered probe results), `HomeXrayProviderBanner` (Home banner, shown only while a provider session is active), `XrayProviderSettingsStatusRow` (compact read-only Settings status), and `DiagnosticsXrayProviderController` (user-only probe trigger mirroring `DiagnosticsDpiToolsController.run*()`). Wiring: `MainUiState.xrayProviderSnapshot` ← `ServiceTelemetrySnapshot.xrayProviderSnapshot` for Home; `SettingsViewModel.xrayProviderSnapshot` for Settings; `DiagnosticsViewModel.runXrayProviderProbe()` → `XrayProviderProbeCoordinator` (NEW process-`@Singleton` seam in `:core:data:runtime-state`; the active `XrayProviderSessionController` registers `runProbes` on start and clears on stop, returning null when no Xray session is bound). New strings landed in all 8 locales (`values` + 7 translated); `config/i18n/translatable-keys.txt` regenerated. Verified offline: `:app:testGithubDebugUnitTest` (1262 tests, 0 failures), `:core:service:testDebugUnitTest`, `:core:data:runtime-state:testDebugUnitTest`, `:app:verifyRoborazziGithubDebug` for `XrayProviderStatusScreenshotTest` (10 NEW baselines — 5 fixtures × light/dark), `:app:lintGithubDebug` (MissingTranslation severity=error passes), `scripts/ci/check-translation-export.sh` (manifest in sync), and `:app:assembleGithubDebug` (real libXray `.so`/AAR links with all new code). STILL OPEN (device-pending): real Xray VPN egress and live snapshots from a running engine on a device. Status stays `blocked` on the device-egress gate.
- 2026-06-15 (import → durable-store WRITE wired — final non-device connector): The import surface now feeds the durable production stores, so selecting the XrayVpn provider mode runs via libXray instead of translating to a native relay. `DefaultXrayProfilePersistence.persist` (`:app`) persists the validated `XrayProfile` to the Keystore-split `DurableXrayProfileStore` and flips the durable `XrayProviderSelectionStore` to Xray (the signal `XrayProviderSessionController.isXraySelected()`/`start()` reads). The typed profile comes from the VALIDATED `XrayImportParser` (render → `XrayConfigValidator` gate), not a hand-rolled converter, so import success implies a connectable profile; XrayVpn fails closed (Finish gated, nothing persisted) when no validated profile exists. Native options set the selection to Native, clear any orphaned Xray secret at rest, and keep relay activation + mode write BYTE-IDENTICAL. Adversarially reviewed (security-reviewer + pr-reviewer PASS — secrets persist only via the Keystore split, no plaintext/log leak). Verified offline: `:app:testGithubDebugUnitTest`, `:core:data:runtime-state:testDebugUnitTest`, `:app:ktlintCheck`/`:app:detekt`, `check_architecture_health.py --staged` (0 new indicators); no new module edge, no new strings. The provider path is now wired END-TO-END in production; the ONLY remaining gate is real device egress (live `RunXrayFromJSON` traffic + protect-denial fail-closed need a device + live Xray server). Status stays `blocked` on the device-egress gate.
