filters:
  and:
    - file.folder == "docs/tasks/issues"
views:
  - type: table
    name: All tasks
    columns: [title, type, status, area, priority, owner, parent, updated]
    sort:
      - column: updated
        direction: desc
