# Reference Relay

Use this directory for a wrapper that starts the real RIPDPI relay/server
implementation locally. Reference relay E2E tests should remain the source of
truth for protocol compatibility; mock relay tests cover edge cases only.
