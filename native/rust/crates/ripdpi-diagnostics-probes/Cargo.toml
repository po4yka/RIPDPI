[package]
name = "ripdpi-diagnostics-probes"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
futures = { workspace = true }
hickory-resolver = { workspace = true }
libc = { workspace = true }
ripdpi-diagnostics-classification = { workspace = true }
ripdpi-diagnostics-contracts = { workspace = true }
ripdpi-diagnostics-http = { workspace = true }
ripdpi-failure-classifier = { workspace = true }
rustls = { workspace = true }
tokio = { workspace = true, features = ["net", "time", "rt"] }
tokio-rustls = { workspace = true }
webpki-roots = { workspace = true }

[features]
default = ["compat-facade"]
# Default-on compatibility marker. The `compat` namespace is empty today;
# root exports are unconditional.
compat-facade = []
