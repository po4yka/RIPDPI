{
  "schemaVersion": 1,
  "family": "relay_kind",
  "name": "relay_mieru",
  "wireId": "mieru",
  "summary": "The 'mieru' relay kind — a Mieru outbound with username/password auth, tcp|udp transport, off|low|middle|high multiplexing, and a 1280..1500 MTU; INLINE_SETTINGS_BACKED, in-process Rust backend, TCP only.",
  "layers": [
    {
      "id": "proto",
      "path": "core/data/model/src/main/proto/app_settings.proto",
      "marker": "| \"mieru\"",
      "fixHint": "Append \"mieru\" to the trailing comment of `string relay_kind = 171;` so the Kotlin drift test sees the kind."
    },
    {
      "id": "kotlin_relay_constant",
      "path": "core/data/settings/src/main/kotlin/com/poyka/ripdpi/data/RelaySettings.kt",
      "marker": "const val RelayKindMieru = \"mieru\"",
      "fixHint": "Define the RelayKindMieru constant alongside the other RelayKind* constants. The constant must equal the wire string."
    },
    {
      "id": "kotlin_descriptor",
      "path": "core/service/src/main/kotlin/com/poyka/ripdpi/services/RelayKindDescriptor.kt",
      "marker": "kindId = RelayKindMieru",
      "fixHint": "Add a RelayKindDescriptor row to RelayKindDescriptors with kindId = RelayKindMieru and the capability tuple tcp=true, udp=false, reusable=false, supportsOutboundBindIp=false."
    },
    {
      "id": "rust_transport_descriptor",
      "path": "native/rust/crates/ripdpi-relay-core/src/transport_descriptor.rs",
      "marker": "kind_id: \"mieru\"",
      "fixHint": "Mirror the Kotlin descriptor row in RELAY_TRANSPORT_REGISTRATIONS with matching capability fields (tcp=true, udp=false, reusable=false, supports_outbound_bind_ip=false) and a build_mieru backend builder."
    }
  ],
  "checklist": [
    "core/data/model/src/main/proto/app_settings.proto — add \"mieru\" to the trailing comment of `string relay_kind = 171;` plus the relay_mieru_* config fields.",
    "core/data/settings/src/main/kotlin/com/poyka/ripdpi/data/RelaySettings.kt — define RelayKindMieru and the Mieru protocol/multiplexing/mtu normalizers.",
    "core/service/src/main/kotlin/com/poyka/ripdpi/services/RelayKindDescriptor.kt — add the RelayKindDescriptor row (INLINE_SETTINGS_BACKED, finalmask NONE).",
    "core/engine-api/src/main/kotlin/com/poyka/ripdpi/core/RelayNativeConfig.kt — add the mieru* wire DTO fields.",
    "native/rust/crates/ripdpi-relay-core/src/transport_descriptor.rs — RelayTransportRegistration row whose capability bits match the Kotlin descriptor exactly.",
    "Existing tests: RelayKindDescriptorDriftTest, relay_transport_descriptors_cover_every_kind_exactly_once."
  ]
}
