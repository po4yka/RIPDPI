[package]
name = "ripdpi-relay-mux"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tokio = { workspace = true, features = ["io-util", "macros", "rt", "sync", "time"] }

[lints]
workspace = true
