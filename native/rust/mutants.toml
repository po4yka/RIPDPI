timeout_multiplier = 3.0
minimum_test_timeout = 30

# All native workspace packages are mutation targets. Prefer narrow source
# excludes over package-wide disablement when a specific path is not worth
# mutating.
