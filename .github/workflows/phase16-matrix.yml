name: Phase 16 Matrix

on:
  workflow_dispatch:
    inputs:
      matrix_filter:
        description: Optional comma-separated Phase 16 matrix entry ids
        required: false
        default: ""
        type: string
      android_serial:
        description: Optional Android serial for self-hosted runners
        required: false
        default: ""
        type: string
      prepare_hook:
        description: Optional executable hook on self-hosted runners for transport or IP-family setup
        required: false
        default: ""
        type: string
      cleanup_hook:
        description: Paired executable hook that restores the self-hosted runner network state
        required: false
        default: ""
        type: string
      include_real_provider:
        description: Include real-provider SIM rows; requires matching self-hosted carrier runners
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  define-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.emit.outputs.matrix }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate Phase 16 lab matrix fixture
        run: python3 scripts/ci/phase16_matrix.py validate

      - id: emit
        name: Emit GitHub matrix
        run: |
          include_real_provider=()
          if [[ "${{ inputs.include_real_provider || false }}" == "true" ]]; then
            include_real_provider+=(--include-real-provider)
          fi
          matrix_json="$(python3 scripts/ci/phase16_matrix.py emit-github-matrix --filter "${{ inputs.matrix_filter || '' }}" "${include_real_provider[@]}")"
          echo "matrix=$matrix_json" >> "$GITHUB_OUTPUT"

  phase16-matrix:
    needs: define-matrix
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.define-matrix.outputs.matrix) }}
    runs-on: ${{ fromJSON(matrix.runsOnJson) }}
    timeout-minutes: 60
    env:
      PHASE16_ENTRY_ID: ${{ matrix.id }}
      PHASE16_EXECUTION_KIND: ${{ matrix.executionKind }}
      PHASE16_TRANSPORT: ${{ matrix.transport }}
      PHASE16_IP_FAMILY: ${{ matrix.ipFamily }}
      PHASE16_ROOTED: ${{ matrix.rooted }}
      PHASE16_MODE: ${{ matrix.mode }}
      PHASE16_NETWORK_CONDITION: ${{ matrix.networkCondition }}
      PHASE16_SCENARIO_FILTER: ${{ matrix.scenarioFilter }}
      PHASE16_CAPTURE_MODE: ${{ matrix.captureMode }}
      PHASE16_RUNNER_REQUIRED: ${{ matrix.runnerRequired }}
      PHASE16_EVIDENCE_TIER: ${{ matrix.evidenceTier }}
      PHASE16_CARRIER_NAMESPACE: ${{ matrix.carrierNamespace }}
      RIPDPI_PHASE16_PREPARE_HOOK: ${{ inputs.prepare_hook || '' }}
      RIPDPI_PHASE16_CLEANUP_HOOK: ${{ inputs.cleanup_hook || '' }}
      ANDROID_SERIAL: ${{ inputs.android_serial || '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: ./.github/actions/setup-android-rust
        if: matrix.executionKind != 'l7_adversarial_emulator'
        with:
          rust-components: ""
          rust-targets: x86_64-linux-android
          rust-cache-shared-key: rust-android
          rust-cache-save: "false"

      - name: Enable KVM for emulator
        if: matrix.executionKind == 'android_packet_smoke' && contains(matrix.runsOnJson, 'ubuntu-latest')
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Provision Phase 16 emulator
        if: matrix.executionKind == 'android_packet_smoke' && contains(matrix.runsOnJson, 'ubuntu-latest')
        run: |
          bash scripts/ci/start-android-emulator.sh \
            --avd phase16 \
            --api 34 \
            --arch x86_64 \
            --target google_apis

      - name: Install packet smoke host tools
        if: matrix.executionKind != 'l7_adversarial_emulator' && contains(matrix.runsOnJson, 'ubuntu-latest')
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y jq tcpdump tshark
          sudo setcap cap_net_raw,cap_net_admin=eip "$(command -v tcpdump)"

      - name: Run Phase 16 matrix entry
        run: bash scripts/ci/run-phase16-matrix-entry.sh

      - name: Capture Phase 16 emulator diagnostics
        if: always() && matrix.executionKind == 'android_packet_smoke' && contains(matrix.runsOnJson, 'ubuntu-latest')
        run: |
          bash scripts/ci/capture-android-emulator-diagnostics.sh \
            --avd phase16 \
            --output-dir "$RUNNER_TEMP/phase16-android-diagnostics"

      - name: Stop emulator
        if: always() && matrix.executionKind == 'android_packet_smoke' && contains(matrix.runsOnJson, 'ubuntu-latest')
        run: bash scripts/ci/stop-android-emulator.sh --avd phase16

      - name: Upload Phase 16 matrix artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ matrix.artifactName }}
          path: |
            build/phase16-matrix/${{ matrix.id }}/**
            ${{ runner.temp }}/phase16-android-diagnostics/
          if-no-files-found: warn
          retention-days: 14
